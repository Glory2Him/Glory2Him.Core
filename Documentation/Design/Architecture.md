# Architecture

Carries `G2H Design.md` §12 "Component Architecture", §16 "Recommended Service
Responsibilities" and §17 "Recommended API Design" — the layer model, the
per-service responsibility list, and the API surface those services expose.

Section numbers below carry an **`ARC` prefix** and are otherwise the numbers
these sections already had: an old `§12.N` is now `§ARC12.N`, an old `§16.N` is
now `§ARC16.N`, and an old `§17.N` is now `§ARC17.N`, and nothing was
renumbered, reordered, merged or split in the move. That is the
prefix-preserving rule of §IDX1.5. It means this file jumps from `ARC12` to
`ARC16` to `ARC17` with nothing between, so its numbering is not contiguous
with the other area files, and the contents list below is what makes those gaps
read as a table of contents rather than as missing content. **§ARC16.7.5 stands
between §ARC16.7.2 and §ARC16.7.3, out of numeric order, exactly as `16.7.5`
stood between `16.7.2` and `16.7.3` before the move** — that position is
accepted permanently, ruled in #568 rather than renumbered now or later:
renumbering it, its ARC16.7.4 neighbour and the ARC16.7.3 named above would, as
measured when this was ruled in September 2026, repoint 102 citations of the
three of them (71 of them outside `Documentation/` in source and tests),
leave two sections competing for the same *(formerly §16.7.N)* grep anchor,
and §IDX1.5 reason 3 already forbids the reorder — the same kind of
preservation `Security.md`'s header
block names for §SEC14.6.1's heading-level anomaly. The
other `Documentation/Design/*.md` files carry their own prefixes — `APR`,
`DOM`, `EVN`, `SEC`, `UI` — so a bare `§ARC12.5` is unambiguous. Where this
file cites one of them, the map at the top of
[`G2H Design.md`](../G2H%20Design.md) is what resolves it.

**`Events.md` is the one file a citation into it cannot be derived for.** It
renumbered rather than prefix-preserved, so an into-Events citation is looked
up in that file's own *(formerly §10.X)* annotations instead of having a
prefix applied to the number it already had. The citations this file carries
into it — `§EVN2`, `§EVN4`, `§EVN5`, `§EVN7`, `§EVN18` and `§EVN19`, alongside
the fifteen `§EVN` occurrences that were already resolved that way before this
extraction — were each looked up against `Events.md`'s own headings rather
than derived by rule: `§EVN10.17` would have been the wrong answer for what is
actually `§EVN18`.

This repository's C# and TypeScript comments cite design sections
extensively, so every relocated section also carries a *(formerly §12.X)*,
*(formerly §16.X)* or *(formerly §17.X)* annotation naming its old position.
The literal old string still appears on the right heading, so an old citation
resolves by grep even though the citable number is now prefixed.

**Contents**

- [ARC12. Component Architecture](#arc12-component-architecture-formerly-12)
  - [ARC12.1 Architecture Overview](#arc121-architecture-overview-formerly-121)
  - [ARC12.2 Broker Layer](#arc122-broker-layer-formerly-122)
    - [ARC12.2.1 StorageBroker](#arc1221-storagebroker-formerly-1221)
    - [ARC12.2.2 EventBroker](#arc1222-eventbroker-formerly-1222)
    - [ARC12.2.3 SecurityBroker](#arc1223-securitybroker-formerly-1223)
    - [ARC12.2.4 SecurityAuditBroker](#arc1224-securityauditbroker-formerly-1224)
    - [ARC12.2.5 AIBroker](#arc1225-aibroker-formerly-1225)
    - [ARC12.2.6 BlobStorageBroker](#arc1226-blobstoragebroker-formerly-1226)
  - [ARC12.3 Foundation Service Layer](#arc123-foundation-service-layer-formerly-123)
    - [ARC12.3.1 Entities With No Service Above Their Foundation](#arc1231-entities-with-no-service-above-their-foundation-formerly-1231)
  - [ARC12.4 Processing Layer](#arc124-processing-layer-formerly-124)
    - [ARC12.4.1 ContentItemProcessingService](#arc1241-contentitemprocessingservice-formerly-1241)
    - [ARC12.4.2 LinkProcessingService](#arc1242-linkprocessingservice-formerly-1242)
  - [ARC12.5 Orchestration Layer](#arc125-orchestration-layer-formerly-125)
    - [ARC12.5.1 ContentType — no orchestration](#arc1251-contenttype--no-orchestration-formerly-1251)
    - [ARC12.5.2 ContentItemSettingOrchestrationService](#arc1252-contentitemsettingorchestrationservice-formerly-1252)
    - [ARC12.5.3 ApprovalOrchestrationService](#arc1253-approvalorchestrationservice-formerly-1253)
    - [ARC12.5.4 ApprovalReviewerOrchestrationService](#arc1254-approvalreviewerorchestrationservice-formerly-1254)
  - [ARC12.6 Controller Layer](#arc126-controller-layer-formerly-126)
  - [ARC12.7 SQL Storage](#arc127-sql-storage-formerly-127)
    - [ARC12.7.1 The second DbContext — reading the identity store](#arc1271-the-second-dbcontext--reading-the-identity-store-formerly-1271)
  - [ARC12.8 Event System](#arc128-event-system-formerly-128)
  - [ARC12.9 Content Analysis Service](#arc129-content-analysis-service-formerly-129)
  - [ARC12.10 The Build Environment](#arc1210-the-build-environment-formerly-1210)
- [ARC16. Recommended Service Responsibilities](#arc16-recommended-service-responsibilities-formerly-16)
  - [ARC16.1 ContentItemService](#arc161-contentitemservice-formerly-161)
  - [ARC16.2 AssociationService](#arc162-associationservice-formerly-162)
    - [ARC16.2.1 The §DOM6.10 Facet Gate On The Association Write](#arc1621-the-dom610-facet-gate-on-the-association-write)
  - [ARC16.3 ContentItemSettingsService](#arc163-contentitemsettingsservice-formerly-163)
  - [ARC16.4 ApprovalService](#arc164-approvalservice-formerly-164)
  - [ARC16.5 ApprovalReviewService](#arc165-approvalreviewservice-formerly-165)
  - [ARC16.6 ApprovalSettingsService](#arc166-approvalsettingsservice-formerly-166)
  - [ARC16.7 ApprovalOrchestrationService](#arc167-approvalorchestrationservice-formerly-167)
    - [ARC16.7.1 The settled shape](#arc1671-the-settled-shape-formerly-1671)
    - [ARC16.7.2 The verdict, and who may see it](#arc1672-the-verdict-and-who-may-see-it-formerly-1672)
    - [ARC16.7.5 Resetting a decided round](#arc1675-resetting-a-decided-round-formerly-1675)
    - [ARC16.7.3 What is deliberately not built](#arc1673-what-is-deliberately-not-built-formerly-1673)
    - [ARC16.7.4 Review requests and reviewer candidates](#arc1674-review-requests-and-reviewer-candidates-formerly-1674)
  - [ARC16.8 AssociationOrchestrationService](#arc168-associationorchestrationservice)
  - [ARC16.9 The endpoint-anchored association read](#arc169-the-endpoint-anchored-association-read)
- [ARC17. Recommended API Design](#arc17-recommended-api-design-formerly-17)
  - [ARC17.1 Content Endpoints](#arc171-content-endpoints-formerly-171)
  - [ARC17.2 Feed Endpoints](#arc172-feed-endpoints-formerly-172)
  - [ARC17.3 Topic Endpoints](#arc173-topic-endpoints-formerly-173)
  - [ARC17.4 Association Endpoints](#arc174-association-endpoints-formerly-174)
  - [ARC17.5 Approval Endpoints](#arc175-approval-endpoints-formerly-175)
  - [ARC17.6 Media, Share and Crawler Endpoints](#arc176-media-share-and-crawler-endpoints-formerly-176)

---

## ARC12. Component Architecture *(formerly §12)*

### ARC12.1 Architecture Overview *(formerly §12.1)*

The component design follows a layered service architecture using:

1. Brokers
2. Foundation Services
3. Processing Services
4. Orchestration Services
5. Controllers
6. SQL Storage
7. Event System
8. Content Analysis Service

The primary dependency direction is:

```text
Controllers
    -> Orchestration Services
        -> Processing Services
            -> Foundation Services
                -> Brokers
                    -> SQL Storage / External Infrastructure
```

**Not every layer exists for every entity, and a caller binds to the highest layer that exists.** The distinction between the two business layers is the entity count, not the amount of logic:

1. A **processing service** owns higher-order logic for **one** entity type, composing repeated calls to that entity's single foundation service — retrieve-then-branch, probe-then-write, read-then-filter. Multi-step is not multi-entity: a version fork writes two rows through one foundation service and is still processing work.
2. An **orchestration service** exists only where a flow spans **two or more** entity types, and coordinates them through their respective processing (or, where none exists, foundation) services.

An entity whose flows never leave its own type therefore has no orchestration service at all, and `ContentItem` is that case (§ARC12.4.1). Where an entity has both, the orchestration sits on top of the processing service rather than beside it.

3. **A single-entity type with no higher-order logic of its own needs nothing above its foundation.** Being approvable does not by itself earn a layer. `Tag`, `Reaction`, `Comment` and `BibleReference` are approvable and Single-Row (§APR7.5.1), so they never fork, and §EVN18 rule 1 lets the approval workflow subscribe to their foundation facts directly. `ApprovalReview` and `ApprovalComment` are not approvable entities at all (§APR7.5), so §APR7.5.1 and §EVN18 do not apply to them in either direction; they qualify on the entity-count rule alone. What earns a processing service is either a version fork (§EVN18 rule 2, which makes one mandatory for a Versioned approvable entity) or a cross-row rule the foundation cannot own — a probe over the unfiltered store, an effective-value merge. See §ARC12.3.1.

This layering is a decomposition rule, not a security boundary — §SEC14.6 still requires every service to gate its own callers independently, because an exposer may bind to any layer directly.

### ARC12.2 Broker Layer *(formerly §12.2)*

Brokers abstract infrastructure, persistence, external systems, security access, event publication, and AI integrations.

Current intended brokers:

1. `StorageBroker`
2. `EventBroker`
3. `SecurityBroker`
4. `SecurityAuditBroker`
5. `AIBroker`
6. `BlobStorageBroker` *(designed, not built — §DOM5.6.1)*

#### ARC12.2.1 StorageBroker *(formerly §12.2.1)*

`StorageBroker` is responsible for SQL persistence through EF Core, and therefore for transaction scope. It exposes transactions only through the storage client's bulk operations today; §EVN19 rule 8 requires a single-entity unit-of-work seam alongside them.

**A storage broker authors no query condition, and reaches its database only through the storage client — ruled 2026-09-22, not built.** The requirement, as stated on 2026-09-22: *"Where, Select, Any is all business logic with conditions that are not tested. If those logic change then no tests will catch it. I also don't want the logic to go straight to the dbcontext it should go via the efClient methods."* Both halves were true when this was ruled. Eighteen keyed reads in `StorageBroker` (counted on `main` on 2026-09-22, across eight entity partials), and both reads in `IdentityCoreStorageBroker` (§ARC12.7.1), compose `Where`, `Select` or `OrderBy` and a predicate-taking terminal operator directly against a `DbSet`, bypassing `IEFCoreClient` because it had no keyed-read surface to route through. Brokers get no unit tests, so no test anywhere fails if one of those conditions is inverted. The conditions began in the services, unit-tested over LINQ-to-Objects, and moved into the broker so the query could be awaited with the caller's cancellation token — the history `ContentItemNarrowReadTests`' class summary records, and the concern #528 raises for `AccessBroker`. The unit coverage did not move with them. This ruling keeps what that move bought and restores what it cost.

1. **The condition is service logic, and the broker authors none of it.** No storage-broker member composes `Where`, `Select`, `OrderBy` or any other query operator, or calls a terminal operator that takes a predicate. Which rows answer a question — tombstones in or out, which row wins when two match, which column comes back — is a decision (`the-standard-core` ts-core-006, `the-standard-brokers` ts-brokers-001), and a decision belongs where it is unit-tested.
2. **No broker member queries through its DbContext.** Every read and write goes through `IEFCoreClient`. A `DbSet<T>` property is model registration, never a query source for a broker member. This binds `IdentityCoreStorageBroker` as well as `StorageBroker`; `EFCoreClient` accepts any `DbContext`.
3. **The storage broker's caller authors the condition as a query-shaping function and passes it down.** The caller is the foundation service, or `AccessBroker` for its gathers — a placement §APR8.6.1 owns and this ruling does not reopen. The function takes the entity's queryable and returns the shaped query using `System.Linq` operators only; a service that imports `Microsoft.EntityFrameworkCore` to shape a query is a finding. The storage broker hands the function straight to the client, and the client applies it to its own source and runs the terminal operator **awaited, with the caller's `CancellationToken`**. The client gains three terminal shapes, additive to its existing surface: the matching rows, optionally projected; the first matching row or none; and whether any row matches. Names follow the storage language of ts-brokers-003.
4. **#528's properties therefore hold unchanged.** The token reaches SQL, the thread is not held for the round trip, and nothing is materialised to be filtered in memory — the shaped query executes in SQL as the in-broker one did. Only its author changes.
5. **The unit test executes the condition; receiving it proves nothing.** The caller's unit test mocks the storage broker, and the mock applies the function it was handed to an in-memory set seeded with a matching row and, for each term, a row that misses on that term alone — so dropping or inverting any term fails a test. A test that only asserts that some function was passed is refused. This is the coverage the conditions had before they moved into the broker.
6. **Integration proves what LINQ-to-Objects cannot stand in for.** EF refuses an untranslatable query at run time, not at compile time; a string comparison follows the column's collation, not C#'s ordinal rule; and an index's claim — at most one row, which rows a filtered index admits — is a claim about the catalogue. Those stay proven against the real catalogue in the integration suites. Where a column is pinned to a binary collation, the in-memory evaluation and SQL agree by construction (§EVN25).
7. **The unfiltered collection read stays, for exposure.** `SelectAll<Entity>sAsync`, returning a live queryable, remains what a collection read composes over and hands up. Composing it further is allowed; running a terminal operator over it inside Core — synchronous or awaited — to answer a question is not, because rule 3 is how a question is asked.
8. **The broker's new input is native.** ts-brokers-006 admits primitive types and native models. A query-shaping function over the native model carries no infrastructure type, and it is the same composition the Standard's own template has the service perform over the queryable a broker returns — authored in the same layer, with only the terminal operator moved below it.

**What this supersedes.** The shape recorded on the keyed reads — predicate and await both in the storage broker — and the XML comments on `IStorageBroker.*.cs` that justify it. The *decisions* those comments carry — a read is unfiltered because §SEC14.7 visibility is the service's to apply, a tombstone still owns its version number — move with their conditions into the service and remain load-bearing there. The mechanism of #528's criterion 1: the awaited terminal operator runs in the client, reached through the storage broker, not in `AccessBroker`. That also dissolves the conflict #528 records with #532's `AsQueryable()`-pinned tests, because `AccessBroker` then calls no EF operator. §APR8.6.2.1's gate-6 rule is amended to match.

**Layer placement.** Condition: foundation service, or `AccessBroker` under §APR8.6.1. Execution: storage client, reached through the storage broker as a pass-through. No layer is skipped and no dependency is added — `G2H.StorageClient` is already the storage broker's client. No entity count changes, because no condition crosses an entity.

**Risks.** No schema, no event, no migration; each read converts independently and each conversion is reversible. The client's new members are additive, so its version takes a minor increment and no other consumer is affected. The residual risk is the one rule 6 exists for: a condition passes its unit test in LINQ-to-Objects and can still translate differently in SQL, so the integration proof is required wherever rule 6 names a difference.

**Open, not ruled here.**

1. **The processed-event probe would be authored fourteen times.** Every foundation substrate's dedup helper asks the same two-term question (§EVN19 rule 4). Under rule 3 each authors it and unit-tests it. The alternative — treating an equality lookup on a unique index as storage addressing rather than a condition, and leaving it in the broker — would be an exception to the requirement above. It is not granted here.
2. **`IdentityCoreStorageBroker.SelectIdentityUsersInRolesAsync` correlates three tables in one query** (roles, user-roles, users). A single-entity shaping function cannot express it. Whether it becomes three single-entity reads composed by its caller, or the client gains a shape over more than one set, is to be settled before that read is converted.

#### ARC12.2.2 EventBroker *(formerly §12.2.2)*

`EventBroker` is responsible for publishing and receiving domain events. It creates the substrate's client on first use rather than in its constructor, so building the object graph touches no database (§EVN24).

#### ARC12.2.3 SecurityBroker *(formerly §12.2.3)*

`SecurityBroker` is responsible for user identity, claims, roles, and permission checks.

#### ARC12.2.4 SecurityAuditBroker *(formerly §12.2.4)*

`SecurityAuditBroker` is responsible for security-sensitive audit logging and traceability.

#### ARC12.2.5 AIBroker *(formerly §12.2.5)*

`AIBroker` is responsible for infrastructure-level access to AI capabilities used by the content analysis workflow.

#### ARC12.2.6 BlobStorageBroker *(formerly §12.2.6)*

`BlobStorageBroker` *(designed, not built — §DOM5.6.1)* is responsible for binary storage in Azure Blob Storage (Azurite in development) — upload, download, delete, exists, and list-by-prefix.

### ARC12.3 Foundation Service Layer *(formerly §12.3)*

Foundation services own core CRUD, validation, and business rules for one entity.

Current intended foundation services:

| Number | Name | Purpose |
| --- | --- | --- |
| 1 | `ContentItemService` | CRUD, validation, and versioning rules for content items. |
| 2 | `ContentItemSettingsService` | CRUD and policy resolution for content item settings. |
| 3 | `ApprovalService` | Approval record creation, status transitions, and uniqueness enforcement. |
| 4 | `ApprovalSettingsService` | Approval policy rule management and effective setting resolution. |
| 5 | `ApprovalCommentService` | CRUD for approval comments. |
| 6 | `ApprovalReviewService` | Reviewer decision recording, the row-local half of eligibility validation, and owner-only write permission. Threshold evaluation is **not** here (§ARC12.3.1). |
| 7 | `TagService` | CRUD and validation for tags. |
| 8 | `ReactionService` | CRUD and validation for reaction definitions. |
| 9 | `CommentService` | CRUD and validation for comments. |
| 10 | `BibleReferenceService` | CRUD and validation for Bible references. |
| 11 | `LinkService` *(future)* | CRUD and validation for links. |
| 12 | `AttachmentService` *(future)* | CRUD and validation for attachments (§DOM5.6.1–§DOM5.6.7), plus the ordinary and bypass approve transitions the host-derived approval needs (§DOM5.6.5); its approve operation must call `IAccessBroker` (§APR8.6.1). |

`ContentType` is not in this list — it is a fixed enum (§DOM3.6), not an entity, so it has no foundation service.

#### ARC12.3.1 Entities With No Service Above Their Foundation *(formerly §12.3.1)*

`Tag`, `Reaction`, `Comment`, `BibleReference`, `ApprovalReview` and `ApprovalComment` need **nothing above their foundation service**. Each touches one entity type, so under the §ARC12.1 rule there is nothing to orchestrate. `Tag`, `Reaction`, `Comment` and `BibleReference` are additionally approvable (§APR7.5) and **Single-Row** (§APR7.5.1), so there is no version fork and §EVN18 rule 1 permits the approval workflow to subscribe to their foundation facts directly. `ApprovalReview` and `ApprovalComment` are **not approvable entities** — neither appears in §APR7.5, neither is an `EntityType`, and neither implements `IApproval`, so §APR7.5.1 has no row for them and §APR7.5.1 rule 2 makes a missing row a hard error rather than a Single-Row default. Their withdrawal rests on the §ARC12.1 entity-count rule alone.

§ARC12.5 previously planned an orchestration for each of these. Those subsections are withdrawn. The rules that were real are restated here as foundation responsibilities; the rules that described versioning are deleted rather than moved, for the reason at the end of this section.

**Shared rules — `Tag`, `Reaction`, `Comment`, `BibleReference`:**

1. **Control fields are defined by the §APR9.7.1 rule 2 subtraction test, not by a list here.** A property is caller-editable content unless it is a member of `IKey`, `IAudit`, `IApproval` (or `IVersion` / `ISortOrder` / `IConfidence` where present), is derived, or is declared create-only. An enumerated list is what fell behind the model last time — it omitted `IsApprovedByBypass`, `ApprovedByBypassReason`, `UpdatedBy` and `UpdatedWhen` — so this section deliberately does not restate one.

    **Three interface members are nonetheless caller-supplied on some path, and "never accepted from an external caller" was false for them:**
    - `ApprovalStatus` **is** accepted on add, restricted to `Draft` or `Submitted` (§SEC14.7 posture A.1), and on modify through the `Draft` ↔ `Submitted` carve-out (§APR9.2 rules 4–6).
    - `DeletionReason` **is** accepted on the remove path, as caller-supplied free text capped at 500.
    - `IsApprovedByBypass` / `ApprovedByBypassReason` are pinned against storage on modify but **unguarded on add** on these four entities — a gap, recorded here rather than asserted away.

2. **On update the foundation pins named fields against storage; it does not load-and-map.** The earlier wording — *"loads the current entity from storage and maps only the permitted caller-supplied fields onto it"* — describes a mechanism **no service implements**. Each service reads the stored row, runs `ValidateAgainstStorage<Entity>OnModify` to refuse changes to an enumerated pin list, and then writes **the caller's own object**.

    The difference is not academic: a load-and-map is closed by default and a pin list is open by default, so anything absent from the pin list is silently writable. That is exactly why the gaps in rule 1 exist, and why the natural-key question in rule 2a below has the answer it does. The permitted content fields remain `Name` (`Tag`), `Name` and `UnicodeEmoji` (`Reaction`), `Content` (`Comment`), and `Reference`, `Translation`, `Scripture` (`BibleReference`).

2a. **Natural keys are not uniformly protected, and the asymmetry is real.** These three fields are the §APR7.5.1 rule 4 ground for their types being Single-Row, so a rename is a structural act rather than an edit:

    | Field | Pinned on modify? | Unique index | Filtered on `IsDeleted`? |
    | --- | --- | --- | --- |
    | `BibleReference.USFM` | **Yes** — refused, "immutable after creation" | `UX_BibleReferences_USFM` | **Yes** |
    | `Tag.Name` | **No** — rename permitted today | `IX_Tags_Name` | **No** |
    | `Reaction.Name` | **No** — rename permitted today | `IX_Reactions_Name` | **No** |

    Two consequences follow and neither is intended. A `Tag` rename to an unused name succeeds and silently re-points everything that resolved that tag by name. And because the two `IX_*_Name` indexes are unfiltered, a **soft-deleted** `Tag` named "Grace" permanently blocks any new or renamed tag from taking that name — `BibleReference` does not have this problem. The rename question interacts with #201, which covers the same unfiltered indexes; it is recorded here and decided there.
3. Soft delete follows §EVN4. **Dependent associations are left untouched** — a soft delete breaks no link, and association visibility is the read-time composite of §SEC14.3 rules 3–4, not a flag written on delete (the §ARC12.4.1 responsibility 5 correction).
4. A soft-deleted row must not be visible on any content item.
5. **A soft-deleted row must also not be usable as an endpoint for a *new* association.** This is distinct from rule 4: rule 4 hides existing links, this one refuses new ones. It is enforced where associations are created, not here.

    The rule is stated as an **invariant, not as a mechanism.** Today it holds because `AssociationOrchestrationService` resolves each endpoint through that endpoint's own foundation service, whose single-row read posture reports a soft-deleted row as not-found. #211 proposes replacing exactly that read — so whatever replaces it must preserve the invariant, and this section must not be read as requiring the current mechanism.

5a. **The precondition is "resolvable and not deleted", never "approved".** The withdrawn subsections said to *"associate an **approved** X"*. That does not survive, and its absence is deliberate rather than an oversight: `AssociationOrchestrationService` reads no endpoint's `ApprovalStatus`, `IsPublished` or `PublishDate` at all. What the visibility-filtered read does enforce is that the endpoint exists, is not soft-deleted, and is **visible to the caller** — public, or the caller's own, or the caller holds a review role.

    So an owner or reviewer **can** associate their own `Draft`, `Submitted` or `Rejected` endpoint, and that is correct: the association carries its own independent approval lifecycle, and whether the pair *renders* is the read-time composite of §SEC14.3 rules 3–4. Approval is deferred to read time, not required at write time. An unrelated caller still cannot, because the endpoint reports not-found to them.
6. Each publishes its own `-Added` / `-Modified` / `-Removed` facts on its own addresses (§EVN2), and `ApprovalOrchestrationService` subscribes to them to manage approval records and workflow state (§EVN18 rule 1 — the foundation tier is the correct one for a Single-Row entity).
7. Review dismissal is not their responsibility. Publishing the entity's own `-Modified` fact is sufficient; `ApprovalOrchestrationService` handles dismissal when it receives that fact (§EVN18).
8. State transitions (submit, approve, reject) are the foundation's own operations, each owning a narrower field scope than the general modify (§APR9.7.1 rule 3, §EVN2 rule 7).
9. **An amendment of a terminal row is refused.** §APR7.5.1 rule 3: `Single-Row` means the row that is edited **is** the published row, so there is nothing to fork into and an amendment of a terminal (`Approved` or `Rejected`) row is refused instead, the only route back being an administrator override to `Submitted` that dismisses active reviews (§APR8.8 regardless-rule 1). §DOM3.4 rule 7 generalises it: a terminal row's content is immutable.

    **Implemented on all seven.** `Tag`, `Reaction`, `Comment`, `BibleReference`, `Link`, `ContentItem` and `Association` each refuse it in their general modify, after the write-permission check so the refusal cannot be used to read a row's approval state without the standing to see it.

    Do not confuse it with the rule that was *already* implemented and did not cover it: every one of them refuses a **change** to `ApprovalStatus` outside the `Draft` ↔ `Submitted` carve-out, but that check's condition is guarded by `inputStatus != storageStatus`. A caller who echoed the stored status back unchanged passed it and edited the body of an approved row, with `IsPublished` and `PublishDate` still pinned at their approved values, so the new content went public without re-review. The row-level write gate bounded the exposure to the owner or a review role; it did not close it. That is the hole this rule closes.

    For the two `Versioned` entities the refusal is not a dead end but a redirection: the amendment becomes a new version through `ContentItemProcessingService` (§ARC12.4.1) or `LinkProcessingService` (§ARC12.4.2), which reach the terminal row first and write a new one rather than amending it. The refusal is what makes that fork the only route, because an exposer may bind straight to the foundation and a rule enforced only above it is not enforced (§APR8.6.1).

    **The refusal is unconditional on all seven** — a stored `Approved` or `Rejected` status refuses the modify outright, with no comparison of what the caller sent.

    It was briefly written otherwise, and the reason is worth keeping because it is a trap a future entity will walk into. The fork used to **demote** the previous latest row — flipping a stored latest-version flag to `false` — through this same general modify, so a refusal written against the stored status alone refused the demotion too and broke the very fork the rule exists to redirect amendments into. The first fix was to compare caller-editable content and refuse only when it differed. That was withdrawn twice over: the demotion first moved to its own operation, and then stopped existing at all when the tip became derived (§APR9.7.1 rule 3a, §DOM3.4.1). Nothing legitimate reaches a terminal row through the modify any more, so the blunt refusal is available again and is both simpler and stricter.

    Refusing on the stored status rather than on a status *change* is the whole point, and is a different check from the pin: the pin's condition is guarded by `inputStatus != storageStatus`, which is exactly the guard a caller echoing the stored status back walks through.

    On `Association` the rule is reachable in principle and inert in practice — it has no caller-editable content at all, so its general modify is nothing but the carve-out. It is enforced anyway: the rule belongs to every approvable entity, and one that holds only by accident of the current field list stops holding the moment that list changes.

**Associating them with a content item is not their work.** The withdrawn subsections gave each entity a responsibility to "associate an approved X with a content item by creating an `Association`, validating that X is permitted by resolving the effective `ContentItemSetting`". Creating the link is `Association`'s own flow (§DOM4, §ARC12.5 entry 1), and the rules that governed it survive there rather than here:

**Association rules — `Tag`, `Reaction`, `Comment`, `BibleReference`; cited as `§ARC12.3.1 association rule N`:**

1. A link may only be created when the effective `ContentItemSetting` permits that association type — `TagsAllowed`, `ReactionsAllowed`, `CommentsAllowed`, `BibleReferenceAllowed` (§DOM6.10) — and `LimitReactionsToLoveOnly` further narrows the reaction case. **Where that gate runs, on which acts, and what it refuses is §ARC16.2.1's** — this rule states the constraint and that section states its enforcement, so neither is a copy of the other.
2. The association carries its own approval according to the effective `ApprovalSetting` for its `EntityType` (§APR8.4).
3. An entity is visible on a content item only when both it and the association satisfy §SEC14.3.
4. The same `Tag`, `Reaction`, `Comment` or `BibleReference` may be associated with multiple content items independently — each link is its own `Association` row with its own approval state.

**Where each gate belongs, and neither is built.** The §SEC14.3 composite visibility rule belongs to one private evaluator on `AssociationOrchestrationService`, composing rules 3, 4 and 6 above the foundation's self-only filter over rules 1, 2 and 5; §ARC16.8's shared-rule table row is where that placement lives, and all three of the service's reads — §ARC16.9's anchored read, the by-id read beside it and §ARC16.8's summary read — consume the evaluator rather than any of them owning it. An earlier version of this sentence assigned it to the reaction summary read alone, which made this passage a second home for a rule that has one. The §DOM6.10 settings gate on the write path belongs to §ARC16.2.1, and it reaches its effective setting through `IAccessBroker` rather than through an `IContentItemSettingService` dependency — a broker arm an orchestration may hold at no Florance cost, which is why the older wording naming that service dependency was wrong about the gate as well as about the state. **Both are designed and neither is built, so both gates are unenforced today.** Moving these rules into this section did not implement them; it recorded where they belong, and §ARC16.8 and §ARC16.2.1 are where they are designed to be honoured.

**`ApprovalReview`** — `ApprovalReviewService` owns the decision record, dismissal retention for audit (a dismissed review is never deleted), and a new review permitted after a dismissal. It publishes five facts on four addresses: `-Added`, `-Modified`, `-Removed` (shared by `-HardRemoved`, distinguished by event name) and `-Dismissed`, which is the fact the approval workflow most needs and which the dismiss transition publishes **instead of** `-Modified` (§APR9.7.1).

Three responsibilities were previously misattributed here, in both directions. The corrected split:

| Rule | Where it actually lives |
| --- | --- |
| **Reviewer eligibility** | **A veto, then two tiers, and all three are real** (§SEC14.6 rule 2). The veto comes first and is not a tier: a `ReadOnly` whose scope covers the entity behind the approval refuses the caller before eligibility is asked at all, and no grant overrides it (§SEC18.6 rule 2). It is split like everything else here — tier 1 asks the global `ReadOnly`, and the **scoped** block is asked at tier 2, where the subjects are resolved and the narrow name can be composed. When the entity behind the approval cannot be READ at all — a content item hard-removed out from under it — the narrow name cannot be composed either, and the veto fails **closed** for anyone that entity type's narrow tier covers: a grant that cannot be composed merely leaves the actor needing a wider role, where a block that cannot be composed would hand a sanctioned user the orphaned approval outright. Then the two tiers proper. Tier 1 is in the service: `ValidateUserIsAllowedToReviewApprovals` runs the contribution gate and a coarse, entity-**agnostic** `EndsWith("-Reviewers")`/`"-Publishers"` role match. Tier 2 is `IAccessClient` behind `IAccessBroker`, which narrows that to an **exact** composed role for the entity actually resolved behind the approval, and adds HR-1 self-review, the open-round check and the one-active-review bar. **Tier 1 covers the add path only** — modify and remove run the plain contribution gate, so on those paths tier 2 is the only role check. |
| **Row-level write permission** | The service, and it is **ownership, not eligibility** — owner-only on modify and remove, no role widening (see §SEC14.7 posture D rule 4). Grouping it with the eligibility checks, as earlier drafts did, misdescribes both. |
| **One active review per reviewer** | **Not the service's, and not one place either.** `IAccessClient` refuses it as `ActiveReviewAlreadyRecorded` — but only on **add**, since amend and withdraw pass `IsAmendingOwnReview: true` and skip the bar. The filtered unique index `UX_ApprovalReviews_ApprovalId_CreatedBy` is the backstop and applies to every write that lands an active row. The two surface differently: the decision function yields an unauthorized error that says nothing about duplication, the index yields a dependency-validation error naming a uniqueness rule. **Any statement of the form "this is enforced by X" is wrong whichever X is chosen.** The service's own contribution is a supporting guard — it pins both halves of the index key against storage on modify, so a review cannot be walked onto another approval or have its `CreatedBy` rewritten. |
| **Threshold evaluation** | `IAccessClient`'s `EvaluateConditions`, reached from Core only through `IAccessBroker.MayDecideApprovalAsync` (§APR8.5, §APR8.6.1). **Not** `ApprovalOrchestrationService` — see §ARC12.5.3 R5, which this section previously deferred to and which is itself unbuilt. |

**A drift worth naming, because §APR8.6.1 asserts it cannot happen.** The index's definition of *active* is `StatusId <> Dismissed AND IsDeleted = 0`; the decision function's is *not deleted, verdict not `Dismissed`*, where the verdict is mapped from `ApprovalStatus` with everything that is not `Approved`/`Rejected` folding to `Dismissed`. A review row sitting at `Draft` or `Submitted` therefore **occupies the index slot while being invisible to the counter**. Such a row is corrupt by construction and the sets agree in practice, but the definitions do not, and only the index is authoritative for rows that exist.

The rule that a user with an active review may not also set the entity's `ApprovalStatus` — administrators excepted — is answered from the `ApprovalReview` rows the approval policy already reads (§APR8.6 regardless-rule 1).

**`ApprovalComment`** — control fields `ApprovalId`, `IsDeleted`, `CreatedBy`, `CreatedWhen`, `DeletedBy`, `DeletedWhen`, `DeletionReason`. Approval comments do not participate in the threshold or status-transition workflow.

A comment may only be created against an **existing, non-deleted** approval record. This section previously said to *"enforce that with the foreign key rather than a read of `Approval`, so the service does not acquire a second entity dependency for an existence check"* — **that is withdrawn, and the reason it failed is instructive.** The foreign key enforces only the *existing* half. Soft deletion is a flag and the row stays (§EVN4), so the key still resolves against a taken-down parent, and no constraint can express the *non-deleted* half. Nor could it express the round being open (§APR7.7 rule 2b), which the same gate needs.

The resolution is not a second entity dependency but `IAccessBroker`, which does the **gathering** so the single-entity service does not (§APR8.6.1). `ApprovalCommentService` therefore takes `IAccessBroker` and consults it on add, modify, remove and resolve; the parent's state and deletion arrive as an `AccessVerdict`, and the service still never reads the `Approval` table itself.

`IsResolved` is a **second permitted caller field on update**, alongside `Comment`, and carries **no shape rule on add** — a comment may legitimately be created settled (§APR7.8). Modify is owner-only, and the owner may settle or re-open a **remark** of their own there as readily as through `ResolveApprovalCommentAsync` — it is their row. `IsResolved` is deliberately **not** pinned against storage: pinning it would leave the owner unable to change a field that is theirs, and would have deadlocked every approval under the fail-closed `RequireReviewCommentResolutionBeforeApprovals` (§APR8.4 rule 2) in the window before the transition existed. What the field carries instead is a **gate** rule, not a pin: the amend decision refuses the one transition that lands on a settled **ask** (§APR7.8), so settling an ask stays the resolve operation's to grant while everything else about the field stays the owner's.

`CommentType` is a **third permitted caller field on update**, and is unpinned for the same reason: the row belongs to whoever wrote it, and correcting a remark into an ask is the author changing their own words. It moves nothing on its own — what blocks an approval is `IsResolved`, and a caller who wants to block or unblock says so in that field.

What the transition adds is the **`Administrators` route**, not exclusivity over the field (§SEC14.7 posture D rule 5).

**The update mechanism is pin-against-storage, not load-and-map** — the same correction as shared rule 2 above, verified here separately. `DoModifyApprovalCommentAsync` reads the stored row, refuses changes to an enumerated pin list (`CreatedWhen`, `CreatedBy`, `ApprovalId`, `UpdatedWhen`) and then writes **the caller's own object**. Nothing maps permitted fields onto the stored row. This is why `IsResolved` is writable through modify at all: it is absent from the pin list, and a pin list is open by default.

Its facts are all live: `-Added`, `-Modified`, `-Removed` (shared by `-HardRemoved`) and `-Resolved`, plus the soft-delete operation behind `-Removed`.

**Deleted, not relocated: the versioning rules.** The withdrawn subsections gave `Tag`, `Reaction`, `Comment` and `BibleReference` a full versioning model — fork on modify of an `Approved` row, `IsLatestVersion` demotion, one latest and one published row per `GroupId`, and `GroupId` / `Version` / `IsLatestVersion` in the control-field list. None of the four implements `IVersion` today; all four carry `IApproval` only, and `EntityTypeVersioning` (§APR7.5.1) declares all four Single-Row.

**But they did not all arrive there the same way, and an earlier single sentence — "describing properties those types never had" — was false for one of them.** §APR7.5.1 rule 1 already draws this distinction; this is the split it implies.

| Entity | Did it ever have versioning? | Why the rules are stale |
| --- | --- | --- |
| `Tag`, `Reaction` | **No — never.** Neither the interface nor the properties has existed at any commit. | §DOM5.1 and §DOM5.2 *describe* `GroupId` / `Version` / `IsLatestVersion` on them, but the code never implemented them. The rules described properties that never existed — the documentation drift §APR7.5.1 rule 1 cites as its first example. |
| `Comment` | **No — never.** Same as above; it gained `IApproval`, never `IVersion`. | As above. |
| `BibleReference` | **Yes.** It was born `IKey, IAudit, IVersion, IApproval` with real `ContentItemGroupId`, `Version` and `IsLatestVersion` columns and their unique indexes. | **Withdrawn deliberately**, not never-existed. Commit `91c6571` removed the interface and all three properties together and added `USFM`; migration `20260806014655_DeversionBibleReferenceAndAddUsfmUniqueIndex` dropped the three columns and their indexes and added `UX_BibleReferences_USFM`. The reason is §APR7.5.1 rule 4 — the natural key a fork would violate. §APR7.5.1 rule 1 cites this as its *second*, sharper example: the storage configuration and validations kept referencing the versioning after the model dropped it. |

So for three of the four the rules were never true; for `BibleReference` they were true and were **decided away**. Both are grounds for deletion, but only the second is a design decision worth being able to find again.

### ARC12.4 Processing Layer *(formerly §12.4)*

Processing services own higher-order business logic for a **single** entity type, composing repeated calls to that entity's foundation service. They exist wherever an entity's flows are richer than CRUD but never leave its own type.

Current intended processings:

| Number | Name | Purpose | Status |
| --- | --- | --- | --- |
| 1 | `ContentItemProcessingService` | Content item creation, versioning (in-place vs. fork), duplicate-content enforcement, soft delete, and per-caller read visibility. | Built (§ARC12.4.1) |
| 2 | `LinkProcessingService` | Same shape as ContentItem: `Link` is Versioned, so an amend of a terminal row forks. | Built (§ARC12.4.2) |
| 3 | `AttachmentProcessingService` | Same shape, plus the upload pipeline (§DOM5.6.3), the replacement fork (§DOM5.6.4), and the sweep, purge and orphan operations (§DOM5.6.7). `Attachment` is Versioned, and has no foundation service yet either. | Required, not built — blocked on the missing `AttachmentService` |

**Entry 3 is not optional.** §EVN18 rule 1 makes a service above the foundation a hard prerequisite for a **Versioned** approvable entity, and `EntityTypeVersioning` (§APR7.5.1) declares exactly three Versioned types — `ContentItem`, `Link` and `Attachment`. Until each has one it cannot participate in approval without hitting the fork-emits-two-facts problem of §EVN18 rule 2. `Attachment` is the one still outstanding, and it is blocked twice over: a processing service composes its entity's foundation service, and `Attachment` has none — only a model, a storage broker and an event broker. The foundation comes first. Any other entity earns a processing service only by having higher-order single-entity logic of its own — a cross-row probe, an effective-value merge — because plain CRUD on a Single-Row entity needs nothing above its foundation.

#### ARC12.4.1 ContentItemProcessingService *(formerly §12.4.1)*

`ContentItemProcessingService` owns the full lifecycle of a content item through `ContentItemService`, its single foundation dependency.

It is deliberately **not** an orchestration service. Every one of its flows touches exactly one entity type, so there is nothing to coordinate (§ARC12.1). The version fork is the case that most resembles orchestration and is not: it writes two rows, but both through the same foundation service, for the same entity. There is no `ContentItemProcessingService` above it, and none is planned — see responsibility 5 below for the requirement that used to imply one.

Responsibilities:

1. Process content item creation and modification, enforcing versioning rules and control field integrity.
2. Determine whether an edit results in an in-place update or a new version, based on current `ApprovalStatus`.
3. Answer whether the row an edit names is still the group's tip — the derived question that replaced the stored flag (§DOM3.4.1). The row is reached by id, so without this check a superseded version would be silently editable.
4. Apply model mapping on every write operation — map only the fields that a caller is permitted to change onto a fresh entity loaded from the database before committing. This prevents any caller from tampering with control fields through the update path.
5. Process soft delete of the content item itself, and **nothing else**. Dependent associations are deliberately left untouched: a soft delete breaks no link, and association visibility is a read-time composite evaluated by whoever can resolve both endpoints (§SEC14.3 rules 3–4), not a flag written on delete. This responsibility previously read "flag dependent associations as appropriate", which predates §SEC14.3 and would have made this a cross-entity write — the single requirement that would have forced this service to be an orchestration.
6. Publish its own completion facts — `ContentItemProcessing-Added`, `ContentItemProcessing-Modified`, and `ContentItemProcessing-Removed` — via `IEventBroker` once the processed work has completed. The underlying row-level facts (`ContentItem-Added`, `-Modified`, `-Removed`) are published by `ContentItemService` and must not be republished here (§EVN2).
7. The approval orchestration service subscribes to these events to manage approval records and workflow state.

Business Rules:

1. A content item in `Draft`, `Submitted` or `Dismissed` status may be edited in-place without creating a new version. `Rejected` was listed here and is not: rule 2 makes it terminal, so an edit of a rejected item forks. `Dismissed` takes its place — it is the one non-terminal status this rule never named.
2. A **terminal** content item — `Approved` or `Rejected` — is immutable in place, including to its owner. An owner edit must create a new version with incremented `Version`, which makes the new row the group's tip; nothing is written to the previous version (§DOM3.4.1). `Rejected` is here for the same reason `Approved` is: the row is the record of a decision, and amending it in place would rewrite what was decided. The `Administrators` in-place carve-out this rule used to describe is withdrawn — see rule 10, which is the governing statement.
3. Exactly one version per `GroupId` is the tip — the highest non-deleted `Version` — and the unique index on (`GroupId`, `Version`) is what makes that name a single row.
4. Only one version per `GroupId` may have `IsPublished = true`. (also enforced by database unique index)
5. A content item must not be published until its `ApprovalStatus` is `Approved`. Publication is not a separate act: the foundation's approve transition derives `IsPublished` from the decision it is applying (§APR9.7.1 rule 3), so a row cannot be published except by being approved. What this service adds is the *group* half — demoting the previously published row before the new one is promoted (rule 10, §APR9.7.7 rules 6–7). `ApprovalOrchestrationService` decides; it does not write the entity.
6. The following fields are control fields and must never be accepted from an external caller. They must always be set internally by this service or the approval workflow:
   - `GroupId`
   - `Version`
   - `IsPublished`
   - `PublishDate`
   - `ApprovalStatus`
   - `IsDeleted`
   - `CreatedBy`
   - `CreatedWhen`
   - `DeletedBy`
   - `DeletedWhen`
   - `DeletionReason`
   - `ContentHash`
   - `Slug` (§DOM19.3 — designed, not a column yet)
   - `ShortCode` (§DOM19.7 — designed, not a column yet)
7. On every update, this service must load the current entity from the database and map only the permitted caller-supplied fields — `Title`, `Author`, `Content` and, when the §DOM19.2 column lands, `MetaDescription` — onto that entity before saving. `ContentType` and `PublishDate` were previously in this list and are removed: the first is create-only (business rule 7a), the second is an `IApproval` member written by the approve operation (§APR9.7.1 rule 3).
7a. **`ContentType` is set at creation and may never change.** Reclassifying a content item is not permitted — different content types carry different validation rules, so a `Story` cannot become a `Testimony` by relabelling it; the existing content was never validated against the target type's rules. An item filed under the wrong type is removed and re-created.

   Enforcement belongs in the foundation, not only here: `ValidateAgainstStorageContentItemOnModify` pins `ContentType` against the stored row and rejects a difference, in the same way it pins `CreatedBy` and `CreatedWhen`. §SEC14.6 requires the foundation to be safe when called alone, and `ContentItem-Modifying` is a request address whose caller is established by the envelope's signature and nothing else (§SEC14.6 rule 4). This service dropping it from the permitted map is defence in depth. Note that pinning against storage is identity-independent, so this particular rule holds whatever the context claims — which is why the pins keep their value on that path even where the identity gates would lose theirs.

   A version fork carries the value forward unchanged; it is preserved, never re-chosen. The fork builds the new row from the stored tip, not from the caller's entity, and the foundation pins the fork as well: a version fork is an **add**, not a modify, so `ValidateAgainstStorageContentItemOnModify` never sees it and there is no stored row of its own to compare against. `ValidateAgainstGroupContentItemOnAdd` closes that by pinning `ContentType` against the row's version **group** — when the incoming `GroupId` already has rows, the type must equal theirs; when it does not, this is the group's first version, which is the one add that chooses a type. Without it the fork was the single path that could relabel an item, which also moved the row's `%ContentItem%-%ContentType%-Reviewers` / `-Publishers` tier (§SEC18.6 rule 5) and its duplicate-check bucket (§DOM3.4.2) — so the duplicate probe is keyed on the type the row will actually land with, the stored type on a fork and the caller's in place.
8. Review dismissal is not the responsibility of this service. Publishing `ContentItemUpdatedEvent` is sufficient — `ApprovalOrchestrationService` must handle dismissal when it receives that event.
9. Only the owner (`CreatedBy`) may modify a content item or its versions. A publisher or administrator may amend the text of a `Draft` or `Submitted` item during review (typos/grammar); their identity is then recorded on `UpdatedBy`. `CreatedBy` never changes on an update. **A reviewer may not amend it at all** — the review tier is out of the modify gate at every scope (§SEC14.7 posture A.3), and the amendment window closes for the publisher tier the moment the item turns terminal (rule 10 below).
10. **There is no in-place amendment of a terminal content item, by any role.** An edit of an `Approved` or `Rejected` item forks a new version (§DOM3.4 rules 7–8), including for an administrator — the in-place carve-out this rule used to describe is withdrawn (§DOM3.4 rule 16). An administrator who wants the row itself re-opened uses the status override instead, which is an approval transition.

    **One approval transition does reach this service, and only one: the publication swap.** This rule used to end "and does not reach this service", on the reasoning that approval state is not processing work. That holds for the *decision* and still does — who may approve, and what the outcome is, are settled on the `Approval` row and never here. What does not hold is publication. §APR9.7.7 rule 6 requires that granting approval also demotes the group's previously published row, and rule 7 requires the demote to precede the promote. `IX_ContentItem_IsPublished` is a unique index filtered on `IsPublished = 1`, so that ordering is not a preference: promoting while the incumbent still holds the slot violates the index and the write fails outright.

    Two rows of one entity, in a guaranteed order, is processing work by the layering — a foundation is one row per call, and an orchestration reaching the sibling would need entity services it deliberately does not have (§ARC16.7.1). The published slot is now the **only** thing that makes a group's rows contend: the tip is derived from the highest `Version` rather than stored, so a fork writes one row and needs no ordering at all (§DOM3.4.1). That leaves the published-slot index above as the single constraint any write can violate by ordering two rows of a group wrongly — and one case is enough, because the ordering has to be guaranteed somewhere and this is the only layer that can hold both rows. So the approval command for `ContentItem` and `Link` is addressed to the processing service, which orders the two writes and forwards each to the foundation; every other approvable type is Single-Row (§APR7.5.1), has no group, and keeps the foundation address.
11. Duplicate content rule (§DOM3.4.2): before add or modify, compute `ContentHash` from the normalized `Content` and check for a duplicate per (`ContentType`, `ContentHash`) across non-deleted rows (excluding the item's own `GroupId` on modify). Add → polite acknowledgement without creating; modify → validation error. The acknowledgement is a `ContentItem` composed exactly as the persisted row would have been, and it is composed **in one place with the genuine arm's row** so the two cannot drift apart; no completion fact goes out with it. This service is also where every caller-supplied add rule is asked, above the probe, for the reason §DOM3.4.2 rule 6 gives — the quiet arm never reaches the foundation, so a rule asked only there tells the caller which arm ran.
12. Slug generation (§DOM19.3 — designed, not built): derive `Slug` from `Title` on add and re-derive while the group has never published; freeze it at the group's first publish; copy it forward on a version fork; pin it thereafter. `Slug` is a control field (rule 6) — derived, never accepted from a caller, the same trust posture as `ContentHash`. `ShortCode` is likewise a control field, but its writer is not this service: it is derived by the foundation's approve transition at the group's first publish (§APR9.7.1 rule 3, §DOM19.7) — the only operation that runs at that moment, which rule 10 keeps out of this service.

#### ARC12.4.2 LinkProcessingService *(formerly §12.4.2)*

`LinkProcessingService` owns the full lifecycle of a link through `LinkService`, its single foundation dependency. It is a **processing** service by the §ARC12.1 rule — one entity type, one foundation dependency — and the fork is what earns it: §EVN18 rule 1 makes a service above the foundation a hard prerequisite for a Versioned approvable entity, and `Link` is Versioned (§APR7.5.1).

It is the same shape as §ARC12.4.1 and deliberately not a copy of it. Three things are **absent**, each because a `Link` is not a `ContentItem`:

- **No duplicate-content rule.** §DOM3.4.2 is keyed on (`ContentType`, `ContentHash`) and a link carries neither. Two links to the same URL are a legitimate pair — the same article cited from two stories, under two names — so there is no hash to compute and no `IHashBroker` dependency.
- **No content-type role tier.** §SEC18.6 rule 5 gives the narrow tier only to `ContentItem`, the one entity type carrying a `ContentType`. `Link`'s review tier is two-deep (`Reviewers` / `Link-Reviewers` and the publisher pair, plus `Administrators`), so a `Link-Reviewers` covers every link there is and no per-row role question is asked. The collection filter has no set of reviewable types to resolve.
- **No `ContentType` immutability rule.** There is nothing to reclassify.

Responsibilities:

1. Process link creation and modification, enforcing versioning rules and control field integrity.
2. Determine whether an edit results in an in-place update or a new version, based on current `ApprovalStatus`.
3. Answer whether the row an edit names is still the group's tip, the derived question that replaced the stored flag (§DOM3.4.1).
4. Apply model mapping on every write operation — map only `Name`, `Url` and `LinkType` onto a fresh entity loaded from the database before committing, so no caller can tamper with a control field through the update path.
5. Process soft delete of the link itself, and nothing else. Dependent associations are left untouched for the reason given in §ARC12.4.1 responsibility 5.
6. Publish its own completion facts — `LinkProcessing-Added`, `LinkProcessing-Modified` and `LinkProcessing-Removed` — once the processed work has completed. The row-level facts (`Link-Added`, `-Modified`, `-Removed`) belong to `LinkService` and must not be republished here (§EVN2 rule 5).
7. Serve the per-caller read posture of §SEC14.1 over links, including the group reads whose endpoint shape §ARC17.1 tables for `ContentItem`.

Business Rules:

1. A link in `Draft`, `Submitted` or `Dismissed` status may be edited in place, by its owner or by a reviewer, publisher or administrator.
2. A terminal link — `Approved` or `Rejected` — is immutable in place and belongs to its owner alone: their edit forks a new version and no role may fork on their behalf, because the fork authors a version and it would land in the moderator's name.
3. Exactly one version per `GroupId` is the tip — the highest non-deleted `Version`, named uniquely by the (`GroupId`, `Version`) index — and only one may have `IsPublished = true`, which a filtered unique index enforces. A fork touches neither: it inserts one row, and that row's `Version` makes it the tip without a second write (§DOM3.4.1).
4. A fork leaves the previous tip's `IsPublished` untouched (§DOM3.4 rule 12), so a group that had a published version keeps serving it through the review that follows. A fork off a `Rejected` row has no published version to preserve — a rejected row was never published — so the group simply has no public row until the new version is approved.
5. Only the latest version of a group may be modified.
6. The control fields — `GroupId`, `Version`, `IsPublished`, `PublishDate`, `ApprovalStatus`, `IsDeleted`, `CreatedBy`, `CreatedWhen`, `DeletedBy`, `DeletedWhen`, `DeletionReason` — are never accepted from a caller on add or modify. `PublishDate` is the one that looks like content and is not: it is an `IApproval` member (§APR9.7.1 rule 2) belonging to the approve operation, so neither the add nor the fork carries it.
7. Removal is a takedown, not a moderation step: the owner or an administrator, and no one else. `ApprovalStatus` is left untouched (§EVN5), and an already-removed link is reported as not found.
8. Exactly one processing fact per completed amend, regardless of how many foundation rows the amend wrote.

### ARC12.5 Orchestration Layer *(formerly §12.5)*

Orchestration services coordinate **two or more** entity types and enforce cross-entity workflows, each entity reached through its own processing service where one exists, or its foundation service where none does.

Current intended orchestrations:

| Number | Name | Purpose |
| --- | --- | --- |
| 1 | `AssociationOrchestrationService` | Resolves an association's two endpoints against their respective entity services and runs the retrieve-or-add suggestion on add. Its only operation today is `AddAssociationAsync`. **Its read surface is designed and not built, and it is THREE reads rather than one**: §ARC16.9's endpoint-anchored read, the by-id read beside it, and §ARC16.8's content item reaction summary read. §ARC16.9 also rules that `RetrieveAllAssociationsAsync` is **not** lifted onto this service — the unfiltered collection read stays the foundation's and nothing exposes it. **The §SEC14.3 composite visibility rule — rules 3, 4 and 6 above the foundation's self-only filter over rules 1, 2 and 5, per §SEC14.3's Layer paragraph — is ONE private evaluator that all three of them consume, and where it lives is §ARC16.8's shared-rule table row.** This entry cites that row rather than naming a second home for the rule; an earlier version of this sentence placed the composite on the summary read alone, which disagreed with that row about which member writes it. **Until one of the three is built the composite rule is implemented nowhere**, which is a recorded open gate rather than a closed one. Its caller-facing write surface is §ARC16.8.1's, likewise designed and not built. |
| 2 | `ContentItemSettingOrchestrationService` | **Built.** It spans two entity types: an override's `ContentType` is derived from the `ContentItem` it names rather than accepted from the caller, because the write gate composes the publisher tier from that value (§ARC12.5.2 business rule 6). Reading a second entity is §ARC12.1 rule 2's definition of an orchestration. An earlier note here struck this entry out as single-entity; that held only while every flow stayed inside `ContentItemSetting`, and one does not. A `ContentItemSettingsProcessingService` may still be earned for the effective-setting merge, which reads two rows of ONE entity — per §ARC12.1 it would sit under this orchestration rather than replace it. |
| 3 | `ApprovalOrchestrationService` | Orchestrates the approval **round** — submission, review decisions, policy outcomes, and the denormalized state on the owning entity. Reviewer coordination is entry 5's and the AI reviewer's own lifecycle is entry 4's; see §ARC12.5.3's banner for the split. |
| 4 | `AIReviewerOrchestrationService` | **Built** (#474). Asking Berean, asking again, withdrawing it, and reporting where it stands (§APR8.6.2). Spans `Approval` and `AIReviewerAssignment`. Absent from this table until now, which was drift rather than a ruling. **Extended by #532 and #534 — it holds two subscriptions and a second seam** (§APR8.6.2.1): `Approval-Added` and `Approval-Modified` for automatic assignment, and `IAIReviewerAssignmentWorkflowService` beside the caller-facing `IAIReviewerAssignmentService` it already held. It still maps **two** exception families, `Approval*` and `AIReviewerAssignment*` — the seam is a second door on the foundation class and shares its `TryCatch`, so §ARC16.7.1 finding 2 counts the pair once. The subscriptions bring `IEnvelopeIntegrityBroker` and **not** `IEventBroker`, for the reason §ARC12.5.4 business rule 1 gives: both handlers verify an inbound envelope and cause their write through a seam that publishes for itself. |
| 5 | `ApprovalReviewerOrchestrationService` | **Built** (#521, completed by #522). Who takes part in a round — reviewer candidates, invitations, withdrawal, and name resolution. Spans `ApprovalReviewRequest`, `ApprovalComment` and `IdentityUser`, and reads the `Approval` foundation for the read-triggered repair alone. §APR7.9 rules 6 and 8 run here, as this service's own subscriptions on `ApprovalReview-Added` and `Approval-Modified` (§ARC12.5.4 business rule 4); `ApprovalReviewersController` binds this service alone for the five reviewer routes (#523). **Four dependencies and four arms — an approved deviation, recorded in the register above.** |

> **Six entries were withdrawn from this table, and entries 4 and 5 above are NOT them.** `ApprovalReviewOrchestration`, `ApprovalCommentOrchestration`, `TagOrchestration`, `ReactionOrchestration`, `CommentOrchestration` and `BibleReferenceOrchestration` once held numbers 4–9 here and were each planned before the §ARC12.1 rule was applied. The numbers were reused when `AIReviewerOrchestrationService` and `ApprovalReviewerOrchestrationService` took them; the withdrawals below stand, and none of the six named here is being reinstated under another name — in particular `ApprovalReviewerOrchestrationService` is not `ApprovalReviewOrchestration`, which was withdrawn for touching a single entity type. The reviewer orchestration touches four (§ARC12.5.4). Every one touches a single entity type, so none needs anything above its foundation — `Tag`, `Reaction`, `Comment` and `BibleReference` are additionally Single-Row (§APR7.5.1) so they never fork, while `ApprovalReview` and `ApprovalComment` are not approvable entities at all; their surviving rules moved to §ARC12.3.1, and their versioning rules were deleted. **Why they were deleted is not the same for all four**, and the single sentence that used to be here — "describing properties those types never had" — was true of three and false of the fourth. See §ARC12.3.1's split, which §APR7.5.1 rule 1 already anticipated.
>
> **Entry 1 (`Association`) is provisional, and two things about it are now ruled.** It does read several entity types, so it is not a processing service as written, but it takes seven entity services for a single operation, which breaks the dependency-count guidance regardless of which layer it sits in — four of its seven endpoint branches read a row only to discard it. Its endpoint-resolution design **has been revisited and a shape ruled** — the six far-end entity services collapse onto one `IAccessBroker` endpoint probe — and the classification re-test that shape was waiting for is the second ruling below.
>
> **RULED — the endpoint-resolution redesign lands BEFORE the public CRUD surface, and the earlier reading that the two were separable is reversed.** That reading held while the redesign had no ruled shape. It now has one, and the shape reaches the surface: §ARC16.7.1's counting rule makes **exception families** the thing the guidance measures, and at seven entity services this service owes seven arms — twenty-eight downstream blocks against a ceiling of twelve — every one of which each new public member runs inside. Writing six members, their arms and their exposer's status ladder against a family set about to lose six of its members is rework rather than sequencing. §ARC16.7.1's own finding is the second reason: an overage does not announce itself with an explosion of catch blocks, it announces itself with arms nobody wrote, and a family with no arm reports a routine refusal as a `424` and leaves a dead `Conflict` branch in the exposer. The arms are audited and the dependencies collapsed first; the surface is built on what is left. **Two mechanical items are deliberately NOT gated by this**: making `IAssociationOrchestrationService` public, and registering the service so a host can resolve it at all. Neither touches a dependency or an arm, and whichever piece of work needs them first carries them.
>
> **The registration is a SCOPED registration in the host, and NOT a call to `AddAssociationOrchestrationService()`.** That extension registers the service as a **singleton**, and this service composes `IEventEnvelopeBroker` — so §SEC14.6.1's rule bites directly: *"any broker in the identity chain — and any service that composes one — is `Scoped` or `Transient`, never `Singleton`. A longer-lived consumer of a scoped identity broker is the same defect wearing a different hat."* What to do instead is what the host already does for every other orchestration: `CoreRegistration` registers `IApprovalOrchestrationService`, `IAIReviewerOrchestrationService`, `IApprovalReviewerOrchestrationService` and `IContentItemSettingOrchestrationService` with `AddScoped`, registers `IEventEnvelopeBroker` itself as `AddScoped`, and calls **no** `Add*Service()` helper at all.
>
> **And the extension has no host for which it is correct — it is a live trap rather than a route not taken.** An earlier version of this note kept it alive on the ground that it *"stays correct for a host that does wire subscriptions"*. **Both halves of that were false.** This host **does** wire subscriptions — `Program.Configurations.cs` calls `RegisterAsync()` for *"the participant, all 165 event addresses, and every one of the 109 subscriptions"* — so §SEC14.6.1's *"wires no subscriptions"* antecedent does not even describe it. And the trade that antecedent guards has been bought out: §SEC14.6.1 records that `EventSubscriptionRegistration` binds every handler through a per-delivery `AsyncServiceScope` rather than as a method group on a held service, *"which is what lets the host register them scoped"*. So a singleton over this service buys nothing and costs the identity chain. **The prescription was right and the reason given for it was wrong**, which is worth saying in those words: a correct instruction propped up by a false premise is one review away from being undone. Whether the `Add*Service()` helpers should themselves become scoped is §SEC14.6.1's wider question and is deliberately not answered here; what is settled is that **nothing may cite the method-group trade to justify a singleton**, and that this service is registered scoped. §SEC14.6.1 calls the failure it avoids *silent and total* — every rule enforced correctly against the first principal the process ever saw — so the guard is a **registration-lifetime assertion**, not a behavioural test. The redesign and the composite evaluator also meet, and §ARC16.9 is where that is worked out: *"is this endpoint row visible"* is the question §SEC14.3 rules 3 and 4 ask of each end, it is the same question the redesign's endpoint probe answers, and §ARC16.9's read asks it twice per request. Built before the collapse, that read would reach for six foundation services and put back what the redesign removes — so the ordering is not only cheaper, it is what keeps the read's own dependency ruling true.
>
> **RULED — the classification the redesign re-tests starts from one settled input: this service stays an ORCHESTRATION.** The re-test is made against the service's **designed** surface rather than against only what is built, because the layer is what decides where a rule may live and §SEC14.3's composite is already placed here — a single-entity processing service cannot hold a rule whose own *Layer* paragraph sends it above the foundation. On that surface the service holds **three** entity services — `IAssociationService`, plus the `IContentItemService` and `IReactionService` §ARC16.8 records its read as already holding — while the flow spans a **fourth** entity type, `ContentItemSetting`, reached through `IAccessBroker`, which §ARC16.8 counts in the flow and which this section's broker rule keeps out of the Florance count. Two or more entity types is §ARC12.1 rule 2's definition of an orchestration, and three four-block arms is twelve blocks — at §ARC16.7.1's ceiling and not over it. So collapsing the six far-end probes takes this service from over the guidance to inside it, and not below the orchestration threshold. What the redesign still owns is the probe itself and what it costs; nothing here settles that.
>
> **What this does and does NOT foreclose, because the redesign's own write-up reaches the opposite conclusion and is not simply overruled.** That write-up measures the **write flow** — one entity service plus brokers — and concludes *"a count of one is also outside two-to-three… the signal that the layer is wrong"*, naming `AssociationProcessingService`. On the write flow alone that reasoning is sound, and the ruling above does not contradict it: what it contradicts is applying a write-flow measurement to the **whole class**, when the same class is already designed to carry §ARC16.8's read and that read needs `IContentItemService` and `IReactionService` for their **rows** — `Reaction.Name` and `Reaction.UnicodeEmoji` are in the projection — which no verdict-shaped broker arm returns.
>
> **So the reclassification may still happen, BENEATH this service rather than instead of it.** §ARC12.1 is explicit that the two can coexist: *"Where an entity has both, the orchestration sits on top of the processing service rather than beside it."* A single-entity `AssociationProcessingService` owning the retrieve-or-add write, with this orchestration above it holding the composite read, satisfies both measurements at once and is the redesign's to propose or refuse. **What is settled is only that the composite read's home stays an orchestration**, because §SEC14.3's *Layer* paragraph admits only *"an orchestration **or aggregation** service that can resolve both endpoints"*, and this solution has no aggregation layer — §ARC12.1's list of layers does not carry one — so of the two homes that paragraph allows, one exists. The re-test therefore has an answer for the class and an open question for the write flow, which is a narrower outcome than either reading alone.
>
> **Entry 2 (`ContentItemSettings`) is an orchestration and is built.** An earlier note here called it single-entity; deriving an override's `ContentType` from the `ContentItem` it names reads a second entity type, which §ARC12.1 rule 2 makes an orchestration. The effective-setting merge remains genuinely single-entity and may still earn a processing service beneath it (§ARC12.5.2's banner has the full reasoning).
>
> **Entry 3 (`Approval`) is confirmed multi-entity** — it subscribes to entity facts and spans `Approval`, `ApprovalReview` and `ApprovalSetting`.
>
> **Entries 4 and 5 are entry 3's decomposition, not new features.** Entry 3 grew to seven service references over six foundation objects, against the two-to-three the Florance guidance allows, and §ARC16.7.1 went on recording "three dependencies, not ten" while the constructor carried more than twice that. What follows §ARC12.5.3 splits it along the seams the design already drew — §ARC16.7.4 says in as many words that review requests "compose beside" the round rather than into it, and §APR8.6.2's caller-facing trio had already left for entry 4. Nothing here changes a rule about approval; it changes which service holds which rule.

**RULE — an orchestration holds brokers, and the Florance count does not count them.** Every orchestration in this solution holds `ILoggingBroker`, and every one that takes part in the event substrate holds `IEventEnvelopeBroker` and `IEnvelopeIntegrityBroker`, plus `IEventBroker` **where it publishes** — `ApprovalOrchestrationService` does, and `ApprovalReviewerOrchestrationService` after #522 does not, because its two subscriptions verify and then cause their write through a workflow seam that publishes for itself (§ARC12.5.4 business rule 1); `ApprovalOrchestrationService`, `AIReviewerOrchestrationService` and `ApprovalReviewerOrchestrationService` additionally hold `IAccessBroker`, which is the whole reason a service spanning several entity types can ask a policy question without resolving policy itself (§ARC12.3.1, §APR8.6.1 rule 4). §ARC16.8's reaction summary read and §ARC16.2.1's write gate both design a fourth holder, `AssociationOrchestrationService`; neither is built, so the enumeration above is what holds one today. The count §ARC12.1 and the Florance guidance are about is the count of **exception families a service must catch and map** (§ARC16.7.1), and a broker obliges none: it holds no logic to validate (§ARC12.2), so it raises no family of its own and its raw failures are what the closing `catch (Exception)` is for. That is why brokers sit outside the count — the reason is the arm, not the layer, and a broker that ever did raise a family would count like any other contract.

> **Three instruction files stated this the other way, and all three are corrected in the same change as this rule.** The clause was written as the tail of the same-kind rule, whose real subject is that an orchestration must not mix processing services with foundation services. Read literally it forbids `ILoggingBroker`, which would leave an orchestration unable to log, and it forbids `IAccessBroker`, which would put §APR8.4's most-specific-wins back inside every service that asks a policy question — the exact duplication §APR8.6.1 rule 4 exists to prevent. Each now bars a **storage** broker, which is the boundary actually being protected: an orchestration reaching storage directly skips every layer beneath it at once.
>
> - `.claude/agents/architect.md` — "Brokers remain off limits to an orchestration entirely".
> - `.claude/agents/qa.md` — "A broker dependency on an orchestration is a finding regardless". **The stronger of the three, and the one the review role actually loads**, so leaving it would have put QA on record to flag `IAccessBroker` on every orchestration in the solution.
> - The repository `CLAUDE.md` non-negotiable — "(never a mix of foundation and processing, and never a broker)".
>
> Naming only the first of the three was itself a defect in this design, found in review. The agent files own the same-kind rule (per `CLAUDE.md`), so this is not a separate cleanup — a design that asserts a correction it does not make leaves the contradiction live in the files a reviewer reads.

**RULE — every orchestration stays within two-to-three, and a DEVIATION requires clear justification AND explicit signoff.** Neither an implementer, a reviewer nor an analyst may grant one to themselves. A deviation is granted per service, in advance, on its own merits, and it is recorded **here** — in the register below — naming the service, the count, and the argument that earned it. A count over three with no entry in that register is a finding, not a judgement call.

*The universal rule and the protocol for granting a deviation live in `.claude/agents/architect.md` and `qa.md` (#526 / PR #527), not in this document. Those two files are seeded into other repositories as a template, and those repositories have no `G2H Design.md` — a protocol clause leaning on a design section existing would be silently absent the moment it was templated. The split is that the **rule and protocol** live in the agent files while the **register of approved deviations** lives here, beside the services it applies to. It fails closed when templated: no design document means no approved deviations, so every count over three is a finding in the template.*

**The register of approved deviations:**

| Service | Count | Why it was granted |
| --- | --- | --- |
| `ApprovalReviewerOrchestrationService` | Four dependencies, four arms, fifteen blocks | The read-triggered repair (§APR9.7.2 rule 1) is documented behaviour serving precisely these operations, and dropping it would answer a moderator `404` on a picker they can do nothing about. Alternatives were considered and rejected. Four is that service's **ceiling**: a fifth dependency is the tripwire to split it again. Full argument in §ARC12.5.4 business rule 1. |

**"`ApprovalReviewerOrchestrationService` has four" is NOT a justification.** It is the one case that was argued and approved, and its own entry says so. Somebody who finds that service and argues "four is fine, there is precedent" is answered on this page, without anybody having to argue with them. §ARC12.5 entry 1's `AssociationOrchestrationService` is in the same position and is deliberately **not** in the register: it is recorded as breaking the guidance and has never been granted a deviation — recording a break is not accepting one.

*Filed here rather than in §ARC16.7.1 on purpose, and it is a decision somebody will revisit. This rule block sits directly under the orchestration table, so it is what the author of a **new** orchestration reads, and it already hosts a general rule about the Florance count's scope (the broker rule above). §ARC16.7.1 is titled for ONE service; putting a solution-wide protocol inside it would repeat the filing mistake this design already names — "this sentence said 'three dependencies, not ten' while the constructor held seven" — where a general claim sat in a service-specific section and went stale unread. §ARC16.7.1's counting rule carries a pointer here instead, so a reader counting arms still meets the protocol.*

#### ARC12.5.1 ContentType — no orchestration *(formerly §12.5.1)*

`ContentType` is a fixed enum (§DOM3.6), not a database entity — there is no `ContentTypeOrchestration`, no `ContentTypeService`, no lifecycle to orchestrate, and no events to publish. Adding or removing a content type is a code change and a release, gated by the normal PR/build process rather than by a runtime authorization rule.

Content-type-scoped identity roles (§SEC18.6) are seeded once, at startup, for every member of the enum — they are not created or removed reactively in response to a content type lifecycle, because there is no such lifecycle.

**The seed walks the enums, and it must keep walking them.** `SeedData.BuildCoreRoleNames` composes the narrow tier by iterating `ContentType`, so a new member is seeded the moment it is added and no second edit is needed. That is the whole point: a hand-written list is the failure this rule exists to prevent.

**RULE — a change to `ContentType` is a change to the seed.** Adding a member is automatic *only while the loop stays a loop*; replacing it with an explicit list, or filtering members out of it, silently removes an administrator's ability to scope somebody to that content type. Renaming or removing a member is worse and is already forbidden by `ContentType`'s append-only rule (§DOM3.6): role rows carry the old name, association rows carry it denormalised, and a rename reassigns authority that already exists to a role nobody holds.

The failure mode is what makes this worth a rule rather than a convention. A role that is never seeded fails **silently** — the composed name is simply never found among an actor's roles, every gate falls back to the coarser tier, nothing throws and nothing is logged. The only symptom is a tier that never admits anybody, which is exactly how the content-type tier sat unseedable while the code reading it was live on both read paths and the write gates. `ContentTypeRoleSeedTests` pins the composition to the enums for that reason; it fails if the loop is ever replaced by a list that drifts.

**`Series` and `Topic` are seeded like every other member.** They are `ContentType` members on `ContentItem`, and §SEC18.6 rule 5 scopes the narrow tier to the entity type rather than to a chosen subset of its content types. Withholding them would protect nothing — the coarse `ContentItem-Reviewers` still admits somebody to a series either way — and would only remove the ability to scope a person narrowly. A role assigned to nobody grants nothing.

#### ARC12.5.2 ContentItemSettingOrchestrationService *(formerly §12.5.2)*

> **It is an orchestration after all — the misfiling note is withdrawn.** This section was annotated "this is a processing service, not an orchestration (§ARC12.1: single entity type)", and that reading held only while every flow stayed inside `ContentItemSetting`. One does not. Business rule 6 lets the publisher tier for a content type write an override OF that type, and the gate composes both the grant and the §SEC18.6 block from the `ContentType` on the row — so while that value is the caller's to set, the row decides who may write it. Deriving it means READING the referenced `ContentItem`, which is a second entity type and therefore an orchestration by §ARC12.1 rule 2, not a processing service by rule 1.
>
> `ContentItemSettingOrchestrationService` is that service, and the exposer binds to it. A processing service may still be earned later for single-entity work (§ARC12.5's entry 2, and issue #209's effective-value merge); it would sit UNDER this one, per §ARC12.1's closing note that an orchestration sits on top of a processing service rather than beside it.

`ContentItemSettingOrchestrationService` coordinates the content item setting flows that span a second entity type — today, deriving an override's `ContentType` from the content item it names. The single-entity rules below are the entity's and are enforced in its foundation.

Responsibilities:

1. Orchestrate content item setting creation and modification, enforcing control field integrity.
2. Apply model mapping on every write operation — map only the fields that a caller is permitted to change onto a fresh entity loaded from the database before committing. This prevents any caller from tampering with control fields through the update path.
3. Orchestrate creation of default settings per content type.
4. Orchestrate creation of per-item overrides when a specific content item requires different behaviour.
5. Resolve the effective setting for a given content item by merging the content type default with any item-level override.
6. Validate that settings are consistent and do not conflict with system-level constraints.
7. Orchestrate soft delete of settings.
8. Publish `ContentItemSettingCreatedEvent`, `ContentItemSettingUpdatedEvent`, and `ContentItemSettingDeletedEvent` via `ContentItemEventService`.
9. The approval orchestration service subscribes to these events to manage approval records and workflow state.

Business Rules:

1. If no item-level override exists, the content type default setting applies.
2. If an item-level override exists, it takes full precedence over the content type default.
3. Only one **live** default setting per content type may exist where `ContentItemId IS NULL`. (also enforced by database unique index, filtered `IsDeleted = 0`)
4. Only one **live** override setting per content item may exist where `ContentItemId IS NOT NULL`. (also enforced by database unique index, filtered `IsDeleted = 0`)

   The `IsDeleted` term is not a loosening of rules 3 and 4 — a soft-deleted row is not a setting, being invisible to every caller including `Administrators` under §SEC14.5 rule 3 and never resolved by rules 1 and 2. Without the term the row went on occupying its scope anyway, and since the API's delete *is* a soft delete, the ordinary way to remove a setting was the way that trapped its content type, or its content item, permanently (#326). `ApprovalSetting`'s two scope indexes carry the term for the same reason.
5. **Every `ContentType` member must ALWAYS have a live default setting.** Rules 3 and 4 cap each scope at one row; this one puts a floor under the default scope. It is not optional configuration: rule 1 makes the default the setting that applies when no item override exists, so a content type without one has no resolvable setting at all and rendering for that type falls back to nothing.

   **The rule belongs to the entity, not to a service above it.** The invariant is enforced in the foundation — `ContentItemSettingService` refuses to remove a row whose `ContentItemId` is null — and stays there now that `ContentItemSettingOrchestrationService` exists, because it is a rule about the row rather than about coordinating anything. The refusal is a validation error naming the rule rather than a not-found: the row is there and every caller may read it, so the caller is being told the entity does not permit what they asked, which is the shape `BibleReference.USFM`'s immutability takes (§ARC12.3.1 rule 2a). Overrides stay freely removable.

   **Hard removal is refused on the same terms — ruled.** The invariant is about the row existing, so the mechanism that removes it is irrelevant and no code path may leave a content type without a default even briefly. Hard delete as an escape hatch, with the startup re-seed as the repair, was considered and rejected: it leaves a window in which anything rendering that content type resolves nothing.

   **The seed restores a missing default.** `ContentItemSettingSeedData` runs on every startup and tests for a live default per content type — `ContentItemId IS NULL AND IsDeleted = 0` — so a content type that lost its default by a route the service does not own (a direct write, a restore, a database seeded before the refusal existed) gets it back. The `IsDeleted` term on `UX_ContentItemSettings_DefaultPerType` (rule 3) is what makes that insert possible; without it the dead row would still hold the scope and the repair would take Core initialisation down.
6. **Who may write a setting is decided by the SHAPE OF THE ROW.** A per-type default (`ContentItemId IS NULL`) is configuration for a whole content type and may be written — added, modified, removed, hard removed — only by `Administrators`. An item override (`ContentItemId IS NOT NULL`) is configuration for one content item, and additionally admits the **publisher tier for that row's content type**: the global `Publishers`, `ContentItem-Publishers`, or `ContentItem-%ContentType%-Publishers` (§SEC18.6). Any `ReadOnly` variation whose scope covers the row still refuses it first, `Administrators` included (§SEC18.6 rule 2).

   **The reasoning is the scope of the blast radius, not seniority.** One edit to a default re-shapes every item of its type; one edit to an override re-shapes the item somebody is already trusted to publish. Widening the default to publishers would let a single narrow grant change a whole type; keeping the override at `Administrators` would mean a moderator who may take an item down may not switch off its comments.

   **This is a deliberate exception to §SEC18.6 rule 1** — see the rule for why, and for the block it obliges in return.

   **The decision is made against the STORED row**, never the caller's copy, on every path that has one. A caller who could answer "is this a default?" for themselves could promote their own override into a default they may not author.

   **An override's `ContentType` is DERIVED from the content item it names, never accepted from the caller** — by `ContentItemSettingOrchestrationService`, since reading that item spans a second entity type. Without it the row's own claim decides who may write the row: a holder of `ContentItem-Devotional-Publishers` could label a row `Devotional`, aim it at a quote, pass the gate, and take that quote's only override scope (`UX_ContentItemSettings_OverridePerEntity` is keyed on `ContentItemId` alone), leaving the quote's own publishers unable to create one or remove it. This is the same rule, and the same reason, as an association's denormalised `Entity{A,B}ContentType` (§DOM4.1) — "derived from the resolved endpoint, never caller-supplied".

   A per-type **default** names no item, so there is nothing to derive from and its content type is the whole of what the row declares; only an administrator may write one. And the read carries §ARC16.6's visibility posture, so a caller who cannot see the item cannot resolve it at all — the attempt fails as an unresolvable reference before any role is consulted, which tells them less than a role refusal would.

   **The derivation runs on BOTH entry paths, which is what the subscription tier decides.** §SEC14.6 rule 1 requires the rule to hold regardless of which layer is called, and a derivation is only a property of the entity if every entry path performs it. `ContentItemSetting-Adding` therefore binds `IContentItemSettingOrchestrationService`, not the foundation. While it bound the foundation, an add request published to that address reached `DoAddContentItemSettingAsync` with the caller's own `ContentType` still on the row and the write gate composed the publisher tier out of it — the exact composition this rule exists to remove, left live on the event path after the HTTP path was fixed (#450 → #455 → #456). This is §EVN18 rule 3's argument for approvable entities ("a write made against a foundation service bypasses… an approvable entity must be exposed through its top-layer service") generalised to a derived authorization input. **RULE — an address whose handler derives an authorization input binds the layer that derives it.** The other four `ContentItemSetting` addresses stay on the foundation and must: modify pins both scope fields against the stored row, both removals and the retrieve read it, so none of them has a caller claim left to derive and routing them upward would add a layer that only forwards (§ARC12.1).

   **The derivation's own read must be the SIGNED CALLER'S, which on the event path takes carrying the envelope.** The paragraph below says the resolve carries §ARC16.6's visibility posture; that is a claim about *whose* posture, and it does not hold for free off the substrate. `ContentItemService.RetrieveContentItemByIdAsync(id, ct)` mints its own envelope, which captures the AMBIENT caller — correct on an HTTP request, wrong on a delivery, which either has no ambient context (so the read runs unauthenticated and answers not-found for every row that is not publicly visible, refusing a legitimate override on an unpublished item) or, because delivery is synchronous inside a publish and `HttpContextAccessor` flows on an `AsyncLocal`, inherits whoever PUBLISHED — who for a relayed or system-minted envelope is not the subject the envelope was signed for, and whose sight of the row is therefore the wrong answer in the permissive direction. The event path therefore resolves through the internal `RetrieveContentItemByIdAsync(id, inboundEnvelope, ct)` overload, which chains off the inbound envelope through `CreateNextAsync` — copying the security context forward rather than minting one — exactly as the publication swap's `UnpublishContentItemByIdAsync` overload does and for the same reason. **RULE — a read whose answer depends on who is asking is passed the envelope it is being made under, never left to mint one, on any path that is not the ambient caller's own.**

   **On the event path the derivation REFUSES a contradicting claim rather than overwriting it, and the difference is the signature.** Both paths run the same read of the same item and let the derived value govern. On the method path it simply replaces whatever the caller sent — a loose object nobody attested to. An event request is not: it arrives inside a signed envelope whose HMAC covers the content (§SEC14.6 rule 4), and the property that signature buys is that no receiver silently edits a part the rules read. Rewriting the field there would leave the `ProcessedEvents` record and the reply built from that envelope carrying content the publisher never signed, and would need a *service* to mint a request signature — which nothing but `EventBroker` does, and nothing else should. So the event path answers a mismatch with a validation failure. An honest publisher is unaffected, because for anything that resolved the item the two values already agree.

   **`Association` has the same shape and is NOT covered by this.** `Association-Adding` binds `IAssociationService`, whose validation of `Entity{A,B}ContentType` is enum-definedness only, while the derivation lives in `AssociationOrchestrationService` — so the same gap exists there. It is left open deliberately rather than by oversight: that orchestration's classification is under revision (§ARC12.5 entry 1's provisional note), and repointing its subscription is part of that work rather than ahead of it. Recorded here so the next reader finds the answer instead of the question.

   **The block is asked before the scope is, and covers BOTH scopes.** A `ContentItem`-scoped block bars a caller from a `ContentItemSetting` default as surely as from an override: the default is the row that configures every content item of that type, so a caller barred from the type is barred from the wider write too. Asking it only on the override branch inverts the rule — it stops the write that governs one item and waves through the one that governs all of them. This says nothing about any other settings entity; `ApprovalSetting`'s own write gate is unchanged and outside this rule.
7. Disabling a feature in settings must prevent the creation of new associations of that type for the affected content items.
8. The following are control fields. `ContentType` and `ContentItemId` need stating precisely rather than simply listed, because a setting cannot be addressed at all without them:
   - `ContentType` — **derived, not accepted**, on an item override: `ContentItemSettingOrchestrationService` reads the `ContentItem` the row names and overwrites whatever the caller sent (rule 6, #450), because the write gate composes the publisher tier from this value. The same service handles the `ContentItemSetting-Adding` event address for the same reason (#456); there it refuses a contradicting claim instead of overwriting it, because the claim is inside a signed envelope — see rule 6. On a per-type default there is no item to derive from and the field is the row's own subject, so it is caller-supplied and validated against the enum.
   - `ContentItemId` — **caller-supplied on add, by necessity**: it is how a caller names the item being configured. It is pinned on modify by rule 9, so a row can never be moved to another item, nor flattened into a default.
   - `ApprovalStatus`, `IsDeleted`, `CreatedBy`, `CreatedWhen`, `DeletedBy`, `DeletedWhen`, `DeletionReason` — never accepted from an external caller; set internally by the owning workflow.
9. On every update the stored row must be loaded and the control fields above must not be permitted to change. This is the **foundation's** work rather than an orchestration's — it is a rule about one row (§ARC12.3, and rule 5 above) — and `ContentItemSettingService.ValidateAgainstStorageContentItemSettingOnModify` enforces it by comparing the incoming `ContentItemId`, `ContentType`, `CreatedWhen`, `CreatedBy` and `UpdatedWhen` against the stored values and refusing any that differ, for every caller including `Administrators` — it is what the row IS, not who may change it. Note that this REFUSES a changed control field rather than silently mapping around it, so a caller learns their write was rejected. Only the setting fields themselves (`TagsAllowed`, `ShowTags`, `ReactionsAllowed`, `ShowReactions`, `LinksAllowed`, `ShowLinks`, `AttachmentsAllowed`, `ShowAttachments`, `CommentsAllowed`, `ShowComments`, `BibleReferenceAllowed`, `ShowBibleReferences`, `LimitReactionsToLoveOnly`) are amendable.
10. Review dismissal is not the responsibility of this orchestration. Publishing `ContentItemSettingUpdatedEvent` is sufficient — `ApprovalOrchestrationService` must handle dismissal when it receives that event.

#### ARC12.5.3 ApprovalOrchestrationService *(formerly §12.5.3)*

`ApprovalOrchestrationService` orchestrates the approval **round** across entities, policy evaluation, and denormalized state.

> **Its subject is the round, and everything below is about the round.** Three other concerns once sat on this service and two have already left: the AI reviewer's caller-facing lifecycle went to `AIReviewerOrchestrationService` (§APR8.6.2, §ARC12.5 entry 4), and reviewer coordination goes to `ApprovalReviewerOrchestrationService` (§ARC12.5.4). What stays is opening a round, evaluating it, deciding it, resetting it, and syncing the outcome onto the owning entity.
>
> **The test for what belongs here is §APR8.5, not the word "approval".** A rule that can move a §APR8.5 predicate — a review, a comment, a threshold, a status — belongs to the round. A rule that cannot is coordination, and §APR7.9's opening sentence already says the request rows "appear in **no** §APR8.5 condition — the verdict, the counts and the blocks never read it." That is the whole of why the reviewer half can leave without any rule changing.

Responsibilities:

1. Subscribe to each approvable entity's **top-layer** `-Added` and `-Modified` facts, per §EVN18 — the orchestration fact where one exists, the processing fact otherwise (`ContentItemProcessing-Added` / `-Modified` for `ContentItem`, `LinkProcessing-Added` / `-Modified` for `Link`). It does **not** subscribe to `-Removed`: a removal is a takedown, not a moderation step, and must never re-open or re-evaluate approval (§APR9.7.6).
2. On receiving a `CreatedEvent`, check whether an approval record already exists for the entity. If none exists, create one via `ApprovalService` at the status §APR9.7.2 rule 1 resolves — the entity's own, which is `Submitted` for a create at `Submitted` and `Draft` otherwise.
3. On receiving an `UpdatedEvent`, check whether an approval record exists for the entity. If none exists, create one as rule 2 does. If one exists, evaluate whether existing reviews must be dismissed based on the effective `ApprovalSetting.RequireReapprovalOnChange` policy.
4. Orchestrate approval submission by moving `ApprovalStatus` from `Draft` to `Submitted`.
5. React to a review or comment fact by **re-testing** whether the approval can now complete.

    **This responsibility does not own the threshold, and the timing here was wrong.** The only threshold comparison in the codebase is `IAccessClient`'s `EvaluateConditions`, reached through `IAccessBroker.MayDecideApprovalAsync` (§APR8.5, §ARC12.3.1) — and it runs **when an approve is attempted**, not "after each review decision". This orchestration's job is to notice that the inputs changed and ask; the answer is not its to compute. Earlier wording ("evaluate approval threshold after each review decision using `ApprovalSettingsService`") was wrong on the owner *and* on the trigger.

    **Subscribe to every fact on both workflow records, and re-test on each.** All four `ApprovalComment` addresses (`-Added`, `-Modified`, `-Resolved`, `-Removed`) and all four `ApprovalReview` addresses (`-Added`, `-Modified`, `-Removed`, `-Dismissed`) can move a §APR8.5 predicate, because the evaluation reads comments through `IsDeleted is false && IsResolved is false` and reviews through `IsDeleted is false && Verdict != Dismissed`. **Both comment resolution addresses are required**: `IsResolved` has two writers by design — the owner through the general modify (on a remark; the amend gate refuses a write that would leave an **ask** settled, §APR7.8), the publisher tier through the resolve transition (§SEC14.7 posture D rule 5) — so watching one would leave the gate movable unnoticed, decided by nothing more than which UI control was clicked. Each fact means "the inputs changed", never "the approval may complete": re-run the whole evaluation, and treat gate-shutting facts (a comment born outstanding, a withdrawn approving review) as seriously as gate-opening ones, since they can re-block an approval that was clear under `AutoApproveIfAllApprovalRequirementsMet`. A fact may also move nothing — a comment born settled (§APR7.8) is the common case — so never infer a direction from the address. These are foundation-tier subscriptions — neither record is approvable and neither has a layer above its foundation (§ARC12.3.1), so §EVN18 rules 1–2 do not apply. See its inbound items (a)–(h) in [`Documentation/Design/Events.md`](Events.md) for the full table.
6. Apply `Approved` status when the approval conditions (§APR8.5) are met and `AutoApproveIfAllApprovalRequirementsMet = true`.
7. Write the denormalized `ApprovalStatus` onto the owning entity itself, through that entity's state-transition operation rather than a general modify (§EVN18 rules 4–5). The two values must never diverge (§APR9.8).
8. On `Approved`, set `IsPublished = true` on the newly approved version.
9. Set `IsPublished = false` on the previously published version, ensuring only one published version exists per `GroupId`, and order the two writes so no window exists in which both are published. The group's tip does not move at publish time, and cannot: approval adds no version (see §DOM3.4.1). For a Single-Row entity (§APR7.5.1) there is no previous row and this rule is vacuous.
10. Use `SecurityBroker` to validate user identity and role claims during submission and review.
11. Publish `ApprovalCreatedEvent`, `ApprovalUpdatedEvent`, and `ApprovalDeletedEvent` via `ApprovalEventService`.
12. Derive attachment approval from the host (§DOM5.6.5): when a host entity completes approval and publication, approve and publish its purposefully-placed (§DOM4.9) and inline-referenced (§DOM5.6.6) attachments through the attachment submit-then-approve transitions, bypass-audited. Until this orchestration exists, §DOM5.6.5's interim rule applies — the publisher flow that approves the host derives the attachment approvals synchronously.

Business Rules:

1. An approval record must be unique per `(EntityType, EntityId)`.
2. If an approval record does not exist when a `CreatedEvent` or `UpdatedEvent` is received, it must be created before any other approval logic is applied.
3. An entity may not be submitted for approval if it is already in `Approved` status.
4. `Dismissed` never applies to `Approval` records — only to `ApprovalReview` records. After reviews are dismissed the item remains `Submitted` and eligible reviewers may submit new reviews.
5. Self-approval on the ordinary route is blocked when `ApprovalSetting.AllowSelfApproval = false`. It does not reach the bypass, which an `Administrators` may take over their own submission (§APR8.6 HR-2).
6. A single rejection blocks further approval when `ApprovalSetting.BlockOnReject = true`.
7. Dismissed reviews must not contribute to the approval threshold count.
8. This orchestration is responsible for evaluating whether existing reviews must be dismissed when an entity updated event is received. The originating orchestration must not perform dismissal directly.
9. This orchestration is responsible for automatic approvals if applicable.
10. This orchestration is responsible for manual approval submission subject to policy rules  i.e. amount of required approvals, self-approval, and role-based approval. Manual approval requires the approval conditions (§APR8.5) to be met and is available to `Publishers` and `Administrators` (global or matching `%EntityType%-Publishers`).
11. This orchestration is responsible for manual approval (bypass rules) i.e. policy rules not met but a permitted user needs to approve anyway. This must be a separate method that does not enforce policy rules except role-based access: bypass is available to `Administrators`, to the global `Publishers` role (any entity type), and to the matching `%EntityType%-Publishers` role (that entity type only) — the `Publishers` tier, composed from the entity type rather than configured (§APR8.3). Bypass is unavailable entirely when `ApprovalSetting.DoNotAllowBypassingSettings = true` — the conditions must then be met by everyone, including `Administrators`. Bypassing sets `IsApprovedByBypass = true` and records the actor on `UpdatedBy`.

    **This is built at the foundation, on all seven approvable entities**, as part of the widened approval transition (§APR8.6.1, §APR9.7.1 rule 3, §APR9.7.5). It is *not* a separate method, as this rule and §APR8.6.1 both previously required: that shape was built on `Association` and then withdrawn, with the reversal and the mitigations that survive it recorded in §APR8.6.1. What replaces "a separate method" is a bypass **request** on the transition's payload, which is refused unless it accompanies a target of `Approved`.

    This also settles what `Attachment` needs. Its host-derived approval can only be recorded as a bypass (§DOM5.6.5), which under the old shape meant `Attachment` had to grow a second bypass *verb* before it could participate; under the widened transition it inherits the bypass with the transition itself, so #181 has one operation to build rather than two. Four points where the built shape is narrower or more specific than the rule above, and each is deliberate:

    - **Approve only.** A rejection withholds approval rather than granting it, and a re-open to `Submitted` decides nothing, so neither has anything for a bypass to waive; the request is refused outright on either target, the decision sent to `IAccessClient` is fixed to `Approve`, and a direct reject stays the ordinary path (business rule 13, §APR9.7.5).
    - **The reason is required, and supplied by the caller** — `ApprovedByBypassReason` on the payload, on both the direct and the event path, since an envelope carries one entity and nothing else. Validated non-empty and capped at 500 to match the column, so an unexplained bypass is refused before any policy is read.
    - **Neither `IsApprovedByBypass` nor `ApprovedByBypassReason` is accepted off the caller's entity.** They record that the conditions were waived, so a caller who could write them could equally clear them. The flag is derived outright, from the verdict's `IsBypassUsed` rather than being hardcoded `true`. The reason's value is necessarily the caller's, since no verdict can say why a human chose to override; what the verdict decides is its **retention** — it is kept only when `IsBypassUsed` is true, and cleared to `null` otherwise.
    - **The verdict reports what the bypass waived** — `BypassedBlockReason` names the block that would have fired, and is `None` when the conditions were in fact met — so a bypass that overrode a standing rejection is distinguishable from one that overrode nothing.

    The row-local `Publishers`-tier gate still runs first and is resolved from the **stored** row, not the caller's copy; the access decision repeats the tier check, which means a defect in the gathering can only make the gate stricter. The outcome publishes the ordinary `-Approved` fact — there is no separate bypass fact (§APR9.7.5).
12. Dismissal is only applied when `ApprovalSetting.RequireReapprovalOnChange = true` for the relevant entity type. If `false`, existing reviews are retained and no dismissal occurs. Exception: an administrator **status override** moving a terminal entity back to `Submitted` always dismisses active reviews regardless of this setting (§APR8.8 regardless-rule 1) — those reviews produced the verdict being overruled, and re-opening the round on their strength would let the override be undone by the very reviews it overrode. The in-place amendment that used to be this exception is withdrawn (§DOM3.4 rule 16).

    **The exception is built, and `ResetApprovalAsync` is what carries it** (§ARC16.7.5). It was written here long before anything implemented it: the only dismissal call in the system belonged to the content-edited flow and was gated on the very setting this exception overrides, so an override dismissed nothing. The reset dismisses unconditionally, and orders the dismissal *after* the status write so that the re-test each dismissal triggers sees the re-opened round rather than the outcome being withdrawn.
13. A publisher or administrator may reject directly while the approval is `Submitted`; the outcome is recorded immediately as `Rejected`.
14. Retrieve-or-create (business rule 2) must evaluate existence against **all** rows for `(EntityType, EntityId)`, including soft-deleted ones, because `UX_Approvals_EntityType_EntityId` is not filtered on `IsDeleted` and the caller-facing reads are visibility-filtered. Either can report "does not exist" for a key that does exist, and the resulting insert cannot succeed (§APR9.7.2).
15. The `-Modified` branch runs only when an approval-sensitive field changed (§APR9.7.4). A fact whose only differences are workflow or bookkeeping fields ends the branch immediately, with no read or write of the approval.
16. The versioned/single-row branch is resolved from the §APR7.5.1 publication-model table, never by probing the entity for `IVersion`, by reflection, or by inspecting EF configuration.
17. No approval transition may be applied to a soft-deleted entity. The approve, reject and bypass operations validate that the subject is not deleted before applying any transition, so a review submitted before a takedown cannot approve and re-publish it afterwards (§APR9.7.6 rule 3). Removal itself never changes the approval record.
18. `Rejected` is reachable by exactly two routes: a blocking review rejection when `BlockOnReject = true` (§APR8.7 rule 1), and a direct `Publishers`/`Administrators` rejection (business rule 13). Both apply immediately and independently of `RequiredNumberOfApprovals`, and both leave `IsPublished` untouched — and neither can move the group's tip, which no approval outcome touches (§DOM3.4.1).
19. **It holds three ARM-BEARING service references and three exception arms: `IApprovalWorkflowService`, `IApprovalReviewWorkflowService`, `IAIReviewerAssignmentWorkflowService`** — `Approval*`, `ApprovalReview*` and `AIReviewerAssignment*`, twelve downstream catch blocks, at the Florance ceiling and not over it. A **fourth** seam used to sit in the constructor and cost no arm: `IApprovalReviewRequestWorkflowService`, held for §APR7.9 rules 6 and 8. **It has left with #522**, which moved both retirements to `ApprovalReviewerOrchestrationService`'s own subscriptions (§ARC12.5.4 business rule 4), so this service now holds no reference to the request record at all. The count of three is unchanged by its departure, because it never cost an arm — the retirement verbs share `ApprovalReviewRequestService`'s own `TryCatch` with the caller-facing door — and it is recorded here so a reader diffing the constructor against an earlier count does not read the removal as a correction. **The `ApprovalReview*` arm was landed ahead of this split, sequenced deliberately first (issue #518) so the split moves a working arm instead of writing it under the refactor**; `DismissStaleApprovalReviewAsync`'s validation-shaped refusals now report as `ApprovalOrchestrationDependencyValidationException` rather than falling to the catch-all. Two of those three are used here for a single verb apiece — dismiss a stale review, return Berean to pending — and they stay together because they are the same act on two records: at the two sites where a round's verdicts stop describing its content (§APR9.7.4's `RequireReapprovalOnChange` branch and §APR8.6 HR-4's reset), the human reviews and the AI assignment are brought into line as one step. Splitting them would put half of one decision in another service. §ARC16.7.1 states the counting rule these three are measured by.

**The AI seam has grown a second verb, and this service is not its caller.** §APR8.6.2.1's `AddAutomaticAIReviewerAssignmentAsync` is on `IAIReviewerAssignmentWorkflowService` since #534, called by `AIReviewerOrchestrationService`'s own subscriptions (§ARC12.5 entry 4). This service goes on holding the seam for `ReturnStaleAIReviewerAssignmentToPendingAsync` alone, and its count of three is unchanged: a seam member this service never calls costs it no arm and no reference. The sentence above therefore says what **this service uses** rather than what the interface carries — an earlier form said the seam "carries one", which the second verb falsified here and in §ARC16.7.1 at the same time.

#### ARC12.5.4 ApprovalReviewerOrchestrationService *(formerly §12.5.4)*

> **The service and its controller split are BUILT (#521, #522, #523).** Nothing in this subsection is a new rule. Every rule it names is §APR7.9's, §ARC16.7.2's or §ARC16.7.4's, already written and already built inside `ApprovalOrchestrationService`; what this subsection settles is that they live on their own service, and what that costs.
>
> **What #521 landed:** responsibilities 1–5, the four exception arms, the read-triggered repair, and business rules 1, 2, 3, 5 and 7. **What #522 landed:** responsibility 6 and business rule 4 — the two retirements are subscriptions on this service, and `ApprovalOrchestrationService` holds no reference to the request record. **What #523 landed:** business rule 6, the controller split. `ApprovalsController` binds `IApprovalOrchestrationService` alone again, and `ApprovalReviewersController` binds `IApprovalReviewerOrchestrationService` alone.

`ApprovalReviewerOrchestrationService` owns **who takes part in an approval round**: who is in scope for it, who has been asked, who answered, and what they are all called. It spans `ApprovalReviewRequest`, `ApprovalComment` and `IdentityUser`, and reads `Approval` for the read-triggered repair alone — which is §ARC12.1 rule 2's definition of an orchestration several times over.

**Why this is a service and not a partial file.** It was a partial file — `ApprovalOrchestrationService.ReviewRequests.cs`, `.ReviewRequests.Validations.cs` and `.ReviewerDisplayNames.cs`, 1,108 of that service's 4,076 implementation lines — and the file split hid the thing that was wrong: one interface carrying two contracts, and a constructor at six foundation dependencies against the Florance two-to-three. §ARC16.7.4's closing sentence had already ruled the coupling absent ("the request rows change no §APR8.5 outcome, so nothing here touches the evaluation, the verdict or the decision paths — the whole feature composes beside §ARC16.7.1–§ARC16.7.2 rather than into them"). A concern that composes beside another is a service beside it.

Responsibilities — the five caller-facing operations of §ARC16.7.4 and §ARC16.7.2's name resolver, moved verbatim:

1. `RetrieveReviewerCandidatesAsync(entityType, entityId)` — who is in scope for this round (§APR7.9 rule 3's eligibility, minus the owner and minus anyone a covering `ReadOnly` blocks).
2. `RequestApprovalReviewAsync(entityType, entityId, requestedUserId)` — §APR7.9 rules 1–4.
3. `RetrieveApprovalReviewRequestsAsync(entityType, entityId)` — who has been asked and has not answered, through the **caller-facing** foundation read.
4. `WithdrawApprovalReviewRequestAsync(entityType, entityId, requestedUserId, deletionReason)` — §APR7.9 rule 5.
5. `RetrieveReviewerDisplayNamesAsync(entityType, entityId)` — the round's people, named once and composed once.
6. **The two retirements, reactively.** §APR7.9 rule 6 (an answered invitation retires itself) and rule 8 (a closed round retires what it never answered) live here with the rows they touch, and they are subscriptions rather than calls — see the business rules below.

Business Rules:

1. **FOUR service dependencies, FOUR exception arms, FIFTEEN downstream blocks: `IApprovalReviewRequestService`, `IApprovalCommentService`, `IIdentityUserService`, `IApprovalWorkflowService`** — plus `IAccessBroker` and the envelope and logging brokers. The **integrity broker arrives with business rule 4 (#522)**, and the event broker does NOT: business rule 4's two handlers verify an inbound envelope and cause their write through the workflow seam, which publishes for itself, so nothing on this contract publishes even after #522. An earlier version of this sentence expected both, which would have injected `IEventBroker` ahead of its first caller — the defect §ARC16.7.1 already keeps against this design in the other direction, a constructor and a paragraph that disagree. Subscribing is not a reason either: `EventSubscriptionRegistration` holds the broker and binds the handler, exactly as it does for every other subscriber. **The workflow seam arrives with business rule 4 too, as a second constructor parameter, and it is not a fifth dependency** — §ARC16.7.1 rule 2 counts two interfaces onto one class once where they share one family, which these do. `IApprovalReviewRequestService`'s workflow seam (`IApprovalReviewRequestWorkflowService`) is a second door onto the same class **and shares its `TryCatch`** — `ApprovalReviewRequestService.Transitions.cs` wraps both retirement verbs in the same one the caller-facing operations use — so both doors throw `ApprovalReviewRequest*` and cost one arm between them. That shared **family**, not the shared instance, is what keeps it to one arm; §ARC16.7.1 states the rule and why the instance is the wrong thing to count. `ApprovalReviewRequest*`, `ApprovalComment*` and `Approval*` are four-block arms; `IdentityUser*` is three, per finding 1 — four + four + three + four = fifteen, not the sixteen a same-sized-arm assumption would predict.

   **`IApprovalWorkflowService` is here for the READ-TRIGGERED REPAIR and nothing else** (§APR9.7.2 rule 1, business rule 2). An earlier version of this rule said the service takes no such dependency and that this is what keeps it at three; that was retired by the ruling to keep the repair, and the count went with it.

   **FOUR ARMS IS OVER THE TWO-TO-THREE THE FLORANCE GUIDANCE ALLOWS, AND IT WAS ARGUED RATHER THAN ALLOWED.** Three things travel with the number, and none of them is citable as precedent by anything else:

   - **Four is this service's CEILING, not a new baseline.** A fifth service dependency is the tripwire to **split the service again**, exactly as seven on `ApprovalOrchestrationService` was. It is not an invitation to record a second break. *A break that gets extended once has stopped being a break.*
   - **This is a deliberate, justified, signed-off deviation from the Florance pattern — not evidence the pattern has been relaxed.** It was argued on its merits: the repair is documented behaviour serving precisely these operations, and dropping it would answer a moderator `404` on a picker they can do nothing about. The alternatives — resolving the round through a second broker read, or leaving the repair on the round's service and calling across — were considered and rejected, the first because it buys nothing the by-entity overload does not already give and the second because an orchestration calling an orchestration is what coordination services are for. The deviation was then **explicitly approved**. **None of that transfers to any other service**, and the general protocol for granting one is in §ARC12.5's rule block.
   - **§ARC12.5 entry 1 is the CAUTIONARY case, not the supporting one.** It has recorded `AssociationOrchestrationService` as breaking this guidance since before this work started, and that service is **still at seven**. Recording a break is not the same as accepting one, and this entry must not become the second example of a break that was written down and then left alone.

   **Three of the four arms are MOVED rather than written twice.** `ApprovalReviewRequest*` moves off `ApprovalOrchestrationService`, where it already existed; `ApprovalComment*` and `IdentityUser*` were landed there ahead of this split by issue #518, for the same reason `ApprovalReview*` was (§ARC12.5.3 business rule 19), and move with the operations that raise them. Only `Approval*` is new here, and it arrives with `IApprovalWorkflowService`. A developer who writes the three fresh leaves two copies and a reviewer cannot tell which is authoritative.

2. **It resolves the round through `IAccessBroker.RetrieveApprovalReviewerScopeByEntityAsync`, and REPAIRS on a null rather than reporting one.** Every operation is keyed on an entity, and every one of them needs the round behind that key.

   **The overload is KEPT, for a reason that is now an economy rather than a necessity, and the change of reason is worth seeing.** `RetrieveApprovalReviewerScopeByEntityAsync(entityType, entityId, cancellationToken)` resolves the approval from the entity key and returns the same `ApprovalReviewerScope`, whose `ApprovalId` is then what the request reads and writes are keyed on. It does in **one** broker read what the old shape took a foundation lookup plus a broker read to do. Its original justification — that it is what avoids a fourth exception arm — was **retired by the ruling to keep the repair**: the arm is taken anyway, and the overload survives on the round trip it saves. Resolving an `EntityType` to a row is the one thing this broker exists for (§ARC12.3.1), so it adds no capability — only a second entry point to one it has.

   **The overload's own behaviour is unchanged: it still answers `null` where no round exists.** What changed is what the CALLER does with that null. It **repairs and retries** — ask the by-entity overload, on `null` run the repair, ask the overload again, and report not-found only if the retry is null too — which is behaviourally what the resolver did before the move, in one read per attempt instead of two. An earlier version of this rule said the caller "reports that as not-found", and that is the sentence a careless edit misses.

   **The repair is the NARROW form.** `ApprovalOrchestrationService.RepairMissingApprovalAsync` delegates to `ResolveApprovalAsync`, the round's retrieve-**or-create-or-reinstate**, which the entity-fact flows also call — so it cannot move, only be duplicated. `AIReviewerOrchestrationService` met exactly this and wrote a narrower copy; this service carries the same one, and for the same reason. The repair runs only where the scope read answered `null`, and that overload is **unfiltered on `IsDeleted`** — a soft-deleted approval still answers its scope — so `null` means no row at all and the reinstate branch is unreachable from this entry point. Duplicating `ResolveApprovalAsync` is refused: it would put a branch this path cannot reach into a second service, where somebody would then have to maintain it without being able to tell it was dead. *If a reviewer believes the reinstate branch IS reachable here, that is the thing to challenge — it is the load-bearing step.*

   **Both of the repair's gates travel intact.** The entity must be **visible** (§SEC14.5 rule 3 — not merely present, because a taken-down row keeps its `ApprovalStatus` and this repository has no EF global query filters), and the entity must be **in play** at `Draft` or `Submitted` (§APR9.8 — opening a round at `Draft` beneath an `Approved` entity writes a divergence nothing would ever reconcile). The visibility gate is asked in the resolver as well, before the repair is reached, so the repair's own copy refuses nothing today; it is kept because that helper mints a row for an entity id that came straight off a route and must defend itself rather than trust its caller (§SEC14.6 rule 2).

3. **`IApprovalCommentService` is a caller-facing read and must stay one.** The display-name resolver names the authors of the round's live comments, and §ARC16.7.4 rules that they "arrive through `IApprovalCommentService`, so §SEC14.7 posture D's own visibility filter decides which authors a given caller can name". Reading them off the gathering broker instead would name people whose words the reader cannot see. **It is not tradeable.** It is no longer also the argument that the count is three rather than two — this clause survives the repair ruling, the count it used to justify does not.

4. **Both retirements are subscriptions, not calls, and this is the one behavioural change in the split.** Rule 6 binds `ApprovalReview-Added`; rule 8 binds `Approval-Modified`. Neither needs a new address, and neither needs a fact that says *why* the round moved — but rule 8 does need two things the direct call had for free, and an earlier draft of this rule assumed the broker supplied both. It supplies neither.

   **(i) The round-closed gate is the HANDLER's, and it reads the envelope.** `IAccessBroker.FindRetirableApprovalReviewRequestIdsAsync` does not read the approval's status at all — it filters `ApprovalId` and `IsDeleted == false`, and excludes the standing reviewers 4(ii) requires, and nothing else. The status gate used to live in the caller (`RetireUnansweredApprovalReviewRequestsAsync`'s `isRoundClosed` test), where its own comment recorded it as dead code because all three callers arrived having just written an outcome. **Replacing those callers with a bare subscription is exactly what brought that gate to life**, because only three of the **workflow seam's** six call sites close a round; the other three publish `-Modified` on an OPEN one — the entity-status sync, the reinstatement of a soft-deleted approval, and §APR8.6 HR-4's reset to `Submitted`. Nor are those six the whole inventory: the public `IApprovalService.ModifyApprovalAsync` and the live `Approval-Modifying` command address reach the same `DoModifyApprovalAsync` and publish the same fact, so a caller-facing edit to a decided round is a `-Modified` too. **That the complete list does not have to be known is the point** — an envelope gate answers every publisher, where an enumeration of call sites would have to be kept in step with all of them. Ungated, the handler would retire every pending invitation on an open round, and the HR-4 case is the sharpest: reset, moderator re-invites, and the next `-Modified` sweeps the new invitations — the opposite of what rule 8's "a moderator asks again" promises.

   The gate therefore runs in the handler, against `envelope.Content.ApprovalStatus`, before the gather: retire only where the status is `Approved` or `Rejected`. That value is the updated row the foundation published (`CreateNextAsync(sourceEnvelope, updatedApproval)`) and it sits inside the HMAC, so it is signed system data rather than a caller's claim — §ARC16.7.1's provenance rule, not an exception to it. No second read is needed and none is added.

   **(ii) The gather must exclude whoever answered, so the two retirements stop depending on their order.** The broker's filter used to be `IsDeleted == false` alone, which meant "what the round is still waiting on" **only because rule 6 had already soft-deleted the answerer's row** — an ordering the pre-split code made explicit and a bare pair of subscriptions would have silently dropped. Two independent subscriptions on `ApprovalReview-Added` have no stated order, and the losing order is the common case: the last reviewer's vote completes the round, the re-test's `ModifyApprovalAsync` publishes `-Modified` synchronously, and rule 8's sweep takes the answerer's own invitation and stamps it `"Retired: the approval round closed before this review was cast"` — false about the one person who did cast one. §APR7.9 rule 8 makes `DeletionReason` the reader's only way to tell a close-retirement from a withdrawal, so this degrades the distinction the rule exists to protect.

   **`FindRetirableApprovalReviewRequestIdsAsync` therefore excludes any `RequestedUserId` holding a review that still STANDS on the round** — `IsDeleted == false && Verdict != Dismissed`, which is `ApprovalReviewerScope.ActiveReviewerUserIds`'s own predicate. It governs here for the same reason it governs invitability: it is the set the already-answered short-circuit in `RequestApprovalReviewAsync` (`ApprovalReviewerOrchestrationService.ReviewRequests.cs`) gates on, so the exclusion and invitability stay one definition rather than two that can drift.

   **It is not the same predicate as the review index's own filter, and an earlier version of this rule said it was.** `UX_ApprovalReviews_ApprovalId_CreatedBy` filters `StatusId <> Dismissed AND IsDeleted = 0`. That agrees with the active set on every row the system can legally hold, and diverges on exactly one shape: a stored review at `Draft` or `Submitted`, which the index counts as occupying the slot and which `ToReviewVerdict` treats as corrupt and not standing. The two are therefore nearly the same predicate rather than the same one — right for every legal row, and not a claim to make in general. With the active-set exclusion the ordering is irrelevant and neither subscription has to know the other exists.

   **It must be the ACTIVE set and not the recorded one, and an earlier draft of this rule got that wrong.** `RecordedReviewerUserIds` carries every review's author with no filter at all, and its own definition says in as many words that it is "never a substitute for `ActiveReviewerUserIds` in the §APR7.9 rules". The question this exclusion asks is not *did this person ever answer this round* but *has this person answered the round as it now stands*, and re-invitation after a dismissal is a documented path rather than an edge case: §APR9.7.4's `RequireReapprovalOnChange` branch and §APR8.6 HR-4's reset both dismiss the round's reviews, the invitability short-circuit gates on the active set, and asking that person again is the next step. Keyed on the recorded set, their **second** invitation would be skipped at that close and at every future close of the round — a genuinely unanswered pending row surviving forever, which is precisely the row rule 8 exists to remove and which rule 8 records as clearable by nothing but a manual withdrawal. The draft's reasoning — *a dismissed review still means they answered, and rule 6 retired their invitation when they did* — is true of the first invitation and stops being true of the second, which rule 6 never saw.

   The active set covers the ordering race it was chosen for, and covers it exactly: the review that triggers the race has just been added, so it stands at the moment the sweep runs and its author is excluded.

   **What that trades, stated rather than discovered later.** The answerer's row is swept with the close reason only where rule 6's retirement failed **and** their review was later dismissed or withdrawn — not on every rule 6 failure. Short of that pair, a failed rule 6 now leaves the row standing on a closed round instead of mislabelling it. That is the right way round: rule 8's own text says "the clean-up is therefore about what the record *says*, not about what it gates", and it already accepts a stale row as its failure cost — clearable by hand. A row asserting the wrong sentence is not clearable by anything, because nothing can tell it from a true one. And the worst case degrades to the behaviour that exists today rather than to a permanent leak.

   **Why not a call.** Leaving rule 8 as a direct call is the only thing that would keep `ApprovalOrchestrationService` holding a reference to the request record at all — a fourth arm, held for one verb, on a service whose own §ARC16.7.4 says the feature composes beside it. The seam would be the last of the coupling, and the whole point of the split is that there is none.

   **It improves the failure posture rather than preserving it.** §APR7.9 rule 8 and §APR8.6.2 both require that this bookkeeping never fault the close, and both are honoured today by a `try`/`catch`/log inside the orchestration. A delivery failure is already non-fatal by construction — `EventPublishResult.FailedDeliveries` records it and the publisher decides (§EVN23) — so the posture stops depending on a hand-written catch in each caller.

   **Redelivery and concurrency are different cases, and only the first was argued.** A REDELIVERY is sequential, and safe for the same reason the gather is: the retirement is idempotent, and a redelivered `-Modified` on a round whose requests are already retired finds nothing to do. TWO DELIVERIES AT ONCE is the case that sentence does not cover, and reading it as covering both infers a stronger guarantee than exists. Substrate delivery is serialised within one publish and fully parallel across concurrent publishes — `EventSubscriptionRegistration` opens a fresh DI scope per delivery precisely because *"eight simultaneous publishes were observed running eight handlers at once on eight threads"* — so two `Approval-Modified` deliveries for one round can gather the same request ids before either write commits, and both then write. A direct call could not reach that shape; a subscription can. **#479 ruled that accepted, not a defect**, and ruled against a concurrency token on `ApprovalReviewRequest`, `ApprovalReview` or `AIReviewerAssignment`: the end state is identical either way — the request is soft-deleted — and nothing observes the duplicate fact, because nothing subscribes to `ApprovalReviewRequest-Modified` or `-Removed`. What the duplicate costs is a second `UpdatedWhen`/`UpdatedBy` stamp; if audit accuracy later matters, the outbox (#410/#411) is where it is bought, not a token on three entities.

   **Neither handler records an inbound `ProcessedEvent`, and that is ruled rather than omitted.** The dedup pair is a foundation request-handler device — both halves are storage-broker calls an orchestration may not hold (§ARC12.5), and there is no transaction here to commit them in, because this service writes no row of its own. `Documentation/Design/Events.md` §EVN19 rule 1 carries the ruling and its three reasons; what stands in its place is 4(i)'s signed status gate plus the idempotent gather above.

   **It terminates, and the argument is short.** Retiring a request publishes `ApprovalReviewRequest-Modified` / `-Removed`. Nothing subscribes to either address — not this service, not the round's — so the reaction has no second hop. §EVN18 rule (h)'s cycle rule is untouched: a retirement causes no approval decision and writes nothing the round reads.

   **§EVN18(e) is amended by this, and deliberately.** It records that `Approval-Added` and `Approval-Modified` have no workflow handler bound, on the reasoning that the *re-test* is driven by the entity's and the workflow records' facts. That reasoning is unchanged and the re-test still binds none of them. What (e) did not distinguish is a subscriber that is not re-testing, and rule 8's retirement is exactly that — it reads no §APR8.5 predicate, moves none, and decides nothing. See `Documentation/Design/Events.md` §EVN18(e) for the amended wording and the boundary it now draws.

5. **Every posture in §ARC16.7.4 travels with the operations and none of them is re-derived here** — the requesting tier and nobody else; account id, display name and username and nothing else; the round-keyed set with no caller-supplied ids; the double gate (this service admits the tier, the foundation applies §SEC14.7 posture D again beneath it, §SEC14.6 rule 2). A reviewer checking this split should be able to diff the moved methods and find no behavioural change outside business rule 4 — which is two changes, not one: the trigger becomes a subscription, and the rule 8 gather gains the answered-reviewer exclusion 4(ii) requires. Both are in the broker and the handler, not in the moved methods.

6. **The API surface does not move.** `ApprovalsController` splits into two thin exposers — one dependency each, per the exposer rule — and the second, `ApprovalReviewersController`, carries `[Route("api/Approvals")]` explicitly rather than by convention, so `ReviewerCandidates`, `ReviewerDisplayNames` and the three `ReviewRequests` verbs keep the URLs they have. `Websites/Glory2Him.WebApp.React/src/brokers/apiBroker.approvals.ts` is untouched, and the URL assertions already in `apiBroker.approvals.test.ts` are what proves it. The AI reviewer precedent went the other way — `AIReviewersController` took its own resource — because its routes were new; these are not, and a rename would be a breaking change bought for nothing.

7. **It gets its own exception family**, `ApprovalReviewerOrchestration*`, mirroring `AIReviewerOrchestration*`, and the controller maps it. An orchestration that leaked another orchestration's exception type would make the split invisible at the boundary that most needs to see it (`the-standard-orchestrations` ts-orchestrations-003).

### ARC12.6 Controller Layer *(formerly §12.6)*

Controllers expose API endpoints for the domain.

Current intended controllers:

| Number | Name | Purpose |
| --- | --- | --- |
| 1 | `ContentItemsController` | Exposes endpoints for content item creation, editing, versioning, submission, and soft delete. |
| 2 | `ContentItemSettingsController` | Exposes endpoints for content item policy settings. |
| 3 | `ApprovalsController` | Exposes endpoints for approval submission and status retrieval — the verdict, the decision and the reset. Bound to `IApprovalOrchestrationService` alone (#523 split the five reviewer routes onto entry 3a). |
| 3a | `ApprovalReviewersController` | **Built** (§ARC12.5.4 business rule 6) — #523. Exposes the reviewer-coordination endpoints — candidates, display names and the three review-request verbs — bound to `IApprovalReviewerOrchestrationService` alone. It carries `[Route("api/Approvals")]` explicitly rather than by convention, so the URLs do not move and no client changes. Numbered `3a` because it is entry 3 split in two, not a new resource, and named in the PLURAL like every sibling exposer — `ApprovalsController`, `ApprovalReviewsController`, `ApprovalCommentsController`, `AIReviewersController`. Entry 3 read `ApprovalController` here while the class on disk has always been `ApprovalsController`; that is corrected above rather than carried into a second name. |
| 4 | `ApprovalCommentsController` | Exposes endpoints for adding and reading approval comments. |
| 5 | `ApprovalReviewsController` | Exposes endpoints for submitting and reading approval reviews. |
| 6 | `TagsController` | Exposes endpoints for tag management. |
| 7 | `ReactionsController` | Exposes endpoints for reaction definition management. |
| 8 | `CommentsController` | Exposes endpoints for comment management. |
| 9 | `BibleReferencesController` | Exposes endpoints for Bible reference management. |
| 10 | `LinksController` | Exposes endpoints for link management. **Built** — this row read `LinkController` *(future)* while `LinksController.cs` has nine route attributes on disk. |
| 11 | `AttachmentsController` *(future)* | Exposes endpoints for attachment management. Upload and the media read are host endpoints following the profile-image precedent (§DOM5.6.2–§DOM5.6.3, §ARC17.6), not controller surfaces. |
| 12 | `ApprovalSettingsController` | Exposes endpoints for approval policy settings (§APR8.2, §APR8.4). **Built**, and absent from this table until now. |
| 13 | `AIReviewersController` | Exposes `GET`/`POST`/`DELETE api/AIReviewers/{entityType}/{entityId}` (§APR8.6.2). **Built** (#474), bound to `IAIReviewerOrchestrationService` alone, and absent from this table until now — the same drift §ARC12.5's orchestration table carried for the service behind it. **Its own resource, not a sub-resource of the approval round**, which is the precedent §ARC12.5.4 business rule 6 rests on: Berean's routes were new, so they took a new address, where the reviewer-coordination routes already exist and keep theirs. |
| 14 | `AssociationsController` | **Designed, not built** — the class does not exist and this table has never carried it, which is the same drift rows 12 and 13 record. Exposes the association resource: §ARC17.4's **CRUD six**, plus §ARC16.8's reaction summary read — which is that table's seventh row — and §ARC16.8.1's pair-keyed withdrawal, **each added by the work that needs it rather than all at once**. Bound to `IAssociationOrchestrationService` alone (§EVN13 rule 3), which is why the routes cannot be anchored under another resource's controller — §ARC17.4 has the full argument. Numbered after row 13 because it is a new resource, not a split. |

`ContentType` is not in this list — a fixed enum has no CRUD endpoints.

> **Every name in this table was singular and every class on disk is plural.** The rename flagged in review was rows 4 and 5; the same drift ran through rows 1 and 6–11, and two built controllers were missing altogether. All of it is corrected here rather than the two rows that were pointed at, because a table that is right in the places somebody checked and wrong everywhere else is worse than one that is uniformly stale — the next reader has no way to tell which rows were verified. Names verified against `Websites/Glory2Him.WebApp/Controllers/`.

### ARC12.7 SQL Storage *(formerly §12.7)*

SQL is the persistence layer behind `StorageBroker`.

The EF Core model snapshot currently shows tables and constraints for:

| Number | Name | Purpose |
| --- | --- | --- |
| 1 | `Approvals` | Stores approval workflow state for all approvable entity types. |
| 2 | `ApprovalComments` | Stores discussion and notes attached to approval records. |
| 3 | `ApprovalReviews` | Stores individual reviewer decisions for approval records. |
| 4 | `ContentItems` | Stores all versioned content item records. `ContentType` (§DOM3.6) is a column on this table, not a table of its own. |
| 5 | `ContentItemSettings` | Stores policy settings for content interaction behaviour per content type or content item. |
| 6 | `Associations` | Stores generic associations between content items and other entities. |
| 7 | `Tags` | Stores tag definitions used for content categorisation. |
| 8 | `Reactions` | Stores reusable reaction definitions. |
| 9 | `ApprovalSettings` | Stores approval policy rules per entity type and content type. |
| 10 | `Attachments` | Stores versioned attachment metadata. Binaries are designed to live in blob storage (§DOM5.6.1); `BlobUri` has no producer yet. |
| 11 | `BibleReferences` | Stores canonical scripture references keyed by `USFM`. |
| 12 | `Comments` | Stores discussion records attached to content through associations. |
| 13 | `Links` | Stores link records. |
| 14 | `ProcessedEvents` | Stores processed-event records for event-delivery deduplication per receiver (§SEC14.6 rule 4). |
| 15 | `ApprovalReviewRequests` | Stores review invitations — who has been asked to review an approval and has not yet answered (§APR7.9). |

#### ARC12.7.1 The second DbContext — reading the identity store *(formerly §12.7.1)*

`StorageBroker` is no longer the only `DbContext` in Core. `IdentityCoreStorageBroker` (§ARC16.7.4) opens a **read-only** window onto the SECURITY database over the `Glory2HimSecurityConnection` connection string, because role membership — which §APR7.9 rule 3 and the reviewer-candidates read both depend on — lives there and nowhere else. `ISecurityClient.Users` cannot answer it: that client reads a `ClaimsPrincipal`, so it only ever describes the current caller. Taking the host's word for who is eligible instead would repeat the caller-supplied-identity mistake that `ApprovalReview.ReviewerId` was deleted for, where free text let one reviewer meet a three-approval threshold alone.

Three rules keep the two stores apart:

1. **Read-only, enforced by the interface.** `IIdentityCoreStorageBroker` declares Select members only. No Insert, Update, Delete or Bulk member may be added — the identity store is another component's source of truth, and Core writing to it would put two owners on one schema. The connection string's principal should be granted `SELECT` and nothing more, so the rule survives an edit to the interface.
2. **Core owns no migrations against it.** The tables belong to `Glory2Him.WebApp`'s `SecurityDbContext`. Only the few columns the tier lookup needs are mapped, so a column added there cannot break a read here; and the context has no design-time factory, so an accidental `--context IdentityCoreStorageBroker` fails loudly rather than generating a migration that would fight the host's own.
3. **§SEC18.3's separation still holds.** Two databases on two connections means there is no SQL join between a user and an approval — Core reads each independently and combines them in memory. That is also why `ApprovalReviewRequest.RequestedUserDisplayName` stays denormalised (§APR7.9): a name must be fixed at request time rather than re-read across a boundary that may be unavailable.

**Consequence for the migration workflow.** With two contexts `dotnet ef` can no longer infer which one a command means, and a bare `dotnet ef migrations add` now fails with *"More than one DbContext was found"*. Core migrations must name the context:

```bash
dotnet ef migrations add <Name> --project Glory2Him.Core --context StorageBroker
```

### ARC12.8 Event System *(formerly §12.8)*

The event system decouples entity creation, update, and soft-delete operations from approval workflow side effects.

Events are published through the `EventBroker` and consumed by approval and orchestration services as required.

### ARC12.9 Content Analysis Service *(formerly §12.9)*

The `ContentAnalysisService` orchestrates AI-assisted analysis, duplicate detection, scripture extraction, categorisation, quality checks, and moderation suggestions.

The service may depend on `AIBroker`, `StorageBroker`, and approval/content services, but AI analysis must not replace human approval.

### ARC12.10 The Build Environment *(formerly §12.10)*

**Problem.** The build job runs on `windows-latest` and reaches SQL Server through LocalDB, which does not exist on `ubuntu-latest`. Every suite that needs a real database — the Core integration suite, the WebApp acceptance suite that boots the whole portal, the storage-client suites — and the migration-script rehearsal all resolve `(localdb)\MSSQLLocalDB` today. Moving the job to Linux means a SQL Server the job starts for itself, and that raises the first question this document has to answer rather than the analyst: how a suite finds that server without CI acquiring the ability to point a suite at a database it is not allowed to drop. Running the suites on Linux then surfaced two more, both of which are properties of the product rather than of the build — a native library the product depends on that exists for no Linux process at all (rule 10), and a broker whose construction requires a database (rule 11) — and this section rules those here because a build environment that cannot run the product is the same question wearing a different hat.

**No layer placement, and no boundary crossed.** Rules 1 to 9 describe no component of the system, so §ARC12.1's layering does not apply to them and there is no entity count to state. Rules 10 and 11 do reach the product, and neither moves anything between layers: rule 10 adds native binaries beside a package a broker already uses, and rule 11 states a requirement the build places on the suites, while the component decision behind it — when `EventBroker` creates its client — belongs to and is ruled in §EVN24. Nor does resolving a connection string from the environment cross anything this design guards. §EVN7's identity-on-the-envelope rule governs *who a caller is*, and a connection string asserts nothing about a caller. §ARC12.2's no-logic rule governs *decisions*, and choosing which server to reach is configuration, not a decision the system makes about its domain. This is recorded so it is not relitigated: a test fixture or a `DbContext` factory reading `ConnectionStrings:*` from the environment is inside the rules.

**Rule 1 — CI sets test-only connection keys, and never a production key.** The production keys are `Glory2HimConnectionString`, `EventHighwayConnectionString` and `Glory2HimSecurityConnection` (§ARC12.7.1, §SEC18.3). No CI job may set any of them in the ambient environment. The fixtures that drop catalogues resolve a **test-only** key precisely so that a host, container or CI job configuring the portal through the environment cannot be answered by the fixture's `EnsureDeleted` — this was settled by #302 and #351 and the code comments carry the reasoning. A Linux build that took the obvious route of exporting the production keys would undo it, and the failure mode is a dropped database rather than a red build. The test-only keys are `Glory2HimCoreIntegrationConnectionString`, `EventHighwayIntegrationConnectionString`, `Glory2HimSecurityIntegrationConnectionString` and `Glory2HimAcceptanceConnectionString`. Where a suite must hand the host a production key — the WebApp acceptance suite does — it stays where it already is, layered **in memory** over the host's configuration, never through the environment.

**Rule 2 — the override route already exists, and stays per-fixture.** Each fixture that owns catalogues builds its configuration as `appsettings.json` plus `AddEnvironmentVariables()` against its own key, so `ConnectionStrings__<Key>` already overrides the JSON with no code change at all. No shared "CI connection string" helper is introduced. One name in front of every store is exactly what the per-store keys exist to prevent, and an abstraction whose only caller is a CI job is not worth its cost. CI sets variables; the fixtures are already listening.

**Rule 3 — a local developer never needs Docker.** The JSON value is the fallback and stays LocalDB. No fixture may be changed to *require* the variable, and none may fail differently when it is absent. A developer with LocalDB runs the suites exactly as before this change.

**Rule 4 — one fixture has no route and must gain one.** `G2H.StorageClient.Tests.Acceptance` builds its connection string from an in-memory collection with the server hardcoded, so no variable can reach it. It gains the same shape as its sibling in `G2H.StorageClient.Tests.Integrations` — `appsettings.json` plus `AddEnvironmentVariables()` — and nothing more. That file already holds the value under `DefaultConnection`, which is the ASP.NET default name and therefore the key most likely to be set by something unrelated; it is a test-only catalogue either way, so rule 1 is not breached, but a rename to a key that cannot collide would be the safer shape and is left as an open question rather than ruled here.

**Rule 5 — the design-time factories are not a CI storage surface.** `Glory2HimContextFactory`, `StorageBrokerContextFactory` and `TestDbContextFactory` exist for `dotnet ef` and are never constructed by a running test. Both `dotnet ef` commands the build runs — `migrations has-pending-model-changes` and `migrations script --idempotent` — answer from the model and the migration history without opening a connection, so their hardcoded LocalDB strings only have to *parse* on Linux. They are therefore left alone. If either command is found to connect, the fix is rule 2's route on that factory, not a container address written into source.

**Rule 6 — the migration rehearsal must keep proving what it was built to prove.** The step exists as a regression guard for one specific failure: the generated script must apply under a client whose `QUOTED_IDENTIFIER` default is **OFF**, which is why `Tools/new-database-script.sh` prepends its own SET header and why the step applies the script *without* `sqlcmd -I`. A guard that runs under a client defaulting `QUOTED_IDENTIFIER` **ON** passes vacuously and the schema's filtered indexes lose their only cover. Whichever `sqlcmd` the Linux job uses, the rehearsal is only satisfied if a script with the header deliberately removed still fails it. Nothing else in the build notices a broken header.

**Rule 7 — integrated authentication does not survive the move.** `Trusted_Connection=True` cannot reach a containerised SQL Server, so the CI override strings authenticate with credentials the job mints for itself, and `Microsoft.Data.SqlClient` now defaults `Encrypt=True` against a container presenting a self-signed certificate — the override must say so explicitly rather than discover it as a connection failure. The credential is generated per run and masked; no long-lived secret is introduced, and none is added to the repository.

**Rule 8 — the workflow is generated, and the generator is what changes.** `.github/workflows/build.yml` is emitted by `Glory2Him.Core.Infrastructure`; a hand-edit is overwritten the next time the generator runs. The container is started by a step the generator can already express. A GitHub `services:` block is permitted only if ADotNet 5.1.0 already models it — bumping a package to obtain a YAML key is a dependency decision, and this design does not grant one.

**Rule 9 — a solution filter may narrow what `dotnet` builds, never what CI checks.** If the `.esproj` cannot restore or build on Linux, excluding it from the `dotnet` restore/build/test path via a `.slnf` is acceptable and is a build-plumbing change, not a design one. The four React gates — `npm ci`, `lint`, `test`, `build` — are unaffected and must all remain, because `dotnet build` never ran them in the first place: the esproj sets `ShouldRunBuildScript=false`. A filter that removed React coverage rather than React *project references* would be a weakening of the build and is refused.

**Rule 10 — the build platform must be able to load the product's native dependencies, and this grants the one package that makes that true.** `SkiaSharp` on `Glory2Him.WebApp` ships Win32 and macOS natives only; there is no `libSkiaSharp.so` anywhere in the package graph, so `ImageProcessingBroker` throws `DllNotFoundException` in any Linux process — the unit suite on `ubuntu-latest`, and equally a Linux host. `SkiaSharp.NativeAssets.Linux.NoDependencies` is added at the same version, on the same project. The grant is explicit because rule 8 withholds one, and it is a narrow grant: the package carries native binaries and no managed surface, so it adds no capability, only the missing half of a dependency already taken. The **NoDependencies** variant rather than the plain one because the broker decodes, crops, resizes and encodes WebP and renders no text, so system font discovery is never wanted, and the plain package's `libfontconfig1` requirement would be an unstated prerequisite on a host this document does not describe. That the variant alone carries the whole broker's work on Linux was verified, not assumed. If image text rendering is ever added, the package swaps to `SkiaSharp.NativeAssets.Linux` and the host gains the fontconfig prerequisite — name it here when that happens, because nothing else will.

**Rule 10 reaches past CI, deliberately.** `publish_webapp` produces a RID-less framework-dependent artifact, which carries every `runtimes/<rid>/native` folder its packages ship; before this rule those were Windows and macOS only, so the deployed portal could process images on a Windows host and not on a Linux one. **`g2h-dev` is a Linux App Service** — `ASP-Glory2Him-DEV` (B1) in UK South, runtime stack `Dotnetcore 10.0`, deployed by GitHub Actions and served at `dev.g2h.org`. That was established after this rule was written and is recorded here because native assets and path casing both depend on it. It settles the consequence: before rule 10 the deployed portal carried no `libSkiaSharp.so`, so **profile-image upload has been failing on `g2h-dev` with `DllNotFoundException` since it was first deployed there** — a live defect that predates the move to Linux CI and was invisible to a `windows-latest` build. Rule 10's package is its fix, and ships with that move. The cost is about 137MB of Linux natives beside the roughly 287MB of Windows ones the artifact already carries; a RID-specific publish would cut both and is not ruled here. The gain is cover: with the suites running on Linux, `ImageProcessingBrokerTests` fails on a missing Linux native on every PR, which is exactly what a `windows-latest` build could never report.

**Rule 11 — a unit suite must run with no database at all, on either operating system.** Rule 3 says a developer never needs Docker; the same line holds for the unit tiers, which must not require a SQL Server to be reachable anywhere. One place breaks it, and it is not the test's fault: constructing `EventBroker` opens a connection and migrates the substrate's store, so resolving any Core service needs a database — which is why `EventSubscriptionResolutionTests` passed only where LocalDB was ambient. §EVN24 rules that at the broker. It is **not** fixed by pointing the probe at the job's SQL Server, which would have a unit test create and migrate a catalogue, and **not** by reclassifying the test as integration, which trades away the composition-root check #490 deliberately built and makes its passing depend on the coupling it exists to detect. §EVN24 is therefore a prerequisite of the move to Linux and lands in its own change ahead of it, rather than widening this one.

**Nothing here touches the model.** No table, column, index, migration, seed, event address or service dependency changes — rule 10's package carries native binaries and no types, and §EVN24 changes when a client is created, not what is published or stored. A migration appearing in this work is a signal that the scope was wrong.

**Risks.** Reversible: the runner image, the container, the connection-string variables, the shell rewrites and the filter — all are one commit to the generator. Not reversible by a revert: a production key exported into a CI job that then drops a real database (rule 1), and a rehearsal that has silently stopped rehearsing (rule 6). Those two are the whole of why this is written down. A third, milder one: Linux is case-sensitive, so a `ProjectReference` path or an `appsettings.json` filename whose casing differs from the file on disk fails on Linux and cannot fail on Windows — cheap to fix, but it will not surface until the job runs. Rules 10 and 11 are both reversible — one package line and one broker field. The risk rule 10 was written against is no longer residual but realised and now named above: `g2h-dev` runs Linux, so image processing has been broken on the deployed portal, and nothing reported it because no test ran on Linux. What remains is the general form of that defect rather than this instance of it — a Linux host exercises native assets and case-sensitive paths that a Windows build never touches, and the Linux unit run is now the only thing in this repository that reports either before a deploy does.

**Out of scope.** Deploying to Linux, and the `publish_webapp` and `deploy_webapp` jobs, which already run on `ubuntu-latest` and are unchanged — with the one deliberate exception that rule 10's native assets travel inside the publish artifact and therefore reach whatever host it is deployed to; confirming on the deployed site that profile-image upload works once rule 10's package reaches it, which is its own issue — the hosting platform itself is no longer open and is recorded under rule 10; a RID-specific publish to trim the artifact; running the portal or the developer loop on anything but Windows; consolidating the connection-string keys; replacing LocalDB for local development; and the `DefaultConnection` rename raised in rule 4.

## ARC16. Recommended Service Responsibilities *(formerly §16)*

### ARC16.1 ContentItemService *(formerly §16.1)*

Responsible for:

1. Creating content item versions.
2. Updating `IsPublished` flags when approval completes.
3. Validating content item fields.
4. Reading content by id, group id, type, latest version, and published version.
5. Reading content by (`ContentType`, `ContentHash`) for duplicate detection.
6. Applying soft delete fields.

### ARC16.2 AssociationService *(formerly §16.2)*

Responsible for:

1. Creating associations.
2. Validating scope consistency.
3. Validating supported `EntityType`.
4. Applying publish date rules.
5. Reading associations for content item display — the **self-only** filter §SEC14.3's Layer paragraph assigns the foundation, covering rules 1, 2 and 5. The composite that adds rules 3, 4 and 6 is one evaluator on the orchestration, consumed by all three of that service's reads and placed by §ARC16.8's shared-rule table row.
6. Reading topic children.
7. Applying soft delete fields.

#### ARC16.2.1 The §DOM6.10 Facet Gate On The Association Write

**RULE — the §DOM6.10 facet gate runs on the orchestration, not on the foundation.** Its site is `AssociationOrchestrationService`'s write flow, and its position in that flow is fixed: **after** the contribution gate, **after** both endpoints resolve and **after** the `UserId` derivation, and **before** the canonical-pair probe. The position is not cosmetic. The gate asks the winning setting of **each endpoint that resolves a settings entity**, which is known only once resolution has derived `Entity{A,B}ContentType`, the scope and the version group's effective id from storage rather than from the caller; §DOM6.10 rule 1 (`Domain.md:836`) resolves the switches per endpoint from that endpoint's own settings entity, and rule 2 (`Domain.md:837`) permits the association only when **both** resolving endpoints allow it, denials unioning restrictively, which §DOM4.8's non-content-item host paragraph (`Domain.md:467`) states again in one sentence; and the act it judges is partitioned by `UserId`, which this flow derives and never accepts. Running before the probe means a refused act never reaches a row at all — the refusal is the first thing that happens once the flow knows what is being asked.

**It is recorded under §ARC16.2 rather than under §ARC16.8 because §ARC16.2 is where the association's write rules already live**, while §ARC16.8 is the same service's read. §ARC16.8 points at this section and does not copy it; a rule stated twice is a rule that will eventually disagree with itself.

**The exposer half is existing design and is cited rather than restated.** §EVN13 rule 3 (`Events.md:979`) already binds a controller to "the entity's **top-layer service** — the highest business layer that owns that entity's write flows: its orchestration service where it has one, otherwise its processing service, otherwise the foundation itself", and it names this entity in the same breath: "`Association` carries an orchestration above its foundation and the approval workflow still binds its foundation facts." An exposer calling the association foundation service directly is therefore already ruled out, and nothing new is recorded for it here. **It does not bar the host from registering `IAssociationService`.** `CoreRegistration.cs:234` registers it and must keep registering it, because the orchestration composes its own entity's foundation service (§ARC12.1); nothing injects it except that orchestration and the event registration, and no exposer takes it, so the rule holds today with no change. Recorded so that a later reader does not "enforce" the exposer rule by deleting a registration the orchestration needs.

**RULE — the gate is reached on both entry paths, and the event path is moved rather than excused.** §SEC14.6 rule 1 (`Security.md:150`) binds — "**Every service enforces security itself.** … No service ever assumes an upstream layer already gated the caller" — so an orchestration-only gate with `Association-Adding` still entering the foundation's add would leave a published door into an ungated write. The design has already ruled this exact case, and it is applied rather than re-argued: **RULE — an address whose handler derives an authorization input binds the layer that derives it** (`Architecture.md:535`). `Entity{A,B}ContentType` is an authorization input — §DOM4.5 rule 3, "a caller who could set it could claim authority over a content type they hold no role for" — it is derived in `AssociationOrchestrationService`, and the foundation validates it for enum-definedness only. **`Association-Adding`'s subscription is therefore moved to the orchestration, gated, with ONE implementation, because both entry paths then converge on the gating service.** That **moved subscription** is **#631**'s work, not this section's. The other four `Association` addresses stay on the foundation for that same rule's own reason: nothing is derived on them, and routing them upward "would add a layer that only forwards". The foundation keeps every gate it already holds — it still refuses a terminal-row amendment, still pins the endpoints, still normalises canonical order (§DOM4.4 rule 4) — so nothing is weakened by the move; the facet gate simply sits where the derivation it depends on already lives. **§SEC14.6 rule 1 is satisfied rather than waived:** once both doors open onto the orchestration, every rule the orchestration holds runs on every entry path, which is what rule 1 asks.

**`Architecture.md:541`'s deferral is superseded by that moved subscription, and is amended by #631 rather than here.** That paragraph records the `Association` gap as "left open deliberately rather than by oversight" while the orchestration's classification was under revision, and the deferral ends with the ruling above: this section is the answer that paragraph was waiting for. What replaces it there is a record that the **moved subscription** happened, which cannot truthfully be written while the subscription has not moved, so #631 owns that sentence under its criterion 9 and nothing at that anchor is amended here.

**Two alternative shapes were considered and refused**, named so that neither is re-proposed. **Accepting the address as ungated** would put a published address into the design as a standing hole in a security gate, and §DOM6.1 makes this switch a server-side gate precisely "so the UI cannot be bypassed". **Closing or narrowing the address** would remove a public contract whose event name is inside the HMAC and whose subscription carries a stable identifier; removing a door is a larger change than moving it, and the design's own precedent moved it.

**RULE — the effective setting of the endpoint being asked arrives through `IAccessBroker`, and the gate takes no second entity service.** The broker **gathers** rather than decides: §APR8.6.1 rule 3 (`Approval.md:544`) — "Foundation services reach it through an **`IAccessBroker`** in `Brokers/Securities/`… the policy broker is a dependency like any other, so the service stays single-entity" — and the split is stated in one sentence at `Approval.md:554`: "`securityClient.Access` decides, `IAccessBroker` in `Brokers/Securities/` gathers." **No Florance count changes and none is claimed:** a broker sits outside that count by §ARC12.5's own rule (`Architecture.md:450` — "a broker obliges none"), and §ARC12.5 already blesses `IAccessBroker` on an orchestration by name. An `IContentItemSettingService` dependency instead would be an **eighth** entity service on a service §ARC12.5 entry 1 records as already breaking the dependency-count guidance, and a recorded break is not an approved deviation — an existing deviation is never justification for another.

**The departure from the broker's charter is declared rather than made silently.** §APR8.6.1 rule 4 (`Approval.md:545`) is that charter — "**The broker returns a verdict, not settings.** If it handed back an `ApprovalSetting`, the decision logic would be re-implemented in every foundation service and would drift. One question, one answer, one place." — and this gate departs from it, because it needs the `<Facet>Allowed` switches handed back as settings. **Rule 4 is named here only as the rule departed from; it is not the precedent for the gathering, and it says the opposite.** The justification is re-derived rather than inherited: the arm has **two call sites, both methods of one class** — this gate asks the `<Facet>Allowed` switches and §ARC16.8's read asks `Show<Facet>` for §SEC14.3 rule 6 — and the two ask **different questions of the same row**, so a verdict arm would have to be two verdict arms. Rule 4's stated reason is drift across **every foundation service** — its own words, "the decision logic would be re-implemented in every foundation service and would drift" — and two call sites in one class is not that; the precedence itself stays in one place behind the arm either way. **Rule 4 names neither a count of services nor approvable ones**: the sharper phrasing — "each of the seven approvable services, which is seven places for it to drift" — is `IAccessBroker`'s own XML doc comment (`Glory2Him.Core/Brokers/Securities/IAccessBroker.cs:34-35`, wrapped across two lines, which is why a single-line grep of the solution misses it; a grep of the design files finds nothing because the phrase was never in them), and it is named here so that it is not promoted out of that comment into a design citation again.

**RULE — §DOM6.10 precedence resolves in exactly one place, and every consumer reaches that place rather than restating it.** **Where that place is was already ruled, and is cited here rather than written a third time:** §ARC16.8's shared-rule table row for §DOM6.10 effective-setting precedence (`Architecture.md:1250`) places the resolution in the **query-shaping function `AccessBroker` authors for its gather** and states in terms that it is **not** a storage-broker predicate — §ARC12.2.1 rule 1 (`Architecture.md:154`) makes "which row wins when two match" a decision the broker may not compose. §DOM6.4's two tiers are a **selection, not a merge**: the content-type default (§DOM6.3) and the content-item override (§DOM6.4) are two candidate rows and the resolution picks **one** — **narrowest scope wins**, the override where one exists for that content item and the default otherwise. No field is combined across the two rows, so there is no per-field precedence to get wrong. The consumers, named rather than routed — that same row leaves where §ARC16.3's and §ARC12.5.2's callers reach the rule "an open question and not a licence", and nothing here answers it:

1. The `IAccessBroker` arm this gate calls.
2. `IContentItemSettingsService` — §ARC16.3 responsibility 3, "Resolving effective settings" (`Architecture.md:899`).
3. The processing member **#209** is to build.
4. §ARC16.8's §SEC14.3 rule 6 term (`Architecture.md:1155`), which asks `Show<Facet>` of the same resolution.

**#209 is not a blocker of this gate.** The gate's dependency is that one resolution whether or not the member exists; if #209 lands it becomes another consumer of it and never a second copy of the rule, and this section is unchanged by that.

**RULE — ONE gate covers every association type: in each orientation the host names the settings entity and the far end names the switch asked of it.** The two halves have two sources and neither covers the other. The host half is §DOM6.10 rule 1 (`Domain.md:836`) — the switches "are resolved per endpoint, from that endpoint's own settings entity" — over §DOM6.10's host table. The far-end half has two sources of its own, and neither covers all six types. §ARC12.3.1 association rule 1 (`Architecture.md:277`) maps **four** of them onto their switches — its scope line (`Architecture.md:275`) names `Tag`, `Reaction`, `Comment` and `BibleReference` and no others — while the `Link` and `Attachment` rows rest on §DOM6.5's own pairing of area to switch (`Domain.md:754-759`). Between them the mapping carries all six §DOM6.5 pairs; there is no per-type gate and no per-type copy of one.

**Host: `ContentItem`.** These rows are the `ContentItem` column of a host × far-end matrix rather than the whole matrix, because both of their sources are `ContentItem`-host sources: the rows are §DOM6.5's `ContentItemSetting` settings table, and the far-end half is §ARC12.3.1 association rule 1, which says "the effective `ContentItemSetting`" in the same sentence. **A `BibleReference` host is deliberately not tabled**, and the two Bible-reference switch names are both correct for different cases rather than one being a mistake for the other: `BibleReferenceAllowed` is the switch for a `BibleReference` far end on a **`ContentItem`** host, while `RelatedBibleReferencesAllowed` is the switch for one on a **`BibleReference`** host (§DOM6.10 rule 2, `Domain.md:837`). A `BibleReference`-host row has unruled cells, so writing it would invent rules — see the gap paragraph below.

| Far endpoint type | Switch asked on the `ContentItem` host's winning setting |
| --- | --- |
| `Tag` | `TagsAllowed` |
| `Reaction` | `ReactionsAllowed` |
| `Comment` | `CommentsAllowed` |
| `BibleReference` | `BibleReferenceAllowed` |
| `Link` | `LinksAllowed` |
| `Attachment` | `AttachmentsAllowed` |

**RULE — the gate runs on three acts and no others: the add, the reaction repoint and the revive.** Those three are the arms of **`UpsertAssociationAsync`** (#624), this service's caller-facing write, that end with the caller **holding** an association — creating one, changing which reaction a personal row points at, and re-giving one that was withdrawn. The gate is written **once** and reached on both entry paths, per the event-path rule above. The **add** arm is the one act that runs on both personalities, editorial and personal; the reaction repoint and the revive are personal-only (#624 §5).

**The withdrawal is NOT gated, and this section says so rather than leaving it to be inferred.** `RemoveAssociationByPairAsync` — recorded in §ARC16.8.1 by #624, built by #618 — is outside this gate, and the gate is therefore **not** described as running at every write entry, which would sweep the withdrawal in. The rule that puts it out turns on **creation**, and both sources say so in that word rather than reaching removal: §ARC12.5.2 rule 7 (`Architecture.md:544`) — "Disabling a feature in settings must prevent the creation of new associations of that type for the affected content items" — and §ARC12.3.1 association rule 1 (`Architecture.md:277`) — "A link may only be created when the effective `ContentItemSetting` permits that association type". Removal is governed by **ownership**, not by the facet switch: §SEC14.7 posture A rule 3's remove sentence (`Security.md:263`) — "Remove: owner or `Administrators`". A gate over the withdrawal would trap a reader's existing reaction on the card the moment an administrator switched `ReactionsAllowed` off, which is the opposite of what switching it off is for.

**Two senses of "repoint" meet here and must not be collapsed.** The **reaction repoint** is a personal row's `EntityBKeyId` moving from one `Reaction` to another (#624), and it is one of this gate's three acts. The **moved subscription** is the `Association-Adding` address moving to this service (#631), and it is how the event path reaches the gate. They share a word and nothing else, so this section writes "reaction repoint" and "moved subscription" rather than relying on context to separate them.

**The `BibleReference` host is a GAP pending §DOM6.9, not §DOM6.10 rule 3.** A `BibleReference` that hosts associations resolves `BibleReferenceSetting` — §DOM6.10's host table (`Domain.md:829-832`) names it, and §DOM6.10 rule 4 (`Domain.md:839`) requires one of every host type. **The mapping table above is the `ContentItem` host's switch names and does not answer for this host.** §DOM6.9 as it stands answers **three of the six facets** for its own host: `TagsAllowed`, `RelatedBibleReferencesAllowed`, and `ReactionsAllowed` with `LimitReactionsToLoveOnly`. **Which switch such a host resolves for `Comment`, `Link` and `Attachment` is not settled.** §DOM6.9's property table (`Domain.md:800-812`) carries no `CommentsAllowed`, `LinksAllowed` or `AttachmentsAllowed`, and no section names a switch that permits or refuses those three facets on such a host. The attachment case is **live rather than impossible**: §DOM4.9 names "the verse image of a Bible reference" (`Domain.md:473`), its rule 7 (`Domain.md:490`) derives the scope for "a non-versioned host such as `BibleReference`", and `G2H Design.md:415` makes an `Attachment` ↔ `BibleReference` association with `Purpose = Verse` a named replication proof. **What does not exist is the entity.** `BibleReferenceSetting` is designed and not built — no type, no table, no service, measured as zero hits across the solution's `.cs` files at branch time — so a `BibleReference` host resolves **no settings entity at all** today, and **every** facet on such a host is ungated, not the reaction alone. A reader enforcing this gate must not gate tags on a passage page against an entity that is not there. Both debts — the absent entity and the three unsettled facets — are owed by §DOM6.9's build, which is out of scope here. **Raised and deliberately not answered here:** §DOM6.10 rule 3 rules what an absent settings **entity** means, and nothing rules what an **absent switch on a present entity** means — whether it refuses or admits. §DOM6.10 rule 3 (`Domain.md:838`) is a different case entirely — an endpoint type with no settings entity **by design**, which under rule 4 is the far-end-only types `Tag` and `Reaction` — and it must not be cited for the `BibleReference` host, because that would record a build gap as a design decision and leave it standing permanently.

**RULE — the gate has exactly two refusals, and each names the switch that refused.** The first is the facet switch itself: in a given orientation, the host's winning setting carries the mapped `<Facet>Allowed` as `false`. The second is the narrowing: that same setting carries `LimitReactionsToLoveOnly` and the far-end `Reaction` is not Love — matched on `Reaction.Name`, trimmed and case-insensitive. **That match is this section's own statement of it**: §DOM6.7 (`Domain.md:778-786`) declares `LimitReactionsToLoveOnly` and one sentence of its purpose, and specifies no name match, no trimming and no case rule — it is cited as **where the property is declared**, not as the specification of the match. The rename hazard rests on **#201**. **The gate asks `<Facet>Allowed` and never `Show<Facet>`**: the display switch answers §SEC14.3 rule 6 on the read and has no part in the write.

**Each refusal is a validation failure of the layer that raised it, answered `400`.** On the orchestration that is `InvalidAssociationOrchestrationException`, mapped by this service's `TryCatch` to `AssociationOrchestrationValidationException`. **The exception names moved with the placement**: while this gate was read as the foundation's it was recorded as `InvalidAssociationException` wrapped as `AssociationValidationException`, and those two names keep a different meaning on this service — they are what the **foundation** raises and what this service maps as a *dependency* validation failure, so using them for the gate's own refusal would report an orchestration rule as a foundation one. The response code is `400` under either name and does not change with the layer.

**The gate never falls open.** A settings read that fails is a dependency failure of the layer it happened in and surfaces as that layer's dependency exception; it is never read as permission granted, and an unresolved setting refuses rather than admits.

**§SEC14.5's no-existence-leak posture does not apply to these two refusals, and the endpoint-existence half is unchanged by this gate.** That posture exists so that an unprivileged probe cannot distinguish a non-public entity from one that does not exist. This gate runs **after** endpoint resolution has already established that the caller may see both endpoints, so there is no existence left to hide, and naming the switch that refused discloses a policy on a row the caller can already see rather than anything about a row they cannot. Endpoint existence and visibility keep the not-found posture exactly as resolution already gives it — a non-existent or non-visible endpoint still surfaces as not-found, before this gate runs at all.

### ARC16.3 ContentItemSettingsService *(formerly §16.3)*

Responsible for:

1. Creating default settings per content type.
2. Creating overrides per content item.
3. Resolving effective settings.
4. Validating whether tags, comments, reactions, links, attachments, Bible references, and child content associations are allowed.
5. Applying soft delete fields.

### ARC16.4 ApprovalService *(formerly §16.4)*

Responsible for:

1. Creating approval records.
2. Reading approval status.
3. Submitting items for approval.
4. Applying approval status transitions.
5. Enforcing approval uniqueness per entity.
6. Recording bypass approvals (`IsApprovedByBypass`). Review dismissal is applied on `ApprovalReview` records via `ApprovalReviewService` — `Approval` records never hold `Dismissed`.

### ARC16.5 ApprovalReviewService *(formerly §16.5)*

Responsible for:

1. Recording reviewer decisions, with `CreatedBy` bound to the acting user and pinned against storage on modify.
2. Validating reviewer eligibility — **the row-local half only**: the contribution gate and the coarse suffix-matched review-role gate, and **on the add path only**. Modify and remove run the plain contribution gate; the role question on those paths is answered solely by `IAccessClient` (§ARC12.3.1).
3. Row-level write permission, which is **ownership and not eligibility**: modify and remove are **owner-only** — not `Publishers`, not `Administrators`. An administrator who needs past a standing rejection bypasses (§APR8.6.1) rather than editing the review. Hard remove is `Administrators` only and takes no access decision at all. *(This replaces "owner-or-`Administrators` for amending or withdrawing a verdict", which the code has never done.)*

Explicitly **not** its responsibility, despite earlier drafts of this list: enforcing one active review per reviewer, evaluating approval thresholds, and excluding dismissed reviews from those calculations.

Earlier drafts justified all three with one reason — *"all three need `ApprovalSetting` and the whole review set, which a single-entity foundation service may not read"*. That is **true of the last two and false of the first**, and the difference matters:

- **Threshold evaluation and the dismissed-review exclusion** do need the policy and the whole review set, so they belong to `IAccessClient` behind `IAccessBroker` (§APR8.6.1) — which is where they are.
- **One active review per reviewer** needs neither. The decision function reads only the gathered review list and the actor id; the index needs nothing at all. It is excluded from this service for a different reason — the service cannot read its sibling rows, which is a *reach* limit rather than a *policy* one — and it is enforced in **two** places with two different caller-visible failures. §ARC12.3.1 records the split; do not restate it as belonging to one layer.

### ARC16.6 ApprovalSettingsService *(formerly §16.6)*

Responsible for:

1. Managing approval policy rules.
3. Resolving effective approval settings.
4. Validating approval configuration.

### ARC16.7 ApprovalOrchestrationService *(formerly §16.7)*

Responsible for:

1. Coordinating approval submission.
2. Coordinating review decisions.
3. Applying approval policy outcomes.
4. Handling event-driven approval creation or reset.
5. Updating the denormalized `ApprovalStatus` on the owning entity, for example setting `ApprovalStatus = ApprovalStatus.Approved` when the required threshold is met.
6. Publishing content versions when approval completes.
7. Using `SecurityBroker` for user and role checks.

#### ARC16.7.1 The settled shape *(formerly §16.7.1)*

**Three dependencies: `IApprovalWorkflowService`, `IApprovalReviewWorkflowService`, `IAIReviewerAssignmentWorkflowService`** — plus brokers. All three are **workflow seams** rather than the public services (#287, #290, and #474 for the AI assignment's). The review seam carries one member, because the orchestration's only write to a review is the automatic dismissal of a stale one, and this service calls one member of the AI seam, for the twin of that act on Berean's assignment. The approval seam carries four, because the orchestration opens, reads, decides and resolves the round — but every one of them is the workflow acting rather than a person, so all four mint the system identity in process and none accepts a context from a caller. Narrowing them that far is a compile-time guarantee in place of an assertion — the orchestration cannot read, amend or remove a review, because the type it holds has no such member. It takes **no entity services at all**, which is what keeps it inside the dependency-count guidance that §ARC12.5 entry 1 is already recorded as breaking. It also takes no `IApprovalSettingService`: resolving §APR8.4 here would put most-specific-wins in a second place beside the decision function, which §APR8.6.1 rule 4 exists to prevent. Every policy question is asked as a verdict.

> **This sentence said "three dependencies, not ten" while the constructor held seven, and the drift is worth naming rather than quietly fixing.** The three it listed were right when written; what grew afterwards were `IApprovalCommentService`, `IApprovalReviewRequestService`, `IApprovalReviewRequestWorkflowService`, `IAIReviewerAssignmentWorkflowService` and `IIdentityUserService`, each added for a feature that was reviewed on its own merits and none of them re-tested against this paragraph. The count is now the split of §ARC12.5.3–§ARC12.5.4: the round's three above, and `ApprovalReviewerOrchestrationService`'s four (§ARC12.5.4 business rule 1, and §ARC12.5's register of approved deviations for why four was granted). `IApprovalCommentService` and `IIdentityUserService` are on that service, not this one — the only thing this service reads a comment for is the §APR8.5 count, and that arrives as a verdict.

> **A seam may grow a second verb, and `IAIReviewerAssignmentWorkflowService` has — ACCEPTED, on a stated limit. BUILT (#534).** §APR8.6.2.1's `AddAutomaticAIReviewerAssignmentAsync` sits beside `ReturnStaleAIReviewerAssignmentToPendingAsync`, called by `AIReviewerOrchestrationService` rather than by this one. A seam narrowed to a single member is the strongest form of the compile-time guarantee above, and widening one is the direction that loses guarantees, so it is worth saying what is being accepted and what is not.
>
> **What makes it acceptable is that both verbs are the same KIND of act**: nobody's. Each mints the system identity in process, neither takes a context from a caller, and both exist for the same reason the interface documents — the system identity holds no roles by construction, so the caller-facing gate cannot admit it. A second verb of that kind narrows nothing that was not already narrow: this service still cannot read, amend or remove an assignment, because the type it holds has no such member.
>
> **The limit — a workflow seam must NEVER carry a caller-facing verb.** The moment a member of one could be reached on a person's behalf, the seam stops being an authorization device and becomes a second public surface with a weaker gate, and every "the caller asks for the ACT and the service supplies the identity" claim in this document stops holding for that class. That is the tripwire, not the member count. A seam growing a third verb of the same kind is a smaller question than a seam growing its first verb of the other kind.
>
> **The count is untouched either way.** Two interfaces onto one class count once where they share a family (finding 2 below), and both doors here route through `AIReviewerAssignmentService`'s own `TryCatch`, so the second verb costs no arm on either consuming service. §ARC12.5.3 rule 19 records the same thing from the round's side.

**RULE — the Florance count counts EXCEPTION FAMILIES: one per downstream family this service must catch and map.** Not objects, and not models. The pattern exists to cap the `TryCatch`, and the `TryCatch` grows per family — each downstream contract obliges an arm of **up to four blocks** (`…ValidationException`, `…DependencyValidationException`, `…DependencyException`, `…ServiceException`) inside this service's single generic `TryCatch<T>`. Four dependencies is up to sixteen blocks before this service's own exceptions are counted, which is the runaway the limit is against.

**An arm is UP TO four blocks — a read-only contract raises no dependency-validation family and costs three.** The bound is deliberately an upper one rather than a flat number, and stating it flatly is what made the inventory below wrong: a read-only contract has no uniqueness collision, no foreign-key violation and no constraint conflict, so there is nothing for a dependency-validation family to carry, and `IIdentityUserService` is the worked example. **Nobody is to mint the fourth type for symmetry** — there is no `IdentityUserDependencyValidationException` anywhere in the solution and there should not be one. The upper bound also leaves the runaway argument exactly where it was: the cap is on how fast the `TryCatch` can grow, and an arm that costs three rather than four does not make a fifth dependency affordable.

**RULE — a count over three needs an entry in §ARC12.5's register of approved deviations.** That is where the protocol for granting one lives, beside the rule an author of a new orchestration reads; this section deliberately holds only the counting rule and the inventory it produces.

**A missing arm is a defect, not untidiness, and that is what makes the count the right metric.** `ApprovalOrchestrationService.Exceptions.cs` states the cost itself, above its `ApprovalReviewRequest` arm: *"Without these the whole family falls to the catch-all below and every routine refusal — an over-long deletion reason, a `ReadOnly` caller, a uniqueness collision — is reported as a 424 infrastructure fault, and the Conflict branch on the exposer is dead code that can never be reached."* A dependency you have not written an arm for is not a cheaper dependency; it is a dependency whose validation failures lie to the client.

Three consequences, and the second is the one that decides the question this rule was written for:

1. **A contract exposing only primitives still counts in full.** It returns no model, so a count of *objects* or of *entity types* scores it zero — and it still has four families to map. Counting anything but the arm undercounts exactly this case.
2. **Two interfaces onto one class count ONCE if, and only if, they share one family — which is a fact to verify, never to assume from the registration.** All four of this solution's workflow seams — `IApprovalWorkflowService`, `IApprovalReviewWorkflowService`, `IApprovalReviewRequestWorkflowService`, `IAIReviewerAssignmentWorkflowService` — are second interfaces on the foundation class they narrow ("ONE object behind two doors", `ServiceRegistration.AddApprovalService`), and each routes through **that class's own `TryCatch`**: `ApprovalReviewRequestService.RetireApprovalReviewRequestAsync` wraps both retirement verbs in the same `TryCatch` the caller-facing operations use, so both doors throw `ApprovalReviewRequest*` and cost one arm between them. A seam that grew its own family would cost two arms and would count twice, and nothing about it being one object would change that.
3. **Brokers stay outside the count because they oblige no arm**, not because of which layer they sit in. A broker raises no exception family of its own (§ARC12.2 — it holds no logic to validate), so its raw failures are what the closing `catch (Exception)` is for. §ARC12.5's broker rule states this the same way. A broker that ever did raise a family would count like any other contract.

> **Measured against that rule, THREE ARMS WERE MISSING, and issue #518 added them ahead of the split rather than leaving them for it to inherit.** `ApprovalOrchestrationService` holds six foundation objects and, before #518, mapped only **three** families — `Approval*`, `ApprovalReviewRequest*`, `AIReviewerAssignment*` — while `ApprovalReview*`, `ApprovalComment*` and `IdentityUser*` had no arm at all: `DismissStaleApprovalReviewAsync` propagated into the single `TryCatch<T>` with no local handler, and the comment resolver's and the candidates read's failures did the same, so all three landed on `catch (Xeption downstreamException)` and reached the client as a 424 — precisely the fault the `ApprovalReviewRequest` arm was written to stop. All six families are mapped now; the service maps all six until the split moves three of them.
>
> **This is the real shape of the runaway, and it is worth naming because it is not the shape the limit is usually policed for.** A service over the Florance count does not announce itself with an explosion of catch blocks; the arms simply stop being written, one dependency at a time, and each omission looks locally harmless. Counting objects would have hidden it completely — six objects, three arms, and no number in the constructor that says so.
>
> **The post-split inventory is NOT the same on both sides, and neither the arm count nor the block count is.** `ApprovalOrchestrationService`: `Approval*`, `ApprovalReview*` (**landed by #518, stays**), `AIReviewerAssignment*`, with `ApprovalReviewRequest*` **removed** along with the dependency — **three** four-block arms, **twelve blocks**, at the Florance ceiling and not over it. `ApprovalReviewerOrchestrationService`: `ApprovalReviewRequest*` (moved), `ApprovalComment*` (**landed by #518, moves**), `IdentityUser*` (**landed by #518, moves**) and `Approval*` (**new, and the cost of keeping the read-triggered repair**) — **four** arms, **fifteen blocks**, which is 4 + 4 + 3 + 4. That is OVER the guidance, deliberately, and §ARC12.5's register of approved deviations records why. Twenty-seven downstream blocks between the two services. A reviewer should check the arms before the constructor: the arms are what the rule is actually measuring.
>
> **`IdentityUser*` is the THREE-type family, and that is correct rather than an omission.** `IIdentityUserService` exposes two operations and both are reads, so no dependency-validation family exists for a fourth block to carry — the absent type reads as right rather than as something nobody got round to, and nobody is to mint it later for symmetry with the arms beside it (finding 1).

**Every COUNT comes from `IAccessBroker`; every WRITE needs a seam, and that is the whole of why the review and AI dependencies survive.** Confirmed against the broker's surface: `EvaluateApprovalConditionsByIdAsync` carries `ApprovalCount`, `RequiredNumberOfApprovals` and `UnresolvedApprovalCommentCount`; `FindDismissableApprovalReviewIdsAsync`, `FindRetirableApprovalReviewRequestIdsAsync` and `FindResettableAIReviewerAssignmentIdAsync` each answer a whole "which rows need this doing to them" question; and `RetrieveApprovalReviewerScopeByIdAsync` gathers the round's status, the entity's owner, the role subjects, the active and recorded reviewers and the active requests in one read. So nothing above this layer reads a review, a comment or a request row *to count it*. What the broker does not do is write — it gathers and it decides, by design (§ARC12.3.1) — so dismissing a stale review, retiring an invitation and returning Berean to pending each still need the foundation seam that owns the row. **A reviewer proposing to drop one of these three dependencies must say which write it replaces, not which read.**

**And a workflow seam is neither a processing service nor a layer.** It touches one entity type, so §ARC12.1 rule 1 already bars it from being an orchestration; it composes no repeated calls to the foundation and owns no cross-row rule, so §ARC12.1 rule 3 does not earn it a processing service either. What it exists for is **authorization, not decomposition**: the system identity holds no roles (`IEventEnvelopeBroker.CreateSystemAsync` mints it deliberately roleless), so it cannot pass the caller-facing gate, and §APR7.9 rule 6 states the shape that follows — the caller asks for the ACT and the service supplies the identity, which is what makes the flag unforgeable by construction rather than by validation. Adding a layer to carry that would be a layer that only forwards (§ARC12.1).

**The human authorises once, on the `Approval` row; the entity write is a sync.** This is forced rather than chosen. The decision function refuses any outcome when the approval is not `Submitted` — `ApprovalNotOpenForReview`, checked before policy is resolved — so writing the `Approval` row first and then asking the same question again on the entity under the same human identity fails deterministically on the happy path. And §APR9.8 names `Approval.ApprovalStatus` the source of truth, so a repair pass can only mean *drive the entity to match the approval*; entity-first would make repair revert a real decision. The entity write is therefore a denormalisation sync performed under the workflow's own identity, not a second authorisation.

**The sync is a command event, not a method call.** The orchestration publishes an instruction to the owning entity's request address and observes the reply, rather than calling the foundation service directly. Two consequences follow, and both are the point: the orchestration needs no entity services, and each side is testable on its own — the orchestration proves it published the command, the foundation proves it honours one. A single method call would be invisible to both.

Because the sync is asynchronous in principle, the orchestration carries an explicit *requested but unconfirmed* state. §APR9.8's "must never diverge" is a steady-state invariant, not a claim that the two rows are written in one instant. **No reconcile path exists to settle a sync whose reply never arrived** — an earlier version of this sentence claimed one does, and it was wrong: §ARC16.7.2's read-triggered repair only opens a MISSING round and never reconciles an existing one, and no sweep runs. [`§EVN23`](Events.md) rules what a publisher must do about that in the meantime, which is report it.

**Provenance is carried by a signature, not by a call site.** §APR9.7.1 rule 3 admitted the workflow's system identity only on a context minted in process, on the reasoning that provenance is not carried by the payload. That reasoning is superseded: every inbound envelope is signature-verified at the receiver, and **only this system holds the signing key**, so a verified envelope is one this system minted — whichever path it arrived by. The claim itself sits inside the signed payload, so it cannot be added to a genuine envelope without breaking the HMAC, and a fresh envelope carrying it cannot be produced without the key.

That is verifiable provenance rather than a convention, and unlike the call-site argument it survives the write travelling over an event. It rests on one assumption worth stating: the signing key never leaves this system. Signing happens internally, after an API has already authorised the caller; verification exists to detect tampering, not to distinguish between senders. A future host that publishes with a key of its own would be able to assert this identity too, and would need its own answer here before it got one.

**The system identity must not erase the bypass record.** The seam returns "no bypass used" for a system identity, which is right for an ordinary sync and wrong for the sync of a bypass-approved decision — it would write `IsApprovedByBypass = false` onto the entity while the `Approval` row records `true`, diverging the two records (§APR9.8) and erasing exactly the evidence §APR9.7.1 rule 3 exists to protect. The command carries the decided pair, and a system-identity sync writes what it is told rather than deriving.

#### ARC16.7.2 The verdict, and who may see it *(formerly §16.7.2)*

**`RetrieveApprovalVerdictAsync` answers "what may happen to this approval now, and what is stopping it".** It grants nothing and decides nothing. It writes exactly one thing, and only in one case: **a missing round is opened**, per §APR9.7.2 rule 1's read-triggered case. Nothing else — no evaluation, no transition, no command, no bypass. It carries:

- `IsBlocked` — true when **any** condition blocks. There is no partial block: a blocker is either resolved or waived by a bypass.
- `IsBypassAllowedForCurrentUser` — one bool folding the caller's role and `DoNotAllowBypassingSettings` into the only question a UI needs. Bypass waives the §APR8.5 conditions **wholesale** (§APR9.7.5), so there is no per-reason bypassability to express.
- `BlockReasons` — **every** current reason, in readable English, not just the first.

A UI enables approve on `IsBlocked == false`, and approve-with-bypass on `IsBlocked && IsBypassAllowedForCurrentUser`.

**The one write, and why it stops where it does.** Two ways leave an entity with no `Approval` at all — seed data written straight to the storage broker, which publishes no fact, and a fact that does not land — and every read here keys on the approval, so such an entity answers `NotFound` to a caller who can do nothing about it. The read therefore *opens* the round (§APR9.7.2) rather than reporting its absence.

It stops there, and the boundary is the ruling rather than an implementation detail. Re-running the whole added flow was the earlier shape, and it made this GET a write that could also **decide**: for an entity at `Submitted` under a policy with `RequireApprovals = false` and `AutoApproveIfAllApprovalRequirementsMet = true` — the shape the seed writes for the `(Association, IsPersonal = true)` tier, and one an administrator may set on any tier — the evaluation drove the fresh round to `Approved` and published the `-Approving` command under the **workflow** identity, so the entity's own publisher gate never saw the human who issued the read. The panel's reason for repairing is to have something to *report*, and reporting needs the row, not the outcome; the outcome belongs to the fact the repair is standing in for, and lands when that fact next does.

**And it opens no round for an entity nobody may see.** The repair is gated on the subject being **visible** — read back and not soft-deleted — not merely present. A takedown leaves the approval record alone (§APR9.7.6) and leaves the entity's own `ApprovalStatus` alone with it, so a removed row still reads `Submitted` and passes every status-shaped gate; asking whether its *author* can be read passes too, because the probe behind that question is a raw by-id read and this system applies no global query filter. A verdict for a taken-down entity is answered `NotFound`, for every caller including `Administrators`, exactly as §SEC14.5 rule 3 requires.

**The per-caller answer accounts for the `ReadOnly` veto**, and gets it for free: `CanApprove` *is* the decision verdict, and the veto is evaluated inside it (§SEC18.6 rule 2). Both halves matter. A blocked caller comes back `CanApprove = false` **and** `IsBypassAllowedForCurrentUser = false` — what a bypass waives are the §APR8.5 conditions, never the veto — and on a submitted round the refusal travels into `BlockReasons` as `BlockedByReadOnlyRole`, so the panel states the block rather than rendering "nothing is blocking this" beside a disabled control. A `Draft` approval is the exception and keeps its existing posture: the reason set is `BlockedDueToDraftStatus` **alone** (the early return above), because a draft has not entered a round and the action it needs is to submit — the veto still shows through `CanApprove`. The panel takes that answer verbatim rather than re-deriving it (§UI20.6.1); a surface that offered a decision the server then refuses would be reporting a bug that is not there. The message says the sanction applies and does **not** name which of the three scopes fired: that is the sanction's own detail, and no scope of it is appealable through this surface.

**Two changes this forces.** `ApprovalConditionsVerdict.BlockReason` is singular — "the first condition that failed" — and must become the full set, evaluating every condition rather than short-circuiting; a caller cannot be told "one more reviewer **and** two unresolved comments" otherwise. And `AccessDenialReason` gains `BlockedDueToDraftStatus`: a `Draft` approval is blocked for a reason a UI must state plainly, and `ApprovalNotOpenForReview` is accurate but too vague to render.

**The readable text is composed in Core, not in the decision function.** The client returns codes and the counts behind them; the orchestration maps them to messages. Putting user-facing English in a policy engine makes it own presentation and fixes one language into a shared package.

**§SEC14.5's non-leakage rule is resolved by the tier, not weakened.** The rule that a caller is told nothing about the policy protects an *ordinary* caller: it exists so that a refusal cannot be used to probe how many approvals a type requires. An approver is not that caller — they are the party the policy is addressed to, and telling them "two more approvals and one unresolved comment" is the operation working, not a leak. So the verdict is exposed to the moderation tier — `Administrators`, the `Publishers` tier, and the `Reviewers` tier — and to nobody else. Below that the posture is unchanged.

A reviewer is admitted deliberately, and it is the one admission worth arguing. They cannot decide (HR-3), so this tells them nothing they may act on directly. What it tells them is whether their own review completed the round — "1 of 3 required approvals recorded" — which is the question a reviewer actually has, and the counts behind it are already visible to them through the reviews and comments they can read individually. The only genuinely new fact is the *required* number, and withholding that from someone inside the round buys nothing: they are the party the policy is addressed to, not the ordinary caller §SEC14.5 protects it from.

The tier is matched by SUFFIX, not by a fixed list — `%EntityType%-Publishers` and the content-type-scoped `%EntityType%-%ContentType%-Publishers` of §SEC18.6 rule 5 both qualify. That is why the exposer carries a bare authorisation attribute rather than a role list: any list would lock out the content-type tier and every entity type added later, and the tier decision belongs in the service regardless.

#### ARC16.7.5 Resetting a decided round *(formerly §16.7.5)*

**`ResetApprovalAsync` puts a round that reached `Approved` or `Rejected` back to `Submitted`.** It exists because an outcome can be applied by accident — a misclick on a decision, an auto-approval under a policy nobody meant to leave permissive — and until it existed the only way back was an administrator editing storage.

**`Administrators` alone.** Deciding an open round is the `Publishers` tier's work; *un*deciding a closed one is the §APR8.6 HR-4 override, and the override has one holder — a state a publisher could edit out of would not be terminal at all. The panel hides the control from the publisher and review tiers, and the orchestration refuses them; §SEC14.6 rule 2 makes that pair deliberate.

**§APR8.6 regardless-rule 1 does not apply.** It bars anyone holding an active review from *deciding* the round, and a reset decides nothing — it takes a decision away. An administrator who reviewed may still put their own round back.

**Every active review is dismissed**, regardless of `RequireReapprovalOnChange` (§ARC12.5.3 business rule 12). The reviews produced the verdict being overruled, so re-opening on their strength would let the override be undone by the very reviews it overrode. The status is written first and the dismissal follows, so that the re-test each dismissal triggers sees the round as it now stands rather than the outcome being taken away. §APR8.8 regardless-rule 1 *requires* dismissal to work on a terminal round an administrator has moved back, so nothing refuses it either way — the order is a correctness choice, not a workaround.

**The bypass pair is cleared.** It records how *this* decision was reached (§APR9.7.5), and after a reset there is no decision: a round back with the reviewers must not still claim a waiver for an outcome it no longer holds.

**It unpublishes, and that is the operation working.** The entity follows through the ordinary approval command, and a transition landing anywhere but `Approved` forces `IsPublished = false` and clears `PublishDate` (§APR9.7.1). Resetting an approved item therefore takes it off the public site until it is approved again, and for a versioned entity its group has nothing published meanwhile — the same recoverable dark state §APR9.7.7 rule 7 already describes. A reset that left the accident publicly visible would recover nothing. Because a rejected item was never public, only the approved case is worth warning about, and the panel confirms on that case alone.

**The workflow does not react to its own writes.** The reset commands the entity to `Submitted`, the entity's transition answers under the workflow identity and publishes `-Submitted`, and the approval orchestration subscribes to that address. Re-entering the flow there would re-evaluate the round it just re-opened: under any tier with `RequireApprovals = false` and `AutoApproveIfAllApprovalRequirementsMet = true` — the shape the seed writes for `(Association, IsPersonal = true)` — the conditions are trivially met and the round is driven straight back to `Approved` in the same request, undoing the override. So the substrate reaction refuses any entity fact carrying the system identity. A human submission carries a human identity and is unaffected, and the envelope is verified before the flag is read, so no caller can assert it (§SEC14.6 rule 4).

#### ARC16.7.3 What is deliberately not built *(formerly §16.7.3)*

**Bypass cannot rescue an approval that was never submitted.** The decision function refuses a non-`Submitted` approval before the bypass branch is reached, and the foundation refuses `Draft` as a transition source. Both stay. The case this would have served — content whose author has departed — is already served in two steps that leave a full audit trail: a publisher or administrator may read a `Draft` (the review roles see non-public rows), amend it as moderation (§APR9.7.1 rule 2 admits them while the entity is not yet approved), submit it (`Submit<Entity>ByIdAsync` admits the owner *or* the publisher tier), and then approve it normally. Admitting bypass-on-draft instead would cost the invariant that **nothing reaches `Approved` without having passed through a review window**, which is too much to trade for a path that already exists.

**The entity is not READ through a request event, and an `IApprovalEntity` projection is not introduced to carry one.** The proposal was symmetry: the sync already publishes a command to the entity's address and observes the reply, so a fire-and-observe request could fetch the entity's approval state back the same way — either broadcast, with each approvable foundation answering where the type matches, or targeted, since the approval knows its `EntityType`. It is refused on three counts, and the first is the one that settles it.

1. **There is nothing left to fetch.** `IAccessBroker` already answers every question this orchestration asks of an entity — `RetrieveEntityApprovalStatusAsync` for the status a round opens at, `IsEntityVisibleAsync` for §SEC14.5 rule 3's takedown gate, `RetrieveEntityAuthorAsync` for the §SEC14.7 posture D submitter, and `FilterApprovalsToEntityAuthorAsync` for the collection half — and it answers them from storage in one read. The orchestration is already at **zero** entity services and has been since #287; a request event would not remove a dependency, it would add a second way of reading state that the gathering broker already owns.
2. **It would put an invariant behind a reply.** §SEC14.6 and the security-context rules require that an identity-filtered read never decide an invariant, and a request event's *absence of a reply* is indistinguishable from "no such entity", "the listener faulted" and "nothing is subscribed". The broker read fails loudly; a missing reply fails quietly and in the permissive direction. Nothing that gates a takedown may be built on that.
3. **A model is not a dependency.** `IApprovalEntity : IApproval { EntityType; Guid EntityId; }` would count for nothing against the Florance guidance either way, and the command payload already carries exactly that shape — the entity's id and the `IApproval` members, and nothing else (`PublishEntityApprovalCommandAsync`'s `ApplyDecision`). The projection exists; it just does not have a name.

**What the proposal was actually pointing at is real, and it is a different problem: the seven-arm fan-out.** `PublishEntityApprovalCommandAsync` carries one `case` per approvable type, and `IApprovalOrchestrationService.Substrate` carries twenty-one entity-typed handlers for the same reason — seven approvable types across `-Added`, `-Modified` and `-Submitted`, out of twenty-nine declarations in all. The seven `-Submitted` handlers are as typed as the other fourteen and would collapse with them. A single polymorphic approvable-entity command address would collapse both. **It is deferred, not rejected, and it has a stated precondition**: the envelope's HMAC is computed over a typed payload (§EVN10), each entity carries its own operation enum and its own `ProcessedEvents` receiver identity (§EVN18), and the tier split between the two Versioned types' processing addresses and the five Single-Row types' foundation addresses (§EVN18 rule 1) is load-bearing and would have to survive any collapse. That is a change to the event substrate, not to this orchestration, and it buys legibility rather than a dependency. It is recorded here so the next reader finds the answer instead of the question.

#### ARC16.7.4 Review requests and reviewer candidates *(formerly §16.7.4)*

> **These operations MOVED to `ApprovalReviewerOrchestrationService` (§ARC12.5.4, built by #521); every rule below is unchanged and travelled with them.** They were bound to `ApprovalOrchestrationService` because each spans the approval, its reviews and the request rows — true then and true now, but it is not the same *span* as the round's, and this subsection's own closing sentence says so. Read "the orchestration" below as the reviewer orchestration; the round's is named explicitly wherever the two differ. The one thing that changed is where the two retirements are triggered from — §ARC12.5.4 business rule 4, built by #522 — and that paragraph is marked in rule 6's and rule 8's text in §APR7.9.

Five operations serve §APR7.9 and §ARC16.7.2's naming, each spanning the approval, its reviews and the request rows:

- **`RequestApprovalReviewAsync(entityType, entityId, requestedUserId)`** — validates the §APR7.9 rules (target eligibility, owner exclusion, the `Submitted` window) and lands the request, or returns the existing state on a duplicate (rule 4). Rule 4 covers both duplicate shapes and neither errors: an active invitation comes back unchanged, and a target who has already answered yields nothing at all — rule 6 retired their invitation when they answered, and a fresh one could be neither retired nor withdrawn.
- **`RetrieveApprovalReviewRequestsAsync(entityType, entityId)`** — who has been asked and has not yet answered. Answered through the **caller-facing** foundation read rather than off the gathered scope: the scope's request rows are deliberately unfiltered (below), and answering a person from that view would hand them rows their own posture refuses. It also carries no display name and no username, which are the two things a picker renders.
- **`WithdrawApprovalReviewRequestAsync(entityType, entityId, requestedUserId, deletionReason)`** — soft-deletes a pending request; open to the requesting tier (§APR7.9 rule 5). **Keyed on the round and the person, not on the row.** The pair is already unique (`UX_ApprovalReviewRequests_ApprovalId_RequestedUserId`) and it is how a moderation surface thinks — the panel's withdraw hands its consumer an account id, never a request id. Keying on the row id required a round trip that no longer exists: the create returns `204`, and the id had appeared nowhere else, which left withdrawal unreachable from a browser. Resolving the row from the round also removes the not-found translation the id-keyed form needed, since a miss is now simply an invitation that is not outstanding. Distinct from the **two** retirements — rule 6's, when the invited person answers, and rule 8's, when the round closes on an outcome — which the orchestration reaches through `IApprovalReviewRequestWorkflowService` rather than performing itself: the system identity those run under holds no roles and so cannot pass this operation's gate. Neither is a caller-facing operation; both are reactions. Rule 6's fires off `ApprovalReview-Added` and rule 8's off `Approval-Modified` — the round's own outcome write — and §ARC12.5.4 business rule 4 has why both are subscriptions rather than calls, and why that is the one behavioural change in the split. An earlier wording had rule 8 running "off the three sites that write an outcome (§APR9.7.5, §APR9.7.7)"; all three still go through `ModifyApprovalAsync`, so the single subscription hears all three and no site enumerates them any more.
- **`RetrieveReviewerCandidatesAsync(entityType, entityId)`** — who is **in scope** for this round: users satisfying the review tier for the entity, minus the entity's owner and minus anyone a `ReadOnly` in this entity's scope covers (§SEC18.6 rule 2). Those two subtractions are the same kind of thing and the only two of their kind: both name people for whom an invitation is a click that always fails, and neither is a state a moderator can clear by asking again. People who have already answered, and people already invited, are deliberately included — the read answers "who belongs to this round", not "who is not yet dealt with". A moderation surface renders an answered person inert and an invited one under its own heading, so somebody searching for a name finds them and learns their state rather than finding nothing; a surface cannot show a person it was never sent. The owner and the blocked are the exclusions because rule 3 refuses an invitation aimed at either outright, so listing them would offer a click that always fails.

**The candidates read is a user-enumeration surface, and that decides its posture.** Nothing else below the Administrators-only admin endpoints lists users, so this read is exposed to exactly the tier that may request (§APR7.9 rule 2) and returns the minimum a picker needs — account id, display name and **username** — and nothing else. The username was added to that minimum deliberately: a display name is not unique, two colleagues called "John" are ordinary, and a moderator picking between them from the name alone is guessing. It costs nothing a caller could mine that the display name did not already cost — a username is how a person is addressed throughout the site, and §SEC18.3.1 forbids it from being an email address — while a role list, an email or an account state would, and none of those are here. Answering the request rows (`ApprovalReviewRequest` reads keyed by `ApprovalId`) follows the same tier: they are moderation coordination, not public state, so §SEC14.7 posture D applies to them as it does to the verdict. **That read is now built** — `RetrieveApprovalReviewRequestsAsync` above — and it gates twice on purpose: the orchestration admits only the requesting tier, and the foundation applies posture D again beneath it, which §SEC14.6 rule 2 makes a deliberate duplicate rather than a redundancy.

**A fifth operation, and it is a NAME RESOLVER rather than a fourth list.** `RetrieveReviewerDisplayNamesAsync(entityType, entityId)` answers what everybody a round names is called. It exists because `ApprovalReview` names its reviewer by `CreatedBy` — an account id — and nothing below the `Administrators`-only `/api/admin/users` could turn one into a name, so a `Publisher` who is not an administrator could render their own name and nobody else's. The candidates read does not close it: that read answers who is in scope for the round, so somebody who reviewed and then lost the role is absent from it entirely.

**One composition, not a projection per read.** The panel needs names for reviewers, for invited people and for candidates; a display-name projection on the review read would have answered the first and left the next to invent its own, and three lookups are three chances to render one person under two names. The candidates read carries its own names because eligibility and naming are one question there; this resolver answers the other two, and both compose through the SAME method — which is what actually stops one person rendering under two names, rather than the number of round trips.

**It IS keyed on a round, and that is what makes its posture hold.** The set it names is built from the approval's own rows and nothing else: the review rows — **dismissed and soft-deleted ones included**, because the panel renders those and their authors still need naming — the outstanding invitations, and the **authors of the round's live comments**. Comment authors are in the set because the review thread renders a name per row and an author is frequently neither a reviewer nor an invitee — the submitter answering a question is the ordinary case — so leaving them out would have left the one surface that most needs names rendering account guids. They arrive through `IApprovalCommentService`, so §SEC14.7 posture D's own visibility filter decides which authors a given caller can name, which is the right scope: naming somebody whose words are hidden from the reader would say more than the thread does. Withdrawn comments are excluded, since nothing renders them.

**It carries a `UserName` beside the composed `DisplayName`.** Two accounts can share a display name — the reason every gate in this system compares account ids and never names — and on a comment thread that ambiguity is the difference between reading the submitter's answer and reading somebody else's, so a surface that must disambiguate has the username to do it with instead of an account guid. It goes no wider than the display name beside it: this route is round-keyed and requesting-tier only, and §SEC18.3.1 rules a username may not contain an `@`, so it cannot become a contact detail leaking through a moderation surface. **The review tier is deliberately NOT in it.** Reading the tier only ever admitted anybody while the caller supplied ids to intersect against; with the round deciding the set, its one remaining effect would be to add every global `Publisher`, `Reviewer` and `Administrator` to the answer — which is `ReviewerCandidates`, a read the same panel already makes and which already carries display names. Nor did the bug force the unscoped form — a departed reviewer's id is still stamped on the review row, so a resolver scoped to this approval resolves them, which is exactly what the candidates read cannot do. What round-scoping buys is that the tier gate composes with an entity gate instead of standing alone: a `Tag-Reviewer` can name the people a tag round actually involved, rather than any account id in `AspNetUsers` and no longer the whole moderator directory either. Establishing the round costs the gather the invitation operations already pay for — the approval behind the entity key, then `ApprovalReviewerScope`'s own read of that approval, its entity, its review rows and its outstanding invitations — and naming the result on top of it is exactly one identity query, or none at all when the round names nobody.

**The caller names no ids, and that is a security property rather than a convenience.** With the round deciding the set there is nothing to probe with, so there is no batch to bound and no caller-supplied array to bind — the entity key rides the route like every other operation on the controller, and the batch ceiling the unscoped form carried is retired along with the query string it bounded. `ApprovalReviewerScope` therefore reports its reviewers TWICE, filtered and unfiltered: invitability (§APR7.9 rules 4 and 5) turns on a review that still STANDS, so a dismissed or withdrawn one releases the person, while who the round INVOLVED is released by nothing. Collapsing the two would cost the panel the name of every dismissed reviewer it renders.

**It applies no role filter and no disabled filter over the round's own people, deliberately.** Their ids came off rows the approval already stores, so the account is part of the record whatever has happened to it since; filtering by the review tier is exactly the mistake that left a departed reviewer with a blank name. It is a user-enumeration surface all the same, so the posture above governs it rather than being re-derived: the requesting tier and nobody else, an account id, a display name and a username, and nothing else. It carries the username for the same reason the candidates read does and by the same composition — a panel that labelled a candidate with their username and a cast reviewer without one would be showing one person two ways depending on which list they landed in. There is exactly ONE identity read and it is that one: the round hands over a set of ids and the resolution read hands back the accounts behind them, so nobody is looked up twice and no second read is conditional on anything. The ids are parsed and trimmed inside that read, and an empty set fails closed there rather than becoming a directory dump — so a round involving nobody costs no identity query at all. Ids naming no account come back absent rather than as an error, so one deleted account cannot blank a whole panel. The id echoed back is read off the resolved row, so it is always the canonical form, and a caller holding ids in another spelling normalises its own side to join on the answer.

**`ApprovalReviewRequest.RequestedUserDisplayName` stays where it is.** Denormalising a name onto a row at write time leaves every historical row asserting a name its owner may since have changed, so the trade is not repeated on `ApprovalReview` and no second denormalised name is added. The existing one is kept rather than removed because it is the write-time record of who was asked and keeps a request row self-describing when the identity store is unavailable; a surface wanting the current name resolves it like any other id.

**Where the identity half comes from.** Both the candidates read and rule 3's tier check ask *who holds a role*, and role membership lives in the ASP.NET Identity store, not in Core's database — `ISecurityClient.Users` cannot answer it, because that client reads a `ClaimsPrincipal` and so only ever describes the current caller. Core therefore reads the security database directly through the read-only `IdentityCoreStorageBroker` and its `IdentityUserService` (§ARC12.7.1). Taking the host's word for who is eligible was rejected: that is the caller-supplied-identity mistake `ApprovalReview.ReviewerId` was deleted for, where free text let one reviewer meet a three-approval threshold alone.

**The §SEC18.6 composition stays in the orchestration.** `ApprovalReviewerScope` reports the approval's *role subjects*; the orchestration turns them into tier role NAMES (the global trio, the `%EntityType%-` pair per subject, and the `%EntityType%-%ContentType%-` pair where a subject carries one), and the identity service is handed finished names and only reports membership. One home for the convention, and the association case falls out for free — an association names both endpoints, so a publisher trusted with either end is invitable for the pairing.

**The veto's names are composed from the same subjects, read the other way round** — the global `ReadOnly`, the `%EntityType%-ReadOnly` per subject, and the `%EntityType%-%ContentType%-ReadOnly` where a subject carries a content type. It is a second membership read rather than a filter over the first, because role membership lives in the identity store and an `IdentityUser` does not carry the roles it was matched on. The list always includes the global `ReadOnly`, so it is never empty and never trips `IdentityUserService`'s fail-closed guard — which would otherwise answer "nobody is blocked" for a composition bug and quietly restore every blocked person to the picker. A subject whose scope could not be established widens that list rather than narrowing it, exactly as `IAccessClient` widens the refusal: the named-but-undecidable subject contributes every block name of its own entity type, and an unnameable one contributes every scoped block name there is (§SEC18.6 rule 2).

**One gather, three rules.** `IAccessBroker.RetrieveApprovalReviewerScopeByEntityAsync` — the by-id form's entity-keyed twin, and what the reviewer orchestration actually calls (§ARC12.5.4 business rule 2) — returns the round's status (rule 7), the entity's owner (rule 3), the role subjects, the active reviewers, the recorded ones and the active requests in a single read. The last two are gathered without the caller-facing visibility filter, for two different reasons. The requests are unfiltered for the reason `FindDismissableApprovalReviewIdsAsync` already is: deciding invitability from a filtered view would tell a moderator somebody is invitable and then collide with the uniqueness index. The recorded reviewers are unfiltered for the resolver's sake and for nothing to do with invitability — a panel renders a dismissed review, so the round has to be able to name the person who wrote it.

The request rows change no §APR8.5 outcome, so nothing here touches the evaluation, the verdict or the decision paths — the whole feature composes beside §ARC16.7.1–§ARC16.7.2 rather than into them. **That sentence is what licenses §ARC12.5.4**: a feature that composes beside another is a service beside it, and the only reason it was not one from the start is that it was built while §ARC16.7.1's dependency count was still believed to read three.

### ARC16.8 AssociationOrchestrationService

`IAssociationOrchestrationService.RetrieveContentItemReactionSummariesAsync` answers, for a set of content items, which reactions each has actually been given, how many of each, and which one the calling reader holds.

```csharp
ValueTask<IReadOnlyList<ContentItemReactionSummary>> RetrieveContentItemReactionSummariesAsync(
    IReadOnlyList<Guid> contentItemIds,
    CancellationToken cancellationToken = default);
```

**Why the read sits on the orchestration.** §SEC14.3's **Layer** paragraph is the rule: rules 3, 4 and 6 span more than one entity, so "the composite rule belongs to an orchestration or aggregation service that can resolve both endpoints. A public read surface must therefore bind to that service, not to the foundation's collection read." The foundation keeps the self-only filter over rules 1, 2 and 5, and this read composes the rest above it. §ARC12.1 rule 2 is the second input and agrees: the flow spans `Association`, `Reaction`, `ContentItem` and `ContentItemSetting`, which is an orchestration's definition. **§EVN18 rule 3 is deliberately not the citation** — `Association` is a Single-Row type, for which that rule says a foundation binding is correct, so it argues the other way. The argument that reaches upward here is §SEC14.3's alone.

The read adds **no service dependency**: `IAssociationService`, `IReactionService` and `IContentItemService` are already held, and `IAccessBroker` is a broker, which §ARC12.5 puts outside the Florance count — "a broker obliges none: it holds no logic to validate, so it raises no family of its own". No Florance deviation is sought and none is granted — and none is implied: the service's existing seven entity services already break the dependency-count guidance, which §ARC12.5 entry 1's provisional note records and which the deviation register explicitly refuses to accept as a precedent. This read neither worsens that count nor regularises it.

**Four sections carry this service's rules, and none restates another.**

- **§ARC16.2.1** — the §DOM6.10 facet gate on the **write** path, and the rule that exposers do not interact directly with the association foundation service. Recorded there, not here.
- **§ARC16.8** — this section: the **reaction summary read**.
- **§ARC16.8.1** — the caller-facing **write surface**: the upsert that adds, replaces or revives a reader's reaction, and the pair-keyed removal with its signature, its route and its response codes. Recorded there, not here.
- **§ARC16.9** — the **general endpoint-anchored read**, the service's other read: what anchors and bounds it, why its far-end type is required, and why it carries no `[EnableQuery]`. It consumes this section's composite evaluator and writes none of its own. Recorded there, not here.

Every neighbour is pointed at rather than copied; a rule stated twice is a rule that will eventually disagree with itself. **The three do not land in one direction**: §ARC16.2.1 nests under §ARC16.2, well above this section, so **the pointer to it is a backward reference**; §ARC16.8.1's place is under this section and §ARC16.9's is beside it, so **both of those pointers are forward ones**. Reference by section number rather than by position is this document's house style for all three, which is why no pointer names a direction.

#### The projection

`ContentItemReactionSummary`, one per answerable id:

| Field | Type | Origin |
| --- | --- | --- |
| `ContentItemId` | `Guid` | **Carried** — **the id the caller supplied, echoed back verbatim**, so the client keys its cards on what it already holds. It is deliberately **not** `Association.EntityAEffectiveId`: the caller supplies a version's id, the orchestration resolves it to that version group's effective id to run the count, and it maps the answer back onto the id it was given. A card rendering v2 holds v2's id and nothing else, so echoing the group id would leave every amended item unable to match its own summary. |
| `Reactions` | `IReadOnlyList<ContentItemReactionCount>` | **Aggregate** — over the counted set defined below, grouped by `Association.EntityBKeyId`. |
| `ViewerReactionId` | `Guid?` | **Carried** — `Association.EntityBKeyId` of the caller's own live row for this host; `null` where there is none, and for an anonymous caller. |
| `ViewerReactionName` | `string?` | **Carried** — `Reaction.Name` (§DOM5.2) of the reaction that id names; `null` on the same terms. |

`ContentItemReactionCount`:

| Field | Type | Origin |
| --- | --- | --- |
| `ReactionId` | `Guid` | **Carried** — `Association.EntityBKeyId`, which for a Single-Row far end equals `Reaction.Id`. |
| `Name` | `string` | **Carried** — `Reaction.Name` (§DOM5.2). |
| `UnicodeEmoji` | `string` | **Carried** — `Reaction.UnicodeEmoji` (§DOM5.2). |
| `Count` | `int` | **Aggregate** — over the counted set below, which is the `Association` rows satisfying all six §SEC14.3 rules for this host, **grouped by `(EntityAEffectiveId, EntityBKeyId)`**: one result row per reaction per host. |

**The wire carries the entity's names, not the view's** — `Name` and `UnicodeEmoji` per §DOM5.2, never `label` or `glyph`. Renaming into the view's vocabulary is the view service's job.

**The total is a client sum and is not a carried field.** The sum of the returned counts *is* the total by construction, since every counted row belongs to exactly one reaction, so carrying one would be a second source of truth for a derived number — and two independently computed numbers eventually disagree. The client already sums, at `contentItemDefaultPanel.tsx:136-137`.

**A reaction with a zero count does not appear.** The read counts rows; where no row exists there is nothing to count and the entry is simply absent. **`contentItemDefaultPanel.tsx:389` and `:399` therefore need no zero filter and are correct as written** — they render every row they are handed, and no zero is ever handed to them. An item nobody has reacted to returns `Reactions` empty and renders no cluster.

One case is ruled explicitly rather than left to be discovered: **a reaction given before an item was narrowed to love-only still counts and still renders.** The narrowing governs the write — §ARC12.3.1 association rule 1, where `LimitReactionsToLoveOnly` narrows what may be *created* — and not history; unwinding history on a settings change would silently delete readers' reactions.

#### Which rows are counted

All six §SEC14.3 rules are inside the answer. Where each is evaluated:

| §SEC14.3 rule | Where it is evaluated |
| --- | --- |
| 1 — the association is not soft deleted | The query-shaping function `AssociationService` authors for its narrow read (§ARC12.2.1 rule 3), awaited by the storage client. |
| 2 — `Approved` if approval is required | The same shaping function, as `ApprovalStatus == Approved`. |
| 3 and 4, far end (`Reaction`) | The orchestration. It resolves the canonically visible `Reaction` vocabulary through `IReactionService`, then closes over those ids and passes them into the foundation's grouped read as an `IN (...)` set — so the foundation's read still touches only its own table. |
| 3 and 4, near end (`ContentItem`) | The orchestration, at **group** level: the summary answers for a host whose version group has at least one canonically visible version under §SEC14.1. Group level and not row level, because these associations are written `AllVersions` and belong to the group (§DOM4.3). |
| 5 — `Association.PublishDate` null or passed | The same shaping function. |
| 6 — the effective settings permit it (§DOM6.10) | The orchestration, and **inside the count**. Resolved for the `ContentItem` end **only**: §DOM6.10 rule 4 makes `Reaction` a far-end-only type with no settings entity, and rule 3 says an endpoint type with no settings entity imposes no restriction. Where the host's winning setting carries `ShowReactions = false`, that host's summary is returned with `Reactions` empty and the count is not run. |

**Rule 6 is inside the count deliberately, and the alternative is named.** Leaving it to the client would be cheaper — the card already holds its winning setting and gates on it — but it would mean a public route answering counts §SEC14.3 says are not visible, and would make this design's one visibility composite true of every consumer except the one that matters. The client's own gate stays, as the component's contract with whoever supplies it a summary rather than as a second copy of this rule.

**The two resolution rules sit on two different axes, and collapsing them gets the override backwards.**

| Axis | Rule | Where |
| --- | --- | --- |
| **Which tier governs one host** — the type default versus that item's own override | **Narrowest scope wins: full precedence.** A rule keyed on `(ContentType, ContentItemId)` has a smaller scope than one keyed on `ContentType` alone, and the smaller scope wins outright. The tiers are not merged, and the override wins whether it tightens **or loosens**. | §DOM6.4, §DOM6.9 rule 4 |
| **Which hosts must agree** — endpoint A's switch versus endpoint B's | **Restrictive union.** Where **both** endpoints resolve a switch, the association is permitted only when both allow it: "Denials union restrictively." | §DOM6.10 rule 2 |

The first **selects a row** within one host; the second **combines two already-selected answers** across two hosts. One is tier selection, the other is agreement between hosts, and they cannot be collapsed into a single sentence.

#### The predicate, and what pins it

The predicate pins the host on endpoint **A** and the reaction on endpoint **B**, over the effective id:

```
EntityAType = ContentItem  AND  EntityAEffectiveId IN (...)  AND  EntityBType = Reaction
```

`EntityAEffectiveId` and not `EntityAKeyId`: `ContentItem` is versioned, so the key id names **one version**, and counting on it would reset every count to zero the moment an approved item is amended and forks. §DOM4.6 rule 1 names the effective id "the read predicate" in as many words, and for a non-versioned host the two columns hold the same value, so naming it costs nothing.

**What the predicate rests on is the ordinal fact `"ContentItem" < "Reaction"`, and what pins that fact is a named database constraint rather than a service convention.** The pin is **`CK_Association_CanonicalOrder`**, declared at `Glory2Him.Core/Brokers/Storages/Sql/StorageBroker.Association.Configurations.cs:60-68`, applied by the merged migration `20260806225353_AddAssociationPairUniquenessAndCanonicalOrder` and live in the model snapshot. It compares the two stored type **names** under **`Latin1_General_BIN2`** — a **binary** collation, so the comparison is **ordinal**: the same order `CompareEndpoints` produces through `string.CompareOrdinal` (§DOM4.4 rule 1), and deliberately not the database's own case-insensitive default. Because `'ContentItem'` sorts before `'Reaction'`, **a `ContentItem` ↔ `Reaction` row carrying the reaction on endpoint A cannot be written — by any layer, on any entry path, including a write made straight at the storage broker.** That is what makes the host reliably endpoint A and grouping on `EntityBKeyId` safe. §DOM4.6 rule 2 already names the constraint as the pair index's partner.

**§DOM4.4's prose alone would not pin it.** §DOM4.4 rule 4 places `NormalizeEndpointOrder` inside `DoAddAssociationAsync` — a service-level normalisation binding **one method on one flow** — and that rule itself records why that is not enough: `Association-Adding` is a public event address whose substrate handler enters `DoAdd` directly, so anything layered above it is bypassed. §DOM4.4 is the right rule for *how* a row is canonicalised and the wrong one for *what guarantees* it. A read's predicate may rest on a database guarantee; it may not rest on a convention a caller can route around.

**The claim is scoped to this pair, and is not "a reaction is always endpoint B".** The constraint pins the *canonical order*, not `Reaction`'s column: `'Reaction'` sorts **before** `'Tag'`, so a `Reaction` ↔ `Tag` row would put the reaction on A. What is pinned is the `ContentItem` ↔ `Reaction` pair.

**Standing condition, in §DOM4.9 rule 6's form.** The ordinal order of the `EntityType` names is `Association`, `Attachment`, `BibleReference`, `Comment`, `ContentItem`, `Link`, `Reaction`, `Tag`. A future far-end type for a `ContentItem` host whose name sorts **before** `ContentItem` lands on A and puts the host on B; equally, a future `EntityType` that both sorts after `"Reaction"` and is itself a host for reactions inverts this predicate. Either would make this read's grouping on `EntityBKeyId` read the wrong column, so whoever adds such a member revisits this section. A symmetric predicate — `(A = host AND B = Reaction) OR (B = host AND A = Reaction)` — is refused: it forfeits the index seek §DOM4.6 rule 1 exists to provide, on the query that runs on every page render, to guard against a pair that cannot exist.

**The residual hazard is a rename, and it belongs to #201.** Renaming an `EntityType` member changes the ordinal order, and SQL Server does not re-validate a `CHECK` constraint against rows already written — so a rename breaks loudly for **new** writes only, and splits old and new rows across the two columns. **The constraint does not protect rows already stored**, and nothing here claims it does.

**Scope fields.** `EntityAScope = AllVersions` (the host is `ContentItem`, versioned) and `EntityBScope = ThisVersionOnly` (`Reaction` is Single-Row), both derived and never caller-supplied (§DOM4.5 rule 1). `UserId` is a row-level field (§DOM4.2), orthogonal to `Scope`; no personal-association carve-out on `Scope` exists or is created. An approved content item edited into v2 therefore keeps its reactions: they are keyed on the group, so the reader who reacted to v1 still holds it, v2's card shows it, and it is counted once and not twice.

**What a reader sees when their own reaction is not yet `Approved`.** Under the seeded `(Association, IsPersonal = true)` tier it always is, because that tier closes the round on submission. Under a **tightened** tier it is not, and the rule is: **the caller's own reaction is identified in `ViewerReactionId` and `ViewerReactionName` regardless of its approval status, while the counts include only `Approved` rows.** A reader whose reaction is awaiting review therefore sees their glyph **pressed and the number unmoved**. That is the honest rendering of a moderated reaction: showing the click as unrecorded would invite a second click, and counting an unreviewed row would defeat the tier. It is the one caller-dependent member of the response, it names only the caller's own row, and it widens nothing.

> **Where the `GROUP BY` runs, and which §ARC12.2.1 shape carries it.** No fourth terminal shape is earned and nothing is grouped in memory. `AssociationService` authors the whole shaped query — the predicate above, then a grouping on `(EntityAEffectiveId, EntityBKeyId)` projected to the pair plus its count — as the §ARC12.2.1 rule 3 query-shaping function, in `System.Linq` only; the storage broker hands it down and the storage client awaits rule 3's **first** shape, the matching rows optionally projected, with the caller's `CancellationToken`. The aggregate therefore lives in the **projection**, not in the terminal operator: `Count()` inside a `Select` is part of the expression tree EF translates, never an operator Core runs over a materialised set, so §ARC12.2.1 rule 7 is not engaged and rule 4 holds unchanged. Counting above the read is refused by that same rule 4 — it would materialise every reaction row of up to twenty-five hosts on an `[AllowAnonymous]` route that renders on every page, and the per-page cost recorded below under *What a rendered page costs* assumes the aggregate executes in SQL. The unit test applies the function to an in-memory set, where the grouping and the counts evaluate identically (rule 5); that the grouped projection **translates** is exactly rule 6's case and is proven against the real catalogue in the integration suite. What comes back is a narrow native row — host effective id, reaction key id, count — and `Name` and `UnicodeEmoji` are joined above it from the vocabulary the orchestration already holds.

#### Anonymity, and what a signed-out caller receives

The route is **`[AllowAnonymous]`**, and **the counted set is caller-independent**. The same content item returns the identical counts to an anonymous caller, to its own contributor, to a narrow reviewer, to a publisher and to an administrator. A count that changes when you sign in is a visible oddity and this design refuses it.

**The caller-independence is not attributed to §SEC14.3, which carries no caller term across any of its six rules.** The sections that do carry one are §SEC14.5 rule 4 and §SEC14.7's owner and reviewer widening, and neither reaches these counts: §SEC14.7 widens what a caller may see **of an entity row**, and this read returns no entity row, only an aggregate over rows no caller reads individually through it. The consequence, because it is easy to get wrong: the orchestration resolves its hosts through a **caller-independent** canonically-visible by-ids read — the §SEC14.1 predicate over a closed-over id list — and never through a by-id read whose posture widens for owners and reviewers.

**What a signed-out caller receives**, as a concrete payload: one `ContentItemReactionSummary` per answerable id, carrying `ContentItemId`, `Reactions` with the full counts, and `ViewerReactionId` and `ViewerReactionName` both **`null`**. That payload is deliberately **indistinguishable from "signed in and has not reacted"**: the client already knows whether it is signed in, and a second copy of the session state on every summary would be a second source of truth for it. The distinction that *is* needed — an unset viewer member versus an empty `Reactions` list — is a different pair of cases and is preserved.

**An id the read cannot answer for is ABSENT from the response** — a group with no canonically visible version, or an id naming nothing. Not an empty summary, and not a `404`. §SEC14.5 rule 4: collection reads apply the same posture by **filtering**, so rows the caller may not see silently drop out of the set rather than producing an error. Because the counted set is caller-independent, "absent" means the item is not canonically visible to **anybody**, so absence reveals only what §SEC14.1 already makes public.

#### The reader's own reaction, and what a withdrawal is keyed on

**The reader's own reaction is identified pair-keyed, not row-keyed, so the response carries no association id.** It carries `ViewerReactionId` and `ViewerReactionName`, and the caller already holds the other half — the card's content item. The justification is **§DOM4.6 rule 2**, which makes the reader's one personal row for a host unique on its endpoints together with `UserId`: the pair already *is* the row's key, so an id would be a second handle on something already addressable, and one that could be probed with. That rule is cited **by section number and never by index name**, because the index behind it is replaced within this same feature while the section is rewritten in place.

**A withdrawal is therefore keyed on (content item, reaction, caller).** The caller supplies no user id at all — it is derived from the inbound envelope — so a pair-keyed withdrawal cannot address another reader's row.

**A withdrawal publishes `Association-Removed`** (§EVN2), through the foundation's soft delete, and **mints no orchestration address**. `AssociationOrchestrationService` holds no `IEventBroker` and owns no event address, and this read does not give it one.

**The member that performs the withdrawal — its signature, its route and its response codes — is §ARC16.8.1's**, not this section's. It is a write, and §ARC16.8.1 is the write surface.

#### The set, its bounds, and what a page costs

The read **takes a set**, bounded at **1 to 25 ids**. An empty set and a set over 25 are both a `400`, raised by the layer's validation before any storage call. The bound is a **server-side guard on a public read against a hand-formed request**, not a client-side budget: 25 GUIDs in a query string is roughly 1.4 KB, comfortably inside IIS's default `maxQueryString`, so the route needs no host configuration change. Duplicate ids are de-duplicated rather than refused, and the response carries one entry per distinct answerable id.

**The set is keyed on the ids of the page just delivered, never on the accumulated list.** Every list surface renders the accumulated list — `home.tsx:78`, `posts.tsx:67`, `myPosts.tsx:74` and `admin/contentItemModerationPage.tsx:73`, each `(data?.pages ?? []).flatMap((page) => page.items)` — so a read keyed on what is *rendered* asks for 8 ids, then 16, then 24, then 32: a `400` on the fourth press of *load more*, and quadratic in presses before that. Keyed on the delivered page it is one request per page at a constant 8, so **every rendered card's cluster shows at any scroll depth** and the bound never binds. Summaries accumulate in the client the way the cards do, cached per content item id. **A caller ever holding more than 25 ids in one ask chunks at 25** — `contentItemService.ts:69` lets a caller override `pageSize`, so the chunking must exist even though today's four surfaces never reach 25.

Capping what the client asks for instead is refused: a card whose engagement row is absent for a reason no setting explains is an invisible second switch. Raising the ceiling is refused too — it makes the re-request worse and leaves the same `400` waiting at a larger number.

**What a rendered page costs.** At **N = 8**, the card page size (`contentItemService.ts:30`), one rendered page costs **one HTTP request and four database round trips** for an anonymous caller:

1. `IReactionService` — the canonically visible `Reaction` vocabulary.
2. `IContentItemService` — the canonically visible subset of the eight ids, resolved to group ids.
3. `IAccessBroker` — the winning `ContentItemSetting` per host, for §SEC14.3 rule 6.
4. `IAssociationService` — the grouped counts, `GROUP BY EntityAEffectiveId, EntityBKeyId`.

An **authenticated** caller costs a **fifth** round trip: the caller's own rows across the eight hosts, read regardless of approval status, which is what makes the pressed-but-uncounted rule above possible. So the cost is one request and four round trips anonymous, one request and five authenticated, per rendered page of eight cards. A per-item read at the same N would be 8 HTTP requests and 32 or 40 round trips.

**This read is NOT folded into the content item feed read (#591), and #591's criterion 5 is therefore not amended.** Folding the viewer's own reaction in would contradict that criterion directly — it forbids the feed read resolving a `SecurityContext`, a user id or a role at all; folding only the counts would leave the viewer half needing a second route anyway — the whole cost of the fold with none of the saving. The summary is its own route, and §ARC17.2's feed table gains nothing.

#### The route

Served literal: **`GET api/Associations/ReactionSummaries`**, on `AssociationsController`.

- Binding: `[FromQuery] Guid[] contentItemIds`, on `[HttpGet("ReactionSummaries")]`.
- **`[AllowAnonymous]`**, consistent with the caller-independent counts above.
- **No `[EnableQuery]`, and the query surface is closed rather than allow-listed.** A route with no OData surface cannot take an ordinal `$filter` and cannot have its generated SQL un-capped. A caller supplying `$filter`, `$orderby`, `$top`, `$skip`, `$select`, `$expand`, `$search`, `$compute`, `$apply`, `$format`, `$skiptoken` or `$deltatoken` gets **the same answer as the same request without them** — the parameter is off-surface and ignored.
- The set bound is **1 to 25 ids**, as above.

The §ARC17.4 row for this endpoint carries its **shape** rather than this literal, because that is the convention every row in the §ARC17 tables follows; the served literal lives here, beside the binding it belongs to.

#### Where each shared rule lives, and what must not re-derive it

Recorded so a later implementer meets it rather than rediscovering it.

| The rule | Its single home |
| --- | --- |
| §DOM6.10 effective-setting precedence | **Designed, not built — §ARC12.2.1 is itself ruled and unbuilt, and `IAccessBroker` carries no content-item-setting arm today.** The **query-shaping function `AccessBroker` authors for its gather** — §ARC12.2.1 rule 3, which names `AccessBroker` as a condition author for its gathers and leaves §APR8.6.1's placement unreopened. It is **not** a storage-broker predicate: §ARC12.2.1 rule 1 makes *"which row wins when two match"* a decision the broker may not compose, and §DOM6.4 precedence — a **selection, not a merge** — is that decision. The function shapes one set-keyed query over all the `(ContentType, ContentItemId)` keys asked for, excluding soft-deleted rows (§DOM6.6); the storage broker hands it straight down as a pass-through and the storage client applies it and awaits the terminal operator with the caller's `CancellationToken`, so the selection still runs in SQL in **one** round trip and no unresolved list is picked over above it (§ARC12.2.1 rules 3-4). **§ARC12.2.1 rule 5 binds it: the unit test executes the condition.** `AccessBroker`'s gathers are unit-tested today — fifteen `AccessBrokerTests.*.Logic.cs` files — and this one is tested the same way, over an in-memory set seeded with a matching row and, per term, a row that misses on that term alone, so dropping or inverting a tier term fails a test. Integration is **additional** and not a substitute (rule 6): only the real catalogue proves that the per-key tier selection translates. **The one-round-trip claim made earlier in this cell, and the per-page cost recorded earlier under *What a rendered page costs*, are design intent rather than anything rule 6 proves** — rule 6 names translation, collation and an index's claim, and **establishes no round-trip count**. The arm returns the winning **row** per key and decides nothing further. This read's §SEC14.3 rule 6 term reaches it through that one arm. §ARC16.3 responsibility 3 and §ARC12.5.2 responsibility 5 answer the same question for their own callers, **and this issue does not settle where either reaches it** — §ARC16.3's is the foundation service for the entity, which §ARC12.2.1 rule 3 admits as a condition author for its own rows **generally — which is not licence to author THIS condition, whose single home is the shaping function named earlier in this cell**; §ARC12.5.2's is an orchestration, which that rule's *Layer placement* admits as neither, so its route to the rule is an open question and not a licence. **What is settled is that the precedence rule has ONE implementation and none of the three authors a second**, which is the whole point of this row. Separately, and not corrected by this issue: several passages in this file still call that resolution a **merge**, which §DOM6.9 rule 4 contradicts (*the tiers are not merged*) since §DOM6.4 precedence selects a row rather than combining two. Every one of them is corrected together, wherever that wording is next revisited. Should §ARC12.5 entry 2's `ContentItemSettingsProcessingService` ever be earned, it becomes another caller of this one function, never a second copy of the rule. |
| §SEC14.3 rules 1, 2 and 5 | The query-shaping function `AssociationService` authors for its narrow read — **authored in the service and never in the storage broker** (§ARC12.2.1 rule 1), and awaited by the storage client. The orchestration must **not** re-test them in memory after the read returns. |
| §SEC14.3 rules 3, 4 and 6 | One private composite evaluator on `AssociationOrchestrationService` — **#310's to write and #616's to consume**, in whichever order they land. **Three reads consume it and none writes its own** — §ARC16.9's `RetrieveAssociationsByEndpointAsync`, the `RetrieveAssociationByIdAsync` beside it, and `RetrieveContentItemReactionSummariesAsync` — and §ARC12.5 entry 1 cites this row rather than naming a second home. **The evaluator carries NO caller term**, because none of the three rules has one. Posture A′ rule 2's widening for a review tier and rule 5's public-plus-own predicate belong to §ARC16.9's two reads alone — the anchored one and the by-id one — and compose **beside** the evaluator rather than inside it, which is what leaves this read's counts caller-independent while those two are not. |
| "Which pair do these endpoints denote" | `ResolveEndpointAsync` together with §DOM4.4's `NormalizeEndpointOrder`, reused by the withdrawal rather than re-derived — which is what stops the add and the withdrawal drifting apart. |
| `UserId` from the envelope, and `ApprovalStatus.Submitted` on create | **#188**, in the orchestration's endpoint step, once. **#618 criterion 5 and #620 criterion 1 reference the rule; neither implements it**, and `UserId` is never read from the request. |
| The reaction total | `contentItemDefaultPanel.tsx:136-137`, client-side, once. **Nothing server-side computes one**, not even for logging. |
| Which reaction this viewer holds | This read's `ViewerReactionName`. `useContentItemEngagement.ts`'s per-visit `viewerReactions` record is an **optimistic overlay discarded on the next read**, never a parallel store — and the same rule governs optimistic counts, which mutate the element's own summary and are replaced wholesale by the next read. One tally, not a server tally and a client tally being kept in step. |

**One second implementation is permitted, and two conditions bind it.** The client's `resolveContentItemSetting.ts` stays: it resolves the winning row for the **whole card** — title, author, every facet — from a collection the page has already fetched, which no server route replaces. It **must cite §DOM6.4**, and it must **never be copied a second time on the client**; every surface resolves through that one function. Converge only if a per-item winning-setting route is ever shipped — a condition recorded, not an action taken.

Two reads of the reaction vocabulary are **both** needed and are not each other's copy: the client's approved-reactions read feeds the **picker**, the options nobody has chosen yet, while `IReactionService` inside this read names the **counts**. The summary carries `Name` and `UnicodeEmoji` per row so the client never joins the two.

### ARC16.9 The endpoint-anchored association read

`IAssociationOrchestrationService.RetrieveAssociationsByEndpointAsync` answers, for a bounded set of entities of one type, which associations of one far-end type each of them holds that the caller may see.

```csharp
ValueTask<IReadOnlyList<Association>> RetrieveAssociationsByEndpointAsync(
    EntityType anchorEntityType,
    IReadOnlyList<Guid> anchorEntityIds,
    EntityType relatedEntityType,
    CancellationToken cancellationToken = default);
```

**RULE — `RetrieveAllAssociationsAsync` is NOT lifted onto the orchestration, and this member replaces it on the public surface.** The unfiltered collection read stays where §ARC12.2.1 rule 7 puts it — the foundation's, returning a live queryable — and nothing exposes it. §SEC14.3's *Layer* paragraph is why: *"A public read surface must therefore bind to that service, not to the foundation's collection read."*

**Why the read is anchored and bounded, which is forced rather than preferred.** Three rules already recorded meet, and between them they leave one shape:

1. **An orchestration is not a query-condition author.** §ARC12.2.1 rule 3 names the condition's author as the foundation service, or `AccessBroker` for its gathers, and its *Layer placement* line says the same in one clause; §ARC16.8's shared-rule row already reads that as admitting an orchestration as **neither**. So this service cannot compose the composite into the expression tree, whatever its shape.
2. **The composite cannot be the foundation's either.** §SEC14.3's *Layer* paragraph: rules 3, 4 and 6 span more than one entity and "cannot be evaluated by the association's foundation service, whose reads touch only its own table".
3. **So the composite runs ABOVE the storage read, over what came back.** That is tolerable only over a set whose size the request itself fixes. An unbounded collection read narrowed in memory is the shape §ARC12.2.1 rule 4 exists to refuse, and no paging above it helps: `$top` applied after an in-memory narrowing pages a set the database already handed over in full.

**The bound is §ARC16.8's, cited rather than restated: 1 to 25 anchor ids, an empty set and a set over 25 both a `400` raised before any storage call, duplicates de-duplicated rather than refused.** The same number deliberately, so a client holding a page of ids chunks once for **the two reads that take a set** — this one and §ARC16.8's — and no surface has two budgets to keep straight.

**RULE — the far-end type is REQUIRED, and three things depend on it.** It is not a convenience filter.

1. **One far-end probe instead of six.** §SEC14.3 rules 3 and 4 must be answered for the far end of every row returned, and the far ends are only known once the rows are read — a `Reaction` far end cannot be pre-resolved the way §ARC16.8 pre-resolves its vocabulary, because `ContentItem` and `Comment` far ends have no small vocabulary to close over. Naming the type makes that one probe over one type.
2. **It pins the orientation, so the read is an index seek.** Canonical order is decided by the two type names together (§DOM4.4 rule 1), so with both types known the anchor's side is known, and the predicate seeks `IX_Associations_EndpointA` or `IX_Associations_EndpointB` — the two indexes whose declaration in `StorageBroker.Association.Configurations.cs` already names this read, *"associations for this entity"*, and already records why there are two of them. **The one case where the side is not decided is a same-type pairing** — `ContentItem` ↔ `ContentItem`, `BibleReference` ↔ `BibleReference` — where the anchor may be on either end and both indexes are asked. That case is named here rather than discovered.
3. **It bounds the rows.** One facet of one anchor is what a panel renders; the anchor bound alone would not stop twenty-five items' comment threads arriving in one response.

**The alternative — an optional far-end type — is refused.** It forfeits all three: six probes instead of one, no orientation to pin so no seek, and an unbounded row count. A caller wanting several facets asks several times, which is what the client already does: each facet is its own panel with its own read.

**What a request costs** — **one HTTP request and up to four database round trips**, and the count does not move with the anchor count **on the arms this read is designed against**, because all four are set-shaped. **Two of those arms do not exist yet in that shape, and the cost model rests on them**, so this paragraph is design intent in the same sense §ARC16.8's shared-rule table row marks its own §DOM6.10 arm *designed, not built*.

1. **The endpoint probe, over the anchor set** — which of the supplied anchor ids are canonically visible, resolved to effective ids. **Caller-independent**: the §SEC14.1 predicate over a closed-over id list, deliberately not a by-id read whose posture widens for owners and reviewers. The caller widening composes beside the evaluator, per §ARC16.8's shared-rule table row.
2. `IAssociationService` — the shaped read: §SEC14.3 rules 1, 2 and 5 as the query-shaping function it authors (§ARC12.2.1 rule 3), plus the pinned endpoint predicate above.
3. **The endpoint probe again, over the far ends** — which of the distinct far-end ids round trip 2 returned are canonically visible. One type, bounded by that round trip's rows.
4. `IAccessBroker` — the winning setting of each host endpoint, for §SEC14.3 rule 6. **Scoped to a `ContentItem` host**, per the rule below.

**RULE — this read adds NO entity service, and the endpoint probe is `IAccessBroker`'s rather than six foundation services'.** Round trips 1 and 3 ask, of an arbitrary `EntityType`, the question the endpoint-resolution redesign collapses onto that broker — *does this row exist, and is it visible* — and the reason is the broker's own: resolving it means knowing which table an `EntityType` points at, which is the one thing a single-entity foundation service cannot work out for itself. **Reaching for the anchor type's and far-end type's own foundation services instead would put six entity services back on this class**, undoing the collapse §ARC12.5 entry 1's classification ruling depends on. §ARC16.8's read holds `IContentItemService` and `IReactionService` because its two types are **fixed** and it needs their **rows** — `Reaction.Name` and `Reaction.UnicodeEmoji` are in its projection; this read's types are parameters and it needs only the verdict, so the broker is the right instrument and the entity-service count is unchanged at the three entry 1 records.

**What that probe is today, stated rather than assumed.** `IAccessBroker` answers **three questions of exactly that shape** — `RetrieveEntityAuthorAsync`, `RetrieveEntityApprovalStatusAsync` and `IsEntityVisibleAsync` — which is what makes it the right home, and the redesign's own write-up hedges the claim in exactly those words. **It does not yet answer the question this read asks.** Measured on `IAccessBroker`: `IsEntityVisibleAsync(EntityType, Guid, CancellationToken)` takes **one row**, not a set, and its own documentation scopes it to *"it was read back and is not soft-deleted"* — which is §SEC14.3 rule **3** alone, where rule 4 needs the whole §SEC14.1 predicate, approval status and publish date included. So **a new set-shaped canonical-visibility arm is what rounds trips 1 and 3 need**, and until it exists round trip 1 is one call per anchor id and round trip 3 one per distinct far end. That arm is the endpoint-resolution redesign's to build — it is the same arm the collapse needs, asked of a set — and it is named here so that whoever builds this read meets the gap rather than discovering it in the cost model.

**RULE — round trip 4 answers §SEC14.3 rule 6 for a `ContentItem` host ONLY, and the other host is a build gap rather than a design decision.** The arm §ARC16.8's shared-rule table row places is keyed on `(ContentType, ContentItemId)`: it is a `ContentItemSetting` arm and answers for that host. §DOM6.10's host table names **two** settings entities, and the second is `BibleReferenceSetting` — which §ARC16.2.1 records as designed and not built, *"no type, no table, no service"*, so a `BibleReference` host resolves no settings entity at all today and **every** facet on such a host is ungated. That is §DOM6.9's build to close, not this read's, and §ARC16.2.1's warning is repeated here because this read is where it would otherwise be silently elided: the gap **must not** be dressed as §DOM6.10 rule 3, whose subject is a type with no settings entity *by design* — the far-end-only `Tag` and `Reaction` — because doing so would record a build gap as a decision and leave it standing permanently. **The consequence for this read, plainly:** where the anchor or the far end is a `BibleReference` host, round trip 4 has nothing to ask and rule 6 does not narrow the result. `BibleReference` ↔ `BibleReference` is this design's driving symmetric case, so that is not a corner.

**The composite is the ONE evaluator and this read writes none of its own** — §ARC16.8's shared-rule table row is its home, and this read feeds it from round trips 1, 3 and 4 — the near end, the far end and the winning setting, which are the same three inputs that read feeds it from. **`RetrieveAssociationByIdAsync` consumes the same evaluator over a single row**, which is the same question asked of a set of one — so it needs no bound, no far-end parameter and no cost model of its own beyond the four round trips above run at N = 1, and it gets no subsection here for that reason. **The count carries over but the ORDER inverts**: a by-id read is handed an id rather than an endpoint, so round trip 2 runs first and rounds 1 and 3 probe the endpoints it returns — there is no anchor to resolve ahead of the row. A row that fails the evaluator answers **not-found rather than forbidden** (§SEC14.5 rule 1), which is the one thing about it that is not simply this read at N = 1: a collection filters, a by-id read denies. No member writes a second copy of the evaluator, and it carries no caller term — §SEC14.7 posture A′ rule 2's review-tier widening and its public-plus-own predicate compose **beside** it, as that row records.

**§ARC12.2.1 rule 4 is NOT engaged, and what would engage it is worth naming.** Rule 4 is about the **shaped query**: the condition a service authors executes in SQL rather than over a materialised list. Rules 1, 2 and 5 are in that condition and stay there. Rules 3, 4 and 6 are cross-entity and by construction cannot be — which is why §ARC16.8 already resolves its rule 6 above the read and joins its `Name` and `UnicodeEmoji` above it too, with rule 4 unbreached. What would engage rule 4 is reading the association table unbounded and narrowing it in memory, and the anchor bound is precisely what keeps that out of reach.

**RULE — no `[EnableQuery]` on this read, and the query surface is closed rather than allow-listed**, on §ARC16.8's precedent and in its words: a route with no OData surface cannot take an ordinal `$filter` and cannot have its generated SQL un-capped. The reason is sharper here than there. The composite runs above the storage read, so what this member returns is a **materialised list and not a queryable** — and an `[EnableQuery]` over a materialised list is the trap in full: `$filter` degrades to LINQ-to-Objects and compares ordinally where the column's collation does not, while `$top` and `$skip` page a set the database has already handed over whole, so the cap the attribute appears to impose is imposed nowhere. The **absence is the contract** and is asserted as one, the way `ContentItemsController`'s feed read asserts its own.

**RULE — a site-wide unfiltered association list is REFUSED, and the need it looks like it serves is met elsewhere.** No caller may ask "every association", at any tier. A moderator's queue belongs to the approval surface, not to this entity's read: an association's round is decided at `POST api/Approvals/{entityType}/{entityId}/Decision` and read at `GET api/Approvals/{entityType}/{entityId}/Verdict`, both already built and both keyed on the entity. So nothing a moderator does is unreachable, and this read is not the place to rebuild it. §SEC14.7 posture A rule 4's three collection lines are unchanged by this: they say what the filter returns per tier, and the filter is untouched. What this rule constrains is the **addressing** — a collection read that takes an anchor is still a collection read, which §ARC16.8's own bounded set already demonstrates.

**Residual, recorded rather than solved: this route does not page.** One anchor holding very many associations of one facet returns them all, and the anchor bound does not cap that. No consumer is near it — a panel renders one entity's tags or one entity's bible references — and inventing a paging scheme for a pressure nothing exerts would be a second addressing scheme to keep in step with the first. Whoever meets that pressure designs paging then, on this route, and the facet that meets it first is the evidence to design against.

## ARC17. Recommended API Design *(formerly §17)*

### ARC17.1 Content Endpoints *(formerly §17.1)*

Recommended endpoints:

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `POST` | `/api/content-items` | Create content item draft. |
| `PUT` | `/api/content-items/{id}` | Edit draft or create updated version depending on approval state. |
| `GET` | `/api/content-items/{id}` | Retrieve content item version. |
| `GET` | `/api/content-items/groups/{groupId}` | Retrieve all versions. |
| `GET` | `/api/content-items/groups/{groupId}/latest` | Retrieve latest version. |
| `GET` | `/api/content-items/groups/{groupId}/published` | Retrieve published version. |
| `GET` | `/api/content-items/by-slug/{contentType}/{slug}` | Retrieve the published version by slug (§DOM19.4). |
| `POST` | `/api/content-items/{id}/submit` | Submit content item for approval. |
| `DELETE` | `/api/content-items/{id}` | Soft delete content item. |

### ARC17.2 Feed Endpoints *(formerly §17.2)*

Recommended endpoints:

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `GET` | `/api/feed` | Retrieve visible published content excluding topics. |
| `GET` | `/api/feed?contentType={name}` | Retrieve visible published content by content type. |

### ARC17.3 Topic Endpoints *(formerly §17.3)*

Recommended endpoints:

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `GET` | `/api/topics/{id}` | Retrieve visible topic landing page. |
| `GET` | `/api/topics/{id}/items` | Retrieve visible child items for a topic. |
| `POST` | `/api/topics/{id}/items` | Associate a content item with a topic. |

### ARC17.4 Association Endpoints *(formerly §17.4)*

**RULED — an association is addressed as its own resource, and the four content-item-anchored rows this table carried are WITHDRAWN rather than kept beside the rows below.** They were `POST /api/content-items/{id}/associations`, `POST /api/content-item-groups/{groupId}/associations`, `GET /api/content-items/{id}/associations` and `DELETE /api/content-item-associations/{id}`. Three separate rules each make them unservable as written, and the third would still bite if the other two were solved.

1. **No exposer could serve them.** A route under `api/content-items` belongs to `ContentItemsController`, which binds `IContentItemProcessingService` and may take no second business service — `the-standard-exposers` `contracts.json` sets `max_injected_services: 1`. An association's writes bind its **top-layer** service (§EVN13 rule 3), which for this entity is its orchestration, and §ARC16.2.1 already rules out an exposer reaching the association foundation at all. The class that serves this resource is `AssociationsController`, which §ARC16.8 and §ARC12.6 row 14 commit to independently of this ruling — so the question was never whether that class exists, only which routes it carries.
2. **`[Route("api/[controller]")]` is required rather than chosen.** That same `contracts.json` lists it under `required_attributes`, so a class's base path is fixed by its name and a route anchored under another resource cannot be declared on it.
3. **A symmetric association has no anchor to be addressed by.** §DOM4.1 — *"Both endpoints are generic and symmetric: neither is hard-wired to a `ContentItem`"* — and §SEC14.3's own note that the driving case `BibleReference` ↔ `BibleReference` has no content item at all. A path segment naming an endpoint that need not exist cannot address the resource, and the fourth row additionally names `ContentItemAssociation`, a type that survives only in migration history and is in no current model.

**Only this section's rows are withdrawn.** §ARC17.3's `POST /api/topics/{id}/items` is a topic surface with its own designed-and-unbuilt exposer, and nothing here reopens it.

Recommended endpoints:

| Method | Endpoint | Coarse gate | Purpose |
| --- | --- | --- | --- |
| `POST` | `/api/associations` | `[Authorize]` | Suggest a pairing — the retrieve-or-add flow of §ARC12.5 entry 1, whose result carries a status and a row id and nothing else. |
| `GET` | `/api/associations?entityType=&entityIds=&relatedEntityType=` | `[AllowAnonymous]` | The endpoint-anchored read (§ARC16.9): the visible associations of 1 to 25 entities of one type to one far-end type, filtered by §SEC14.7 posture A′ rules 5 and 6 over the §SEC14.3 composite. All three parameters are required; no `[EnableQuery]`. |
| `GET` | `/api/associations/{id}` | `[AllowAnonymous]` | Retrieve one visible association; a row the caller may not see answers not-found (§SEC14.5 rule 1). |
| `PUT` | `/api/associations` | `[Authorize]` | Modify an association. An association carries no caller-editable content (§APR7.5.1 rule 4), so the whole effective payload is the `Draft` ↔ `Submitted` carve-out (§APR9.2 rules 4–6). |
| `DELETE` | `/api/associations/{id}` | `[Authorize]` | Soft delete — owner or `Administrators`, on the split gate of §SEC14.7 posture A′ rule 4. |
| `DELETE` | `/api/associations/{id}/hard` | `[Authorize(Roles = Administrators)]` | Hard delete — `Administrators` **and** the endpoint veto (§SEC14.7 posture A′ rule 4). |
| `GET` | `/api/associations/reaction-summaries` | `[AllowAnonymous]` (§ARC16.8) | The reaction counts for up to 25 content items, with the caller's own reaction where they have one (§ARC16.8). Anonymous and caller-independent; no `[EnableQuery]`, so no query option changes the answer. |

**§SEC14.7's five state transitions have no rows in this table**, and their absence is not a claim that they are unreachable: each is designed with its own field scope, tier and fact, and nothing in this section places one on a route.

**Every row carries the route's SHAPE and not the served literal**, which is the convention §ARC16.8 names for this table's last row and which `ContentItemsController` and `LinksController` each record for §ARC17.1's. The literal follows `[Route("api/[controller]")]`, so the kebab-cased spelling here and the `GET api/Associations/ReactionSummaries` §ARC16.8 publishes are **one** route rather than two, and the same holds of every row above it.

**RULED — the two CRUD reads are `[AllowAnonymous]`, the three ordinary writes carry a BARE `[Authorize]`, and only the hard delete names a role.** (The seventh row, §ARC16.8's summary read, is `[AllowAnonymous]` on its own section's reasoning and is not one of the two this ruling is about.)

- **The reads.** Posture A′ is *"the endpoint-derived variant of posture A"* and inherits posture A rule 4, whose collection line ends *"anonymous callers see public only"*; posture A′ rule 5 says the same thing from the predicate's side — a caller with no scoped roles gets two empty sets and the query degrades to the public-plus-own predicate, which for an anonymous caller is the public set. An `[Authorize]` would answer `401` before that filter ran and refuse the public subset the rule grants. `TagsController` records exactly this reason on its own two reads, and `ContentItemSettingsController` and `ContentItemsController` put theirs the same way — three built instances of one rule rather than a precedent stretched to a new case: `Tag` is posture A and `Association` posture A′, and the read line is the same line.
- **The three ordinary writes.** The tier that governs them cannot be written as a role list at all. Posture A′ rule 2 composes it from the endpoints' denormalised types on the **stored** row, at two tiers each, so a `Tag-Publishers` may act on a `Tag` ↔ `ContentItem` pairing and no attribute can name that person — the role is composed from a column the attribute cannot see. `Roles.cs` carries no `Association-*` constants for the same reason. A role list here would be wrong in the **strict** direction, refusing a caller posture A′ admits, so the attribute asks for authentication only and the service decides. This is `ContentItemSettingsController`'s shape, and `TagsController`'s owner-branch reasoning one step further, because here even the moderator branch is row-derived.
- **The hard delete.** Posture A′ rule 4 makes it `Administrators` **and** subject to the endpoint veto. The role half is expressible, so the attribute names it and the service re-decides both halves.

**RULED — the collection read is ANCHORED and carries NO `[EnableQuery]`.** The question this ruling closes was how an unbounded collection read composes §SEC14.3 rules 3 and 4 — per-endpoint visibility, in other tables — without materialising. It does not: an orchestration authors no query condition (§ARC12.2.1 rule 3) and the foundation cannot evaluate a cross-entity rule (§SEC14.3's *Layer* paragraph), so the composite runs **above** the storage read, and the read is bounded by an anchor so that there is a fixed set for it to run over. §ARC16.9 has the shape, the 1-to-25 bound it borrows from §ARC16.8, why the far-end type is required, and why what comes back is a materialised list rather than a queryable — which is what makes `[EnableQuery]` not merely unnecessary here but actively wrong: over a materialised list `$filter` degrades to an ordinal LINQ-to-Objects comparison and `$top` caps nothing the database did not already hand over. The absence is asserted as the contract, on §ARC16.8's precedent.

### ARC17.5 Approval Endpoints *(formerly §17.5)*

> **The last five rows move to `ApprovalReviewersController` and keep their URLs** (§ARC12.5.4 business rule 6, §ARC12.6 entry 3a). They are listed here rather than in a table of their own because a reader looking up an approval URL should find it in one place; which controller class answers it is §ARC12.6's business, not a caller's.
>
> **Berean's endpoints are NOT in this table, and that is the same rule read the other way.** The AI reviewer is its own resource at `api/AIReviewers/{entityType}/{entityId}` (§APR8.6.2, §ARC12.6 row 13) rather than a sub-resource of the round, so it does not share the `api/Approvals` space these rows do — and a row with a foreign base path in a table titled *Approval Endpoints* would undercut the reason the five rows above stay. Named here so a reader looking for an approval-adjacent URL finds the pointer instead of concluding there is none.

Recommended endpoints:

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `POST` | `/api/approvals/{approvalId}/submit` | Submit for approval. |
| `POST` | `/api/approvals/{approvalId}/reviews` | Add approval review. |
| `POST` | `/api/approvals/{approvalId}/approve` | Approve when the approval conditions are met (`Publishers`/`Administrators`). |
| `POST` | `/api/approvals/{approvalId}/bypass-approve` | Approve without waiting for the conditions (bypass); sets `IsApprovedByBypass = true`. |
| `POST` | `/api/approvals/{approvalId}/reject` | Reject immediately (`Publishers`/`Administrators`). |
| `POST` | `/api/approvals/{approvalId}/comments` | Add approval comment. |
| `GET` | `/api/approvals/entity/{entityType}/{entityId}` | Retrieve approval for entity. |
| `GET` | `/api/Approvals/{entityType}/{entityId}/ReviewerCandidates` | Who may be asked to review — the §ARC16.7.4 candidates read; requesting tier only. |
| `GET` | `/api/Approvals/{entityType}/{entityId}/ReviewerDisplayNames` | What everybody this round names is called — the §ARC16.7.4 name resolver; requesting tier only. **Keyed on the round**, so the tier gate composes with an entity gate rather than standing alone: the set is the approval's review rows (dismissed and soft-deleted included, since the panel renders those), its outstanding invitations and the authors of its live comments, and nobody outside it is named — the review tier is not a source, so this route cannot be used to enumerate the moderator directory. Whoever may yet be asked comes from `ReviewerCandidates`, already named. The caller supplies no ids, so there is nothing to probe with, no batch to cap and no array to bind. No role or disabled filter over the round's own people, so a reviewer who has since left the tier still has a name. Ids naming nobody are absent, not an error; no approval on the key is a `404`. Each row carries an account id, a display name and the account's `UserName` — the last so a thread can disambiguate two people sharing a display name without printing a guid. |
| `POST` | `/api/Approvals/{entityType}/{entityId}/ReviewRequests?requestedUserId=` | Invite an eligible user to review (§APR7.9). Idempotent on both duplicate shapes; `204` on every success, because "already invited", "created" and "already answered" are outcomes a caller has no use for telling apart. The race dissolves too: two callers inviting the same person can both miss the check, and the loser's index collision is answered by re-reading and returning the winner's row. A `409` survives only where that re-read finds nothing — the winning invitation withdrawn in between. |
| `GET` | `/api/Approvals/{entityType}/{entityId}/ReviewRequests` | Who has been asked and has not yet answered — the read §APR7.9 was written around; requesting tier only, as §ARC16.7.4 places these rows under posture D. **Pending-ness is inherited, not asserted**: rule 5 soft-deletes a withdrawal and rule 6 retires an answer, so the foundation's visibility filter leaves exactly the outstanding set and there is no second definition of it to drift. |
| `DELETE` | `/api/Approvals/{entityType}/{entityId}/ReviewRequests?requestedUserId=&deletionReason=` | Withdraw a **pending** review request (soft delete, requesting tier); `204`. **Keyed on the round and the person**, matching the `POST` exactly, so withdrawal is that operation's undo rather than a second addressing scheme — and the row is resolved from the round rather than supplied. Refused with `400` once the invitation has been answered (§APR7.9 rule 5), which is reachable only where rule 6's retirement has not run. Nothing outstanding for that person is a no-op, not a `404`. |

### ARC17.6 Media, Share and Crawler Endpoints *(formerly §17.6)*

Recommended endpoints (§DOM5.6, §DOM19.6–§DOM19.8 — designed, not built):

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `POST` | `/api/attachments` | Multipart image upload (§DOM5.6.3). |
| `GET` | `/media/{attachmentId}` | Attachment bytes behind the visibility gate (§DOM5.6.2). |
| `GET` | `/s/{code}` | 301 to the canonical content URL (§DOM19.7). |
| `GET` | `/sitemap.xml`, `/sitemap-topics.xml` | Published-content sitemaps (§DOM19.6). |
| `GET` | `/robots.txt` | Crawler directives (§DOM19.6). |
