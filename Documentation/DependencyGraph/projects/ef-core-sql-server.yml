# EF Core / SQL Server — components and flows
# Part of the solution dependency graph — see README.md in this folder.
# Data only: index.html fetches graph.yml, then each project file, and
# assembles them. Strings with special characters are double-quoted JSON.

project: ext-efcore
components:
  - id: EXT.EFCore
    name: DbContext (EF Core / SQL)
    layer: external
    col: 11
    shared: true
    deriveMethods: true
    description: "The DbContext handed to EFCoreClient. In this solution that is Glory2Him.Core's StorageBroker itself (it derives from EFxceptionsContext and passes itself into EFCoreClient). IdentityCoreStorageBroker is a SECOND DbContext, over the security database, and queries EF directly rather than through EFCoreClient - it is read-only and needs none of the client's write surface."
    methods: []
