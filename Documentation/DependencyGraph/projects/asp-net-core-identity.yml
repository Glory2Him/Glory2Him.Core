# ASP.NET Core Identity — components and flows
# Part of the solution dependency graph — see README.md in this folder.
# Data only: index.html fetches graph.yml, then each project file, and
# assembles them. Strings with special characters are double-quoted JSON.

project: ext-identity
components:
  - id: EXT.Identity
    name: ASP.NET Identity
    layer: external
    col: 3
    shared: true
    deriveMethods: true
    description: "UserManager<AppUser> / SignInManager<AppUser> / IEmailSender<AppUser>. The WebApp talks to Identity directly from several endpoint groups and via IdentityBroker."
    methods: []
