# SkiaSharp — components and flows
# Part of the solution dependency graph — see README.md in this folder.
# Data only: index.html fetches graph.yml, then each project file, and
# assembles them. Strings with special characters are double-quoted JSON.

project: ext-skiasharp
components:
  - id: EXT.SkiaSharp
    name: SkiaSharp
    layer: external
    col: 3
    shared: true
    deriveMethods: true
    description: Image decode / crop / resize / WebP encode for profile avatars.
    methods: []
