// How a post presents itself in a listing. Presentation only — PostView does not carry this
// yet, so the layouts that need it pass it explicitly.
export type PostType = 'Standard' | 'Video' | 'Audio' | 'Gallery' | 'Quote';
