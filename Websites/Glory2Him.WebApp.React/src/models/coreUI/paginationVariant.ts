// Numbered: square numbered links with chevron arrows either side.
// Rounded: the same control with pill-shaped links.
// PrevNext: just "Prev" and "Next" — no page numbers.
export type PaginationVariant = 'Numbered' | 'Rounded' | 'PrevNext';
