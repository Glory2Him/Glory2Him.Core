{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "OData": {
    "//PageSize": [
      "Page size for the [EnableQuery] collection reads, applied by ODataPageSizeConvention. An",
      "action that names its own PageSize keeps it, and removing the key falls back to",
      "ODataPageSizeConvention.FallbackPageSize. It must be 1 or greater: a value that would serve",
      "these reads unpaged stops the host at startup rather than quietly removing the limit."
    ],
    "PageSize": 50
  },
  "FrontendConfiguration": {
    "YouVersion": {
      "AppKey": "your-real-key-here"
    }
  },
  "ConnectionStrings": {
    "Glory2HimSecurityConnection": "Server=(localdb)\\MSSQLLocalDB;Database=Glory2Him.Security;Trusted_Connection=True;MultipleActiveResultSets=true;ConnectRetryCount=5;ConnectRetryInterval=3",
    "Glory2HimConnectionString": "Server=(localdb)\\MSSQLLocalDB;Database=Glory2Him.Core;Trusted_Connection=True;MultipleActiveResultSets=true;ConnectRetryCount=5;ConnectRetryInterval=3",
    "EventHighwayConnectionString": "Server=(localdb)\\MSSQLLocalDB;Database=Glory2Him.Events;Trusted_Connection=True;MultipleActiveResultSets=true;ConnectRetryCount=5;ConnectRetryInterval=3"
  },
  "//EventEnvelopeSigning": [
    "Deliberately absent. This file ships to every environment, so a key here would silently",
    "become the production signing key and remove the only alarm: EnvelopeIntegrityBroker",
    "REFUSES TO START without one, so a host that was never configured never serves a request.",
    "That is the fail-closed behaviour we want, and it has to happen at boot — the broker used",
    "to defer it to the first publish, which lands after the write has committed and strands",
    "the row it was refusing (#392). A real value belongs in an environment variable or key",
    "vault; a local development key belongs in appsettings.Development.json, which is",
    "gitignored. Shape: EventEnvelopeSigning: [ { KeyId, Key, ActiveFrom, ActiveTo? } ]."
  ]
}
