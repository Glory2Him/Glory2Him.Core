@namespace Glory2Him.WebApp.Components.CoreUI

@* CoreUI Chart, ported onto ApexCharts (the charting library Blogzine ships) instead of
   EventHighway's Chart.js. Renders via the glory2himCharts JS interop module. *@

<div class="chart-wrapper" style="height:@(Height)px">
    <div id="@ElementId"></div>
</div>
