{
  "//": "Test-only key. Deliberately NOT 'Glory2HimConnectionString' — that is the production key StorageBroker reads, and this fixture drops the database it resolves.",
  "//database": "The Database given here is a template only; the fixture replaces it with Glory2HimCoreIntegration_<process id> so concurrent runs cannot collide.",
  "ConnectionStrings": {
    "Glory2HimIntegrationConnectionString": "Server=(localdb)\\MSSQLLocalDB;Database=Glory2HimCoreIntegration;Trusted_Connection=True;MultipleActiveResultSets=true;Connect Timeout=60;ConnectRetryCount=5;ConnectRetryInterval=5"
  }
}
