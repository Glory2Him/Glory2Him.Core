# Copilot Instructions

## Project Guidelines
- This solution requires explicit using directives (no global usings) and all namespaces must use block-scoped syntax (`namespace X { }`) — never file-scoped (`namespace X;`).